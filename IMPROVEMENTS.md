## 2026-07-24 イベント演出まとめ（第4陣）

- **重要人物の来訪時にBGM停止**（`js/game.js` `startVisit`）：客・主人公の停止（`G.paused`）は既にあったが、BGMは鳴りっぱなしだった。歩いてくる／ベンツが来る時点で`Sfx.bgmStop()`。田所・黒田・玲奈・鬼頭決着の全モーダルが対象になる（今まで無音だったのはみかじめ・ヤミ金だけ）
- **会話シーン（Story.play）を完全無音に**（`js/story.js` `Story.play`）：陰音階の専用曲「story」を流す設計から、作者指定で無音に変更。母の電話・親父との一幕・常連イベント・売却エンド・投票対決など、Story.playを使う全シーンに波及。エンディングだけは`checkGrandEnding`が`Story.play`の直後に`Sfx.bgm('ending')`で上書きする既存の仕組みでそのまま鳴る。使われなくなった`SONGS.story`（`js/sfx.js`）は削除
- **みかじめの来訪を「本人か若い衆か」で分岐**（`js/game.js` `startMikajime`/`seeOffMika`）：初対面と「付き合い」の集金だけ鬼頭本人がベンツで乗りつけ、それ以外の日常的な集金は若い衆（`NPC_LOOK.thug`）が他の来訪者と同じ足取りで歩いてくるように変更。見送り処理も来た手段で分岐させた
- **鬼頭との決着イベントをベンツ登場に変更**（`js/game.js` `startVisit`/`dismissVisitor`）：以前は他の重要人物と同様に歩いて来ていたが、決着だけは特別な場面としてベンツで乗りつける形に。`dismissVisitor()`もベンツの見送りに対応させた

## 2026-07-24 数値・画面の小変更（第2陣）

- **アメニティ仕入れ¥50→¥30**（`js/data.js` `CONF.soapUnitCost`）：¥50販売でも利益が出るように
- **みかじめ料のSE**（`js/game.js` `payMikajime`）：以前は完全に無音だった。日報や修理費と同じ「マイナスの現象」として、修理業者への支払いと同じ`Sfx.play('pay')`を鳴らす
- **データ欄の整理**（`js/game.js` `renderData`）：「曜日の客足」（判断に使えない数字）と「設備の状態」（第1陣でマップの耐久度バーと重複するようになった、設備1台ごとの%リスト）を削除。「いま抱えている注文」は「📌 タスク」に改名して先頭セクションへ移動
- **準備中（夜）にバイトが立ちっぱなし**（`js/game.js` `enterPrep`）：`G.staff`をクリアしていなかったため、前夜の営業終了位置にバイトが棒立ちのまま夜も描画され続けていた。準備中は`G.staff = []`にして、`startDay()`で毎回作り直す
- **準備中（夜）の主人公が実在しなかった**（`js/game.js` `enterPrep`/`frame`）：以前の`enterPrep()`は`G.player = null`にしていて、準備中は主人公キャラそのものが存在しなかった（＝何もできない）。`makePlayer()`で生成した上で、`frame()`のprep分岐から毎フレーム`updatePlayer`を呼び、汚れを見つけて掃除して回る、という営業中と同じ挙動を準備中にも与えた

## 2026-07-24 バグ・矛盾つぶし（第1陣）

作者の走り書きメモから、実際に遊べば必ず目につく5件を先に直した。難易度・数値バランスには触れていない。

- **サウナ不満の矛盾**（`js/game.js` `tempVariety`/`hasGentleSauna`/`pickHintKey`）：ミスト・塩(gentle)は元々 `tempVariety` の集計から除外されていたが、不満台詞は「もっと熱い／低温もほしい」の2択からランダムに出す1本のロジックのままだった。そのためミストを置いても「低温が欲しい」と言われる矛盾が起きていた。gentleサウナの有無(`hasGentleSauna`)と熱い方の幅(`tempVariety`)を別軸にし、`hintSaunaHot`／`hintSaunaLow` として分離。`facilityScore` にもgentleサウナぶんの加点を追加（今まで0点だった）
- **浴室専用アイテムの部屋制限漏れ**（`js/data.js`）：風呂・サウナ・水風呂・カラン計21種に `room:'bath'` が付いておらず、脱衣所にも置けていた。`roomOK`（既存の判定関数）はそのまま使えたので、データ側の指定漏れを直すだけで済んだ
- **ベンツのタイヤの回転方向**（`js/game.js` `drawBenz`）：車は常に左へ進む（`b.x -= spd`）のに、タイヤの模様は物理的に逆（時計回り）に描いていた。角速度の符号を反転して反時計回りに
- **cap:0設備の故障が見た目に出ない穴**（`js/game.js` `drawEquip`/`enterPrep`/`pickHintKey`/`selPanel`）：「故障中」表示・耐久度バー・開店前の修理ヒント・スタッフ推奨判定が軒並み `def.cap > 0` を条件にしていたため、洗面所・体重計・テレビ・扇風機・冷水機など「置くだけで効く」設備（cap:0）は、実際には壊れて機能停止していても見た目には一切出なかった。判定を全て `CONF.wearPerDay[cat] > 0`（=修理ロジックが元々使っていた基準）に統一
- **ととのいイス不在への無反応**（`js/data.js` `hintRest`／`js/game.js` `pickHintKey`）：休憩イスを1脚も置いていなくても、誰も文句を言わなかった。不満ヒントに追加

## 2026-07-23 フェーズ4：演出・SE・小物

作者の指示は「フェーズ4からいこう」。評判の厳格化（フェーズ2＝本丸）より先に、
**難易度に効かないもの**をまとめて片づけた。経済のレバー（客足・料金・評判）には一切触れていない。

### 湯船の見せ場（`drawFuroFx`）

画像は1枚も使わない方針なので、全部その場で描く。乱数ではなく時間と番号から作って、
毎フレーム泡が飛び回らないようにしてある。湯面（波）より後・客より先に描くので、
客が浸かっていても効果は見える。

- **ジェットバス**＝左右の噴き出し口の金具から、泡を巻き込んだ水流が中央へ伸びる
- **高濃度炭酸泉**＝細かい泡が底から湯面へ立ちのぼり続ける
- **電気風呂**＝左右の電極板＋ときどき走る黄色いビリビリ。**出しっぱなしにはしない**（目に痛いのと、常時光ると安っぽい）

### サウナの札を「灼熱」に（作者指定）

マップの温度札は、サウナだけ**数字ではなく熱さの言葉**にした。「110℃」と言われても熱さは伝わらないが、
「灼熱」なら一目で分かる。正確な室温は設備をタップすれば読める（パネルは今までどおり℃と帯の両方）。

ついでに帯のいちばん下（80℃未満）を **「マイルド」→「低温」** に改名した。
これで**ミストサウナ(60℃)・塩サウナ(55℃)の札が「低温」**になる＝作者の「ミストサウナを低温のサウナとして設定」が、
数値をいじらずに札の時点で伝わる。

### 番台の主人公・客の動線

- **番台についている間は、台の向こう側に立っている扱い**にした（`atBandaiPost`）。立ち位置ではなく番台そのものの上に描き、**台の高さで胴を切って頭と肩だけ出す**。番台の絵を描き直す必要はなく、描画をクリップするだけで済む
- **来る客は入口の右手から歩いてきて、帰る客は左手へ抜ける**。外の行列も右へ伸びるので、「捌けていない列」と「帰り道」が交差しない。実装は、来店時に立ち位置を右へ2.2マスずらして `outside` 扱いにする（既存の行列コードがそのまま使える）／退店時の経路の末尾に左端のマスを足す、の2か所だけ

### ヤクザが去るときに店が止まらない（作者指定）

みかじめの決着（払う／断る）がついた**瞬間に営業を再開**するようにした。
以前はベンツが画面外へ消えるまで店ごと止めていたので、去っていく車を眺めているあいだ、
客が全員かたまったまま棒立ちになっていた。ベンツの走行はもともと一時停止と無関係に実時間で動くので、
`resumeBiz()` を決着時に呼ぶだけで「連中が帰るところ」と「客の湯浴み」が同時に動く。
**叩き壊している最中だけは今までどおり止める**（見せ場なので）。

### 修理（作者指定で【🔧 修理】ボタンを復活）

- **代金は減った耐久のぶんだけ**（`fixFee`）。基準額は「壊れるまで待った場合の**平均**額」＝不調85%×購入額1.5% ＋ 大規模15%×購入額7%。
  これで**耐久1%あたりの単価はいつ直しても同じ**＝早く直しても損はしないし、得もしない。得られるのは金ではなく「営業中に止まらない」こと
  - 実測：ジェットバス(¥800,000)の平均額は¥18,600。耐久40%なら**¥11,000**、38%なら¥12,000
- **自分で呼べば準備中（営業時間外）でも来る**。NPCは phase を見ずに実時間で動いていたので、そのまま夜の店内を歩いて直しに来る
- **業者は1台直すたびに帰らない**。頼まれたぶん（作業中に別の設備で【🔧 修理】を押すと「ついでに」渡せる）を先に、次に壊れているものを、**いま立っている場所から近い順**に片づける。
  実測：ジェットバス(cond40)を呼ぶ → 作業中にロッカーを追加で頼む → **1回の来訪で 3台（¥11,000＋¥2,000＋¥3,000）を直して帰った**
- **日報の載せ方**は払った時間帯で分ける。営業中＝「修理業者への支払い」（経費）、準備中＝**「設備の修理」**（設備投資の行）。
  どちらも当日の日報に出て、`所持金 A → B` と合う（設備投資の集計は「日報を閉じてから次の日報まで」なので、準備中の出費はそのまま翌日の日報に載る）。
  修理しか無い日は行の見出しを「設備投資」ではなく**「設備の修理」**に差し替える（嘘くさいので）

### 数値（作者指定・叩き台）

- **手ぶらセット ¥300/¥400/¥500**（¥700は「その金でもう一回入れる」額なので廃止）。買う確率は¥300でほぼ全員→¥500で半分弱になるよう分母を¥900→¥400に詰め直した。「高い」と文句が出る閾値も¥700→¥500へ
- **シングルの水風呂：評判45・¥800,000**（地下水かけ流し＝評判55より**先に**解放）。
  8℃はキンキン好き（サウナー青年・サラリーマン）に刺さる代わりに、ぬるめ好きには冷たすぎる（`idealCold`）。
  あとから来る13℃は「万人に当たりのいい一台」として残る。ただし**シングルは電気代が重い**（run 4,000/日 vs 2,500/日）＝安く買えても毎日効く

### 効果音（`js/sfx.js`）

絵と同じで、**音のファイルは1つも持たない**（その場で合成する＝配信サイズが増えない）。狙いは昭和のゲーム機の音。

| 音 | いつ | 作り |
|---|---|---|
| ピッ | ボタンを押した／**カタログで設備を選んだ／マップで設置場所を選んだ** | 矩形波1180Hz・0.05秒 |
| ピピピピ | セリフが1文字ずつ出ている間 | 矩形波1500Hz・0.018秒を**3文字に1回** |
| チャリーン | 売上（`addFloater` の「+」） | 三角波1568Hz＋2093Hzを0.055秒ずらす |
| バコーン | 出費（`addFloater` の「-」） | 矩形波190→62Hzの落下＋低域ノイズ |
| **ガッチャーン** | 営業結果（日報）が開いた | ①打鍵②ベル③引き出しが出る④閉まる、の4つをずらして重ねる |
| **ブオーン** | 黒塗りのベンツが走っているあいだ | ノコギリ波2本を3.5Hzずらして唸らせ＋1オクターブ下の矩形波、低域だけ残す |

- ボタンは**個々に書かず、`pointerdown` を1か所で拾う**（付け忘れが出ないように）。ただし**カタログの行とマップは `<button>` ではない**ので、そこだけ個別に鳴らしている
- **チャリーンとガッチャーンは作り分ける**。前者は硬貨の澄んだ音（売上1件）、後者は「金属の引き出しが開いて閉まる」機械の音（一日の締め）。同じ系統の音にすると日報が「売上がもう1回入った」ようにしか聞こえない
- **エンジン音だけは単発ではなく鳴りっぱなし**なので別あつかい（`Sfx.engine(on)`）。ベンツが `wait`（停車）の間は切る＝**エンジンが止まる間が「連中が降りてくる」前触れ**になる。ぷつっと切らず0.3秒でしぼませる。効果音をオフにした瞬間にも止める
- 同じ音が重なって団子にならないよう種類ごとに最短間隔を置いた（客が増えると売上音が一斉に鳴る）
- ブラウザは操作前に音を出させてくれないので、最初のタップ・キー入力で目を覚ます
- ☰メニューの【🔊 効果音】で切替。設定はセーブではなくブラウザに残す（`orenoSauna_sfx`）＝はじめから始めても消えない

### BGM（作者の質問「BGMを付けられるの？」）

付けられる。効果音と同じで**曲のファイルも持たない**＝譜面を文字で書いて、その場で鳴らす。

- **譜面はただの文字列**。8分音符ごとに1マスで、`C5`＝その高さで鳴らす／`-`＝前の音をのばす／`.`＝休み。
  `SONGS` を書き換えれば曲が変わる（再生側のコードは触らなくていい）
- **音符は0.2秒先まで前もって予約する**。画面の描画に合わせて鳴らすと、客が増えて処理が重くなった瞬間にリズムがよれるが、
  予約しておけば時刻はブラウザの音時計が守ってくれる。タブが裏に回ってタイマーが間引かれた時は、
  戻ってきた瞬間に溜まった音符をまとめて鳴らさないよう現在時刻に置き直す
- **1曲目＝営業中の「夕凪湯のうた」**（96拍・16小節・約40秒でひと回り）。
  音階は**ヨナ抜き長音階（ドレミソラ）**＝昭和の歌謡曲や童謡の音階で、暖簾をくぐる銭湯の空気にいちばん近い。
  前半8小節が「いつもの夕方」、後半8小節が「少し上を向く」。編成は主旋律（三角波）＋ベース（矩形波・低音だけ通す）＋ドンとチッの2点だけ
- **場面ごとに4曲**（作者指定で追加）。曲を分けるより先に「**同じ音階のまま速さと編成を変える**」ことで、
  同じ店の時間が動いている感じを出している。場面が変わったと耳で分からせたいストーリーだけ、音階そのものを変えた
  | 場面 | 曲 | 作り |
  |---|---|---|
  | 営業中 | 夕凪湯のうた（96拍・16小節・40秒） | ヨナ抜き長音階／主旋律＋ベース＋ドンとチッ |
  | 準備中（夜） | 72拍・8小節・27秒 | 同じ音階のまま、太鼓なし・丸い音・音量0.8 |
  | ストーリー | 66拍・8小節・29秒 | **都節（ラ・シ♭・レ・ミ・ファ）**／いちばん薄く・音量0.62 |
  | エンディング | 108拍・16小節・36秒 | 営業中と同じ音階を、速く・高く・厚く |
- **みかじめ料と取り立てのあいだは無音**（作者指定）。曲を止めると、ベンツのエンジン音と静けさだけが残る＝
  緊張は音を足すのではなく**引く**ことで作る。決着して営業が再開したら、また営業中の曲に戻す
- **日報も静か**にしてある＝レジのガッチャーンが立つ
- **入り切りは効果音と別**（`orenoSauna_bgm`）。「音は出したいが曲は繰り返すとうるさい」という人が必ずいるため
- 実装中の事故：**再生状態を持つプロパティ名 `bgm` を、メソッド名 `bgm()` と衝突させていた**（後から書いたメソッドが勝って、状態が常に関数になる）。`cur` に改名して解決
- **「押せるもの」の判定を1か所にまとめた**（作者指定「とにかくボタンを押した時はピッ音」）。
  `Sfx.HIT = 'button, .shop-item, .ad-item, .chip, .tab'` で拾う＝広告のチラシ配り・地元ミニコミ誌のように
  **見た目はボタンでも `<button>` ではない行**もこれで鳴る。押せる見た目のものを足したら、この1行に足すだけでいい

**曲そのものは叩き台**。セリフや数値と同じで、作者が聴いて判断するもの。譜面は文字列なので、
「もっとゆっくり」「暗く」「ここのフレーズだけ変える」は `SONGS.biz` の中で完結する。

### ヤミ金の取り立てに【完済】を足した（作者指定）

取り立ての画面が「本日ぶんを払う」か「待ってくれ」の二択しかなく、
**今日ぶんだけ払い続けると利息5%/日で元金が減らず、いつまでも通ってくる**状態だった。
金があるならその場で縁を切れるように、**「💰 完済する（元金＋今日の利息）」**を真ん中に足した。
額は `yamiPayoff()` に一本化し、**融資画面の【一括で返す】も同じ額に揃えた**
（以前は融資画面だけ利息を取らずに元金だけで完済できていて、取り立て画面と食い違っていた）。

### 実測（10日・小さめの店／副作用が出ていないかの確認）

客14→31人・満足度56〜86・「たどり着けない」警告0件・待ちきれず帰った客0人。
動線と番台の変更で経路が壊れていないこと、修理の作り替えで金の出入りが合っていることを確認済み。

---

## 2026-07-23 「飾り」を作れないようにする（通路に面していない設備）

作者の指摘：「誰も使わないアイテムがあればユーザーに知らせたいし、それはただの飾りで何の効果もないことを表示したい。もしくは通路に面していないアイテムも利用されるようにしたい」。

**「使えるようにする」ではなく「作れないようにする」**を選んだ。狭い土地の配置パズルが夕凪湯の遊びの本体で、囲まれた台も使えるようにすると壁ぎわを2列で埋めるのが常に最適解になり、通路を考える理由が消えるため。絵の問題もある（客は手前のマスへ歩いたあと設備のマスへスライドして使うので、奥の台を使わせると手前の台をすり抜ける）。

### 置く時点で弾く

- ロッカー・洗い場の例外（`bankCat`＝「同じ種類のどれか1台に手が届いていればいい」）を**廃止**。これが奥の列＝飾りを生む唯一の入口だった
- `placementValid` を `placeCheck` に作り替え、`{ok, why, lost}` を返す。**置けない理由**を確認バーとトーストに出し、**巻き添えになる設備そのもの**（`lost`）を画面で光らせる
  - `drawLostMarks()`＝使えなくなる設備を**赤い斜線でハッチし、⚠を重ねて点滅**。文章を読まなくても、どれが死ぬのかが見て分かる
  - 確認バーは横に短いので、理由がある時は**理由だけ**を出す（「ここ（赤いマス）には置けない：」を前置きすると折り返してボタンを潰した）
  - 既にある設備を切り離す置き方 → 「⚠ 利用不可になるアイテムがあります（新しいロッカー）」（作者の言い回しをそのまま）
  - 置こうとしている物そのものが孤立する → 「⚠ 通路につながっていません」
- **作者が心配していた「後から塞がれる」ケースは、これで丸ごと消える**。placeCheck はゴースト配置したうえで**全設備**の到達性を見直すので、隅からロッカーを並べていって最後の1個が手前の列を殺す、という置き方はその1個が弾かれる。設備を減らす操作（売却）で到達性が悪化することはないので、**一度置けた設備が後から飾りになる経路はもう無い**

### 詰みを避ける（実装中に見つけた事故）

最初の実装は「1台でも到達不能なら置けない」だったが、これだと**古いセーブに飾りが残っていると盤面のどこにも何も置けなくなった**（既にある飾りのせいで毎回 false）。しかも「利用不可になるアイテムがあります」と、無関係な設備のせいで怒られる。

判定を**「この置き方のせいで新しく切り離される設備」だけ**に直した。置く前の到達集合で既に切れている物は、最初から居ないものとして扱う。

### 残ってしまった飾りの見せ方（古いセーブ用）

- `refreshDead()` が各設備の `it.dead` を洗い直す。呼ぶのは**買う／動かす／売る／開店する／セーブを読む**時
- マップ上では**暗く落として⚠**を重ねる。タップすると設備パネルの先頭に赤枠で
  「⚠ 通路につながっていません／まわりを設備で囲まれていて、誰もたどり着けません。置いてあるだけで、客も使えず、店の充実度にも数えられません。まわりの設備を動かすか、この設備を売ってください。」
- `usable()` が `!it.dead` を見るようになったので、**客の候補から完全に外れる**（無駄な「たどり着けない」吹き出しも出ない）
- `facilityScore()` からも除外＝**本当に何の効果もない**。売却額は通常どおり（作者指定）

### ついでに直した穴

`needsAccess()` は冷水機・扇風機・洗面所を「置いてあるだけの物」として到達チェックの対象外にしていたが、この3つは実際に歩いて行って使う（`PAS_USE`）。囲んで置けてしまい、しかも充実度には数えられていた。`PAS_USE_IDS` を作って対象に含めた。

### 実測

| | |
|---|---|
| 壁ぎわに`wash1`を詰めていく | 3台目（奥の列になる位置）から「利用不可になるアイテムがあります（新しいカラン）」で拒否 |
| ロッカー1列の手前を塞ぐ | 同じく拒否 |
| 古いセーブ相当（飾り2台）で普通の場所に置く | **置ける**（詰まない） |
| 古いセーブ相当で1日営業 | 飾り2台は**利用0回**／隣の生きた2台は各10回。「たどり着けない」の誤警告なし |
| 飾り2台ぶんの充実度 | 20.0 → **12.3**（数えなくなった） |
| 新ルールで11設備を置いて6日営業 | 飾り0・詰まり警告0 |

---

## 2026-07-23 親父の治療費（改善案フェーズ1）

経済のレバーを**1本だけ**入れて実測する回。入れたのは親父の治療費のみ。

### 仕様

- **10日ごとに¥300,000**（`CONF.careCost` / `careEvery`、初回は5日目 `careFirstDay`）。次回請求日は `G.careNext` に持ち、セーブにも載せた（旧セーブは「読み込んだ日＋10日後」から数え直す）
- **店の経費ではない**＝営業の収支（`profit`）には入れない。設備投資と同じく「営業の外で出ていった金」として扱い、日報の**手残り**だけを削る。ヘッドラインの見出しはその日の中身で変わる（設備投資のみ／治療費のみ／両方）
- 払っていない日も日報に**「あと◯日で ¥300,000」**を出す。いきなり30万が消える日報は理不尽なので、必ず先に見えているようにした
- 初回の夜だけ母からの電話（`STORY_CARE`・セリフは叩き台）。導入で「親父の貯金は入院と療養に消える」と言っているので、**その貯金が底をついた**という形で主人公に回ってくる

### 終わりを「退院」ではなく「和解」にした理由

TODO の当初案は「親父が退院すれば自然に終わる」だった。実装して測ったら、**退院（＝評判30で親父が店に現れる `G.flags.father`）は5日目に来た**。初回請求が5日目なので、**1回¥300,000払って終わり**＝固定費として何の意味も持たない。

そこで終了条件を `G.solved.oyaji`（親父と和解＝自分の足で立ち直る）に変えた。入院費が療養費・リハビリ費に変わるだけで筋は通るし、**親父が番台に戻るまで**という物語の時計と、固定費の重さがそのまま同期する。判定は `careOn()` の1関数にまとめてある。

### 実測（設備13個・追加投資なしで30日）

| | 金額 |
|---|---|
| 営業の収支の合計 | **+¥557,900**（1日平均 +¥18,600） |
| 治療費（5・15・25日目） | **-¥900,000** |
| 通算の手残り | **-¥342,100**（所持金 ¥3,440,000 → ¥3,097,900） |

30日を通して自動借入もヤミ金も一度も発生していない。**店を育てずに現状維持していると目減りする**という圧はかかるが、死ぬほどではない、という形になった。評判は52.5・常連37人まで伸びている。

### 測っている途中で見つかったこと（フェーズ2送り）

`facilityScore()`（充実度＝料金への納得感と評判に効く）が `usable()` を見ていないので、**誰にも使われない台でも充実度が上がる**。壁ぎわに `wash1` を5台並べると2台が「立てるマス0」になり、客26人・丸1日を通して利用回数0回（他は7・8・7回）。それでも充実度は **12.3 → 20.0**。「通路に面していないカランを並べるだけで評判が上がる」抜け道になっている。塞ぐのは1行だが**難易度を上げる修正**なので、評判の厳格化とまとめて測る。

---

## 2026-07-23 日報に設備投資・新規／常連・掃除の詰まり（改善案フェーズ0）

作者のプレイ後の殴り書きから、**難易度に触らない4件だけ**を先に着手した。「初日から儲かりすぎて面白くない。とはいえ金は残らない」という矛盾の正体は経済ではなく**日報の見せ方**だったので、経済のレバー（親父の治療費・客数の抑制・料金の目安・評判の厳格化）に手を付ける前にここを直している。

### 日報に設備投資を出す

作者の指摘「日報では利益が出てるけど、実際は設備購入でなかなか利益が出ない。今の営業レポートでは、めっちゃ簡単に儲かってる気になってしまう」。

- 「本日の収支」（営業利益）の**すぐ下に「設備投資をふくめた手残り」**を並べる。設備を買った日だけ出る（`.rep-headline.sub`）
- 経費の下に**設備投資の行**（購入／移動／売却の内訳つき）と、**所持金 ¥A → ¥B** の行
- 集計は `G.invBuy` / `G.invMove` / `G.invSell`。**締めは `afterReport()`**＝「日報を閉じてから次の日報まで」。設備を買うのは主に準備中なので、この区切りだと「明日のために使った金」が明日の日報に出る。セーブにも載せた
- 実測（1日目・設備7個で¥1,540,000）：**本日の収支 +¥13,850／手残り -¥1,526,150**。緑と赤が並んで出るので、儲かっている気になりようがない

### 新規客・常連客の内訳

- `G.regulars` ＝夕凪湯を**行きつけにしている人数**を積み立てる。満足度62以上で帰った新規の35%が常連になり、満足度42未満で帰った常連は離れる（`CONF.regular*`）
- その日の来店に占める常連の割合は「常連の数×来店頻度(3日に1回) ÷ 来店見込み」。**開店時に固定**する（今日はじめて常連になった人が、その日のうちにもう一度来ないように）
- 日報に「新規 22人・常連 34人」と「常連の数 152人（+2人が行きつけに／-1人が足を遠のかせた）」
- **いまは表示だけで、1日の来店数そのものには効かせていない**（客数の式は従来どおり）。広告→新規→常連 の循環を数字として先に回しておき、集客に効かせるかは作者の判断待ち＝**難易度を動かさないための線引き**
- 実測（評判45の店）：常連の割合が 1日目0% → 10日目30% → 35日目43%、常連の数150人。**休日は来店見込みが増えるぶん新規の比率が上がる**（＝休日に新しい顔が来る）という挙動が勝手に出た

### 掃除に行けなくなるバグ（作者の質問「今ってどんな条件の時に、従業員が掃除に行けなくなってる？」）

`maintain()` が**いちばん近い汚れ1つだけ**を見て、そこへの道が無いと何もせずに終わっていた。フォールバックが無いので次のフレームも同じ届かない汚れを選び直し、**他に掃除できる汚れがあっても永久に棒立ち**になる。設備で囲まれた汚れが1つでもあれば発生する。

→ **近い順に、実際にたどり着ける汚れを探す**ように変更。実測：到達不能な汚れ＋到達可能な汚れ2つを置いて、修正前は「最も近い＝到達不能」を選んで掃除0個、修正後は到達可能な2つを掃除した。

### 「たどり着けない」の赤い吹き出し

通路を塞いでいることは、これまで**どこにも表示されていなかった**（客は黙って待ち続けるか、黙って帰っていた）。運営メニューで直せる不満（赤枠＋「！」）とは別の印として、**赤枠＋「⚠」の濃い赤字**を追加（`stuckBubble` / `stuckAt`）。1人につき6秒に1回まで。営業ログにも1日1回だけ出す。

検出しているのは、設備／満員待ち／アメニティ置き場／ロッカー／番台（客）と、汚れ／番台（主人公・スタッフ）。**`placementValid` はロッカーと洗い場だけ「同じ種類のどれか1台に届けばOK」なので、並べた奥の台が実際に到達不能になりうる**＝作者が不満に挙げていた「通路に面してない部分が客に利用されない」問題は、まずここで見えるようになる。

---

## 2026-07-23 故障を2段階に・曜日システム（作者指定）

作者の指摘「¥2,400,000のフィンランド式サウナは1回の故障で¥168,000くらいかかるんじゃないか、現実の世界では」＝**そのとおりで、修理費に上限を付けるという前案が間違いだった**。60日回して故障を全部記録したところ、**現実的でなかったのは金額ではなく頻度**だった（年間の修理費が購入額に対して フィンランド式サウナ70%／シングルの水風呂105%／ロッカー131%）。

あわせて作者の指摘「そもそも夕凪湯は家賃もローンもゼロだから、利益率は高くなりそう」＝**利益率10〜15%という以前の目標を撤回**。修理費で無理やり押さえつけるのをやめた。

### 故障の2段階化

壊れた瞬間に規模を抽選して `it.fault` に残す（`breakEquip`）。修理代はその規模で決まる（`repairCost`）。

- **軽微な不調（85%）＝購入額の1.5%**（最低¥3,000／古い設備¥5,000）。パッキン・部品交換・詰まりの類い
- **大規模修理（15%）＝購入額の7%**（最低¥7,000／古い設備¥14,000）＝**¥168,000は現実の比率どおり据え置き**。数年に一度のオーバーホール
- **ヤクザに叩き壊されたぶんは必ず大規模修理**（`breakEquip(it, true)`）
- 日報・ログ・設備パネルに規模を明記（「🔧 不調（部品交換で直る）（業者が来る ¥24,000）」／「🔧 大がかりな修理が要る（業者が来る ¥112,000）」）。代金が桁で違うので、どちらなのかが分からないと納得できない
- `it.fault` はセーブに載せ、修理完了時に `null` へ戻す。旧セーブの故障中の設備は「軽微」扱い（安いほうに倒す）

**実測（完成した店・60日）**：故障62回のうち**大規模8回＝12.9%**（設定15%）。年間の修理費が購入額に対して **7〜50%**（旧70〜105%）に。売上に占める修理費は **43.4% → 19.9%**

### 曜日システム

- **1日目＝月曜**。土日が休日で、**平日の客数は休日の70%**（`CONF.weekdayGuestRate`）
- 倍率は**週の平均がちょうど1.0になるよう正規化**してから掛ける（→**休日1.27倍・平日0.89倍**）＝曜日を入れても週あたりの客足の総量は変わらず、日ごとの濃淡だけがつく。`guestMul`（1.3倍）はそのまま
- 表示：トップバー「15日目（月）」／日報の見出し「5日目（金）の営業結果」／メニュー／データ画面に「曜日の客足 89%」
- **日報の末尾に翌日の予告**「📅 明日は6日目・土曜日。休日は客足が伸びる」＝**広告をどの日に打つかを選べる**ようにするため
- **実測**：平日/休日の実客数比は 序盤の店**69%**・設備を絞った店75%・完成した店92%。狙いの70%が出るのは店に余力がある間だけで、**満員に近い店では圧縮される**（土曜でも入れる人数に上限がある＝正しい挙動）

### 変更後の利益率（15〜21日ならしの実測）

| 店 | 客/日 | 売上/日 | 光熱＋水道 | 修理費 | 利益率（前→後） |
|---|---|---|---|---|---|
| 序盤・評判25 | 31人 | ¥34,581 | 27.0% | 31.4% | 12.9% → **35.1%** |
| 設備を絞った店・評判55 | 57人 | ¥63,757 | 19.6% | 17.7% | 27.8% → **42.7%** |
| 完成した店・評判90 | 74人 | ¥100,976 | 20.7% | 19.9% | 12.8% → **35.7%** |

家賃もローンもゼロの店として妥当な水準。3店の差も 12.8〜27.8pt → 35.1〜42.7pt と縮んだ（＝どの規模でも同じくらい儲かる）。

---

## 2026-07-23 光熱費・水道代を変動制へ＝「設備を絞った店だけが儲かる」歪みの解消（作者指定）

作者の問い「水道費光熱費を変動制にするか。売上の20%くらいか？」への実装。**結論＝変動制にした。ただし売上比例ではなく客数比例**にし、結果として売上の20%前後に着地させた。

**なぜ売上比例にしなかったか**：売上に比例させると**入浴料を上げるたびに水道代が上がる**ことになる。使う湯と水の量は値段では変わらないので、値上げが自動的に増税になってしまい、直前に作り直したばかりの料金設定の判断が濁る。客数に比例させれば「繁盛するほど経費も伸びる」という狙いはそのまま満たせて、売上比も結果的に20%前後に収まる。

**実装**（`dailyUtil` / `dailyWater`）＝**「基本（設備の待機ぶん）＋従量（客1人あたり）」の二階建て**

- `CONF.utilStandby = 0.2` … 設備の`run`・水道代のうち、**客が来なくてもかかる割合**。残りの8割は従量へ移した
- `CONF.utilPerGuest = 60` … 客1人あたりの光熱費（湯を沸かし直す・室温を保つ）
- `CONF.waterPerGuest = 40` … 客1人あたりの水道代（かけ湯・シャワー・洗い場・清掃）
- **温度上乗せ（`tempSurcharge`）だけは客数に関係なく満額**。設定温度は「毎日の固定費」というプレイヤーの選択なので、従量に薄めない
- **日報に内訳を出す**：「光熱費 -¥15,600／基本 ¥11,460＋客69人 ¥4,140」「水道代 -¥4,720／基本 ¥1,960＋客69人 ¥2,760」＝客が増えて経費が伸びた理由が読める
- **設備パネルの表示も待機ぶんに**：「光熱費: ¥900/日（置いているだけでかかる分。残りは客1人¥60の従量へ）」。満額を出したままだと嘘になる

**実測（同じ3店で変更前後を比較）**

| 店 | 設備数 | 客/日 | 売上/日 | 光熱＋水道（旧→新） | 売上比（旧→新） | 利益率（旧→新） |
|---|---|---|---|---|---|---|
| 序盤・評判25 | 12 | 29〜30人 | ¥33,000 | ¥13,400 → ¥9,240 | 41.5% → **27.6%** | −3.3% → **+12.9%** |
| 設備を絞った店・評判55 | 16 | 60人 | ¥68,000 | ¥16,050 → ¥12,797 | 23.6% → **18.8%** | 23.2% → **27.8%** |
| 完成した店・評判90 | 34 | 74〜76人 | ¥102,000 | ¥49,100 → ¥20,840 | 47.0% → **20.4%** | −7.5% → **+12.8%** |

- 固定制のままでは**設備の数だけで決まって客が何人来ても1円も動かず**、売上比が23.6%〜47%とばらばらだった。変動制で18.8〜27.6%に収束
- 序盤だけ高いのは**基本料金¥4,500が固定で乗る**ため＝小さい店ほど固定費が重い、は意図どおり
- **残った歪みは修理費**。設備を絞った店の利益率がまだ高め（27.8%）なのは、修理費が`購入額の7%`で**設備の数と単価に紐づく**から。¥2,400,000のフィンランド式サウナは**1回の故障で¥168,000**＝完成した店の1日の売上を超える。**修理費に上限（例：¥60,000）を設けるのが次の一手**（作者判断待ち）

---

## 2026-07-23 料金の作り直し・経費の見直し＝「一生黒字にならない」問題の解決（作者指定）

作者の問い「まさか一生黒字化しない？」に対する**実測の答えは「はい、しませんでした」**。評判85・設備36個・客60人（上限）の“完成した店”で **1日 −¥58,000〜−¥93,000**。固定費 約¥118,000/日（光熱費50,600／水道9,450／**修理49,570〜85,200**／人件費9,000）に対して、売上の天井が **客60人×¥1,000＋サウナ42人×¥600＝¥85,200** しかなく、**どう遊んでも構造的に赤字**だった。以下は作者指定の数値でこれを作り直したもの（※すべて叩き台）。

- **入浴料 ¥600/¥700/¥800 ＋自由（¥500〜¥1,000）**、**サウナ料 ¥300/¥400/¥500 ＋自由（¥200〜¥700）**
  - 既定値も ¥480→¥700／¥300→¥400 に。**集客と満足度の基準額**（旧`480`／`380`のベタ書き）を `FEE_BASE=700` / `SAUNA_FEE_BASE=400` として切り出し、「安いほど客が増える」「高いと満足度が下がる」の基準を新しい価格帯に合わせた
  - **納得される額**の下限も `380 + 充実度×6` → `FEE_RANGE[0](=500) + 充実度×6`、サウナ料の目安も `SAUNA_FEE_RANGE[0](=200) + 充実度×6` に
  - 「安っ！また来る」の台詞は**いちばん安いボタン（¥600）以下**で出るように（旧：¥380以下）
  - 自由スライダーの説明欄を **「¥500〜¥1,000まで選べる（今の目安：¥○○）」** に変更（○＝いまの設備で納得される額）。※作者指示は「¥500〜¥3000」だったが、スライダーの上限も納得額の天井(`FEE_CEIL`)も¥1,000なので、実際に選べる範囲を書いた。**¥3,000まで選べるようにするなら上限自体を上げる**必要がある
  - **田所の「値下げしろ」の注文は ¥430以下 → ¥500以下**（新しい下限＝自由スライダーの最低額）
- **修理費を購入額の12%→7%**（古い設備は¥25,000→¥14,000、最低額¥8,000→¥7,000）
  - 作者指定は当初4%だったが、**「30日ならしで利益率10〜15%」**という後からの指定に合わせて実測で詰めた（4%＝完成した店で利益率30%超／6%＝18.7%／**7%＝12.3%**）
- **光熱費を0.5倍**：`CONF.baseUtil` 9,000→4,500、**全設備の`run`を半分**（大型サウナ9,000→4,500など）、温度上乗せ `tempCostPerDeg` 100→50
- **サウナマット・垢すりタオルの経費を¥1,000/¥800→どちらも¥500/日**
- **客足1.3倍**：`CONF.guestMul=1.3`。あわせて**1日の上限を60→120人**（`CONF.guestMax`）＝上限60のままだと1.3倍がそのまま頭打ちで消えてしまうため
- **牛乳・ドリンクの仕入れ ¥50/本**（作者指定）＝`CONF.milkUnitCost`。売価¥130なので1本¥80のもうけになった（それまで牛乳は仕入れ0＝丸儲けだった）
- **日報の経費を「仕入れ」1行にまとめた**（作者指定）＝タオル経費・アメニティ経費・牛乳仕入れを統合し、右側に「タオル -¥1,000／アメニティ -¥2,850／牛乳 -¥1,000」と**項目と金額だけ**を縦に並べる（「設置¥1,000 + 洗濯¥3,000」のような内訳の説明は廃止）
- **設備の耐久度を1.5倍**（作者指定）＝`CONF.durability = 1.5`。日々の劣化（`applyDailyWear`）も、使われるたびの摩耗（`finishUse`）も、この値で割る。ロッカーが壊れる周期が10日→15日に
- **結果（30日ならしの実測）**
  | 店の段階 | 客/日 | 売上/日 | 収支/日 | 利益率 |
  |---|---|---|---|---|
  | 評判90・設備36個・¥800/¥500・スタッフ2 | 97人 | ¥140,787 | **+¥16,281** | **11.6%** ← 狙い通り |
  | 評判25・設備12個・¥700/¥400・スタッフ0 | 59人 | ¥75,509 | +¥15,072 | 20.0% |
  | 評判55・設備16個・¥700/¥400・スタッフ1 | 80人 | ¥97,365 | +¥43,942 | 45.1% |
  - **10日の測定では利益率が7%〜46%まで暴れた**（修理はまとまった額が不定期に飛ぶため）。作者の言う通り**30日ならさないと利益率は測れない**ので、以後の計測は30日を基準にする
- **残っている歪み＝設備を絞って評判だけ上げた店（上表の45%）**。経費が「設備の数」に紐づき「客数」には紐づかないため、**設備を増やさない店ほど利益率が高い**。詰めるなら「客1人あたりの湯・水・清掃コスト」を足すのが次の一手（＝繁盛するほど経費も伸びる）

## 2026-07-23 田所の顔合わせ・立ちシャワーの絵・初期資金500万（作者指定）

- **田所の「顔合わせ」イベントを1営業日目に追加**（作者指定：「今はいきなり要求を突きつけてくる。その前が欲しい」）
  - `G.tadokoro.hello` を新設。**1営業日目の午前中（開店60〜190分後＝10〜12時台）**に田所が入ってきて、要求も選択肢もない**名乗りだけ**を置いていく（「わしは田所。お前さんがガキの頃から知っとる。……夕凪湯を継ぐのは構わんが、わしらの憩いの場を壊さんでくれよ」※セリフは叩き台）
  - ボタンは1つ（「……ああ、覚えてるよ」）。常連との絆+2。名乗る前なのでログは「白髪の爺さんが、勝手知ったる足取りで入ってきた…」と名前を伏せる
  - 本題（`intro`＝「こんな店にしちまって」＋共存/サウナ路線の二択）は**今まで通りサウナ設置＋評判15**から。顔合わせの後は2〜4日空ける
  - 既存セーブは`hello`が無い＝未挨拶扱いだが、すでに`met`済みなら顔合わせは出ない
- **立ちシャワーに専用の絵**（作者指定：カランと同じ絵だった）
  - カラン＝鏡・桶・腰掛け／**立ちシャワー＝頭上のシャワーヘッド＋立ち上がり配管＋足元の排水口**。湯が上から降り続け、足元で跳ねる
  - **使用中の客の描き分け**も追加：カランは座って頭を洗い泡が立つが、**立ちシャワーは泡を立てず、頭から湯を浴びて立っている**（`e.use.item.id === 'wash_shower'`で分岐）
- **初期資金150万→500万・信用金庫の限度300万→500万**（作者指定：「150万スタートはキツすぎる」）
  - `CONF.startCash` 5,000,000／`CONF.loanMax` 5,000,000。導入ストーリーの「俺の口座に眠る150万円」も500万に差し替え
  - サウナ80万＋水風呂40万＋ロッカー・カラン・イスを揃えても200万以上残る＝**1週目から「箱を整えてから開ける」**余裕ができた
  - ※`KURODA_CASH_OK`（手元50万）と`KURODA_DEBT_OK`（借金300万以下）は据え置き。初期資金が増えたぶん前者はほぼ自動達成になるので、作者判断で上げてもよい
- **🐛 自動修理が「一番高いものを直せない日は、直せる安いものまで直さない」バグを修正**
  - 払える中でいちばん困っているものを直すように（`broken.find(e => G.cash >= repairCost(e))`）
  - あわせて**優先順位を「店が止まるものから」に**変更。ロッカーは`cap:0`なので旧ルール（定員の多い順）では最後尾に回り、**ロッカーが全滅＝客が一人も入れない状態が延々と直らなかった**

## 2026-07-23 黒田の登場を評判50へ・じいさんの湯温に個人差（作者指定）

- **黒田修司の初登場を評判35→50に**（作者指定：「彼は高価な設備中心に要求するから、中盤以降の登場でいい」）
  - `KURODA_APPEAR_REP = 50`。実測：rep35/45/49では登場せず、rep50で登場。要求できる設備は rep50 でジェットバス＋マッサージチェアの2件（＝必要件数ちょうど）、rep60 でインフィニティチェアが加わる
  - 玲奈の初登場（rep50）は**黒田の決着後**というゲートで順番を保っているので数値はそのまま。ただし**投票対決の挑戦状を60→70に後ろ倒し**した。60のままだと黒田の決着直後（rep60前後）に挑戦状が飛んできて、玲奈の**引き抜き・買収の揺さぶりが丸ごと飛ぶ**ため（※叩き台）
- **じいさんの湯温の好みに個人差をつけた**（作者指定：「38度を好む爺さんを増やそう」）
  - `TYPES`に`furoVar`（好みの±℃）を追加し、**来店した1人ごとに湯温の好みを転がす**（`spawnCustomer`で`c.furoPref`を確定、`furoPrefOf(c)`が`equipFit`と`finishUse`の両方で使われる）。個人差を持つのは今のところ**じいさんだけ**（`furoPref:42, furoVar:5` ＝ 37〜47℃／※叩き台）
  - つまり同じ「常連のじいさん」に**あつ湯派とぬる湯で長湯する派**が混ざる＝ぬる湯(38℃)の説明文「長湯派はこっちを好む」が実際にそうなった
  - 実測（じいさん2000人ぶんの抽選）：37〜39.5℃が27%／39.5〜42.5℃が29%／42.5〜47℃が44%。**ぬる湯38とあつ湯44を並べると42%のじいさんがぬる湯を選ぶ**。好み40℃までは100%ぬる湯、41℃で五分、42℃以上はあつ湯
  - 実機でも確認：好み40℃のじいさん2人がぬる湯38へ、43℃のじいさんがあつ湯44へ。38℃を好む客が38℃で「ぬるいな…」と言わないことも据え置き
  - 15日ぶん自動周回で例外0・console error 0、`node --check` 3ファイルOK、テスト用セーブは消去済み

## 2026-07-23 修理の全自動化・解放ペースの見直し・「置くだけの設備」を使いに行かせる（作者指定）

- **🐛 客が全員「常連のじいさん」になっていたのを修正**（作者報告の「ヤクザがいなくなった」の正体）
  - `spawnCustomer`の重みテーブルに`oyaji`が無く、合計が`NaN`→抽選が回らず`keys[0]`＝常連のじいさんだけが延々と来店していた。強面の親分も、他の5タイプも一切出ない状態
  - `oyaji: 24`を追加（サウナがあると+6）。**修正後の実測：35人中 じいさん7／おやじ7／金髪7／サウナー青年7／サラリーマン3／強面4**（＝ヤクザ11%）
  - 行列も戻った（同条件で番台の行列の最大が4人→7人。サウナ好きの客が戻って滞在が伸びたぶん、設備待ちも増えた）。※コメントに「TYPESの全キーぶん必ず書くこと。1つ欠けると合計がNaNになる」と警告を残した
- **【🔧 修理】ボタンを廃止＝修理は全自動に**（作者指定）
  - 耐久度が0になると勝手に壊れ、`autoRepair()`が勝手に修理業者を呼ぶ。直し終わった瞬間に**業者の頭の上へ修理費がポーンと出て**、その場で現金から引かれる（`finishFix`）。日報の「修理業者への支払い」にも載る
  - 同時に来るのは1人だけ。客が使う設備（cap大→高価）から先に直す。**修理代が払えない日は業者が来ない**（1日1回だけ「修理代が足りない…」）。若い衆が暴れている最中は呼ばない
  - 金額は呼んだ時点で固定（`n.fee`）。開店時（`startDay`）にも走るので、前日の傷みで壊れたものは開店と同時に業者が来る
  - 設備パネルは【修理】ボタンごと削除し、壊れている時だけ「🔧 壊れている（業者が来る ¥42,000）」の一行に。cap0の設備（洗面所・テレビ・冷水機など）は**そもそも修理ボタンが出ず直せなかった**が、自動化でこれも直るようになった
  - 「手入れが要る」「修理業者を呼ぼう」など、プレイヤー操作を前提にした文言を全部書き換え
- **新アイテムの解放ペースを全体に引き延ばした**（作者指定：季節の替わり湯=評判40、寝湯=評判70）
  - 20 立ちシャワー／25 リクライニング・ドリンク自販機／30 電気風呂・マッサージチェア／35 ミストサウナ／**40 季節の替わり湯**／45 大型サウナ／50 ジェットバス／55 檜風呂・地下水／60 塩サウナ・インフィニティ／65 炭酸泉／**70 寝湯**／75 フィンランド式／80 シングル／85 露天（※すべて叩き台）
  - **田所・黒田の要求から「まだ解放されていない設備」を外した**（`demandBuyable`）。替わり湯が評判40になったため、評判15で現れる田所が買えない物を宿題に出す事故が起きる。実測：rep15で田所の候補7件（3件クリアが必要）、黒田は rep35〜49 はマッサージチェアのみ→rep50 でジェットバスが加わる
- **ぬる湯の浴槽を40→38℃**（作者指定）
  - 38℃を好む客がいなくなる件は、**作者判断で「38℃を好むじいさんを増やす」方**を採用（おやじの39℃は40℃へ差し戻し）。→ 下の「じいさんの湯温に個人差」を参照
  - 「ぬるいな…」の不満は**その客にとってぬるい時だけ**に（好み−湯温が3℃以上）。38℃を好む客が38℃の湯で文句を言う事故を止めた
- **ロッカーと洗い場は「列で並べられる」ように**（作者指定：上下左右にも設備を置けるように）
  - `placementValid`の到達チェックを、この2種だけ**種類ごとの「1台でも手前が空いていればOK」**に変更（`bankCat`）。壁一面のロッカーも、横並びのシャワーも作れる
  - ただし**その種類が全滅する置き方は今まで通り禁止**。実測：2列×3段のロッカーの壁（6台中3台だけ手前が空く）はOK、最後の通路を塞ぐ置き方はNG
  - 客側は`usable()`で「手前に立てるマスがある台」だけを選ぶ（`findFreeEquip`／`goLocker`／`goWaitFor`／`catExists`）＝奥の列に向かって歩き出さない
- **「置くだけで効く設備」を実際に使いに行くようにした**（作者指定：洗面所・冷水機・扇風機の描画）
  - `PAS_USE`＋`toPas`/`usingPas`の2状態。1台につき同時に1人（`pasBy`で場所取り）。満足度は今まで通り帰り際にまとめて効くので、**収支バランスは変えていない**（＝絵と動きだけ）
  - **冷水機**：サウナから出るたびに飲みに行く。紙コップを口元へ傾け、雫がこぼれる
  - **扇風機**：湯上がり、着替える前に脱衣所へ（60%）。設備側から風が流れてきて、髪と腰タオルの裾がなびく
  - **洗面所**：着替えたあと鏡の前でドライヤー（70%）。ノズルは必ず頭のほうを向き（`ctx.scale(d,1)`で反転）、毛先が逆立って温風が吹く
  - セリフは既存の`coolerGood`／`fanGood`／`dryerFree`・`dryerPaid`を、使っている**その場**で言わせるようにした
- **営業結果ページ**（作者指定）
  - 水道代は金額だけに（「（カラン・湯船の大きさ分）」を削除）。設備パネル側も「（大きさ分）」だけに詰めた
  - 「ととのった」客は**「◯人（サウナ客の◯%）」**に（母数を来店数からサウナ客に。ととのいはサウナ客だけの体験なので）
- 確認：20日連続営業でコンソールエラー0・例外0（修理費は日により¥8,000〜¥152,000）／セーブ→ロード往復で設備の状態と温度が一致／設備パネルに【修理】ボタンが無い／冷水機・扇風機・洗面所の使用中の絵を実機のスクリーンショットで確認

## 2026-07-23 湯温調整の廃止（作者指定）＝客が求めるのは「湯温」ではなく「風呂の種類」
- **浴槽の温度調整をやめた**。湯温は設備ごとに固定（`TEMP_RANGE`から`furo`を削除。残るのは`sauna: [60,120]`だけ）
  - 判定は`canSetTemp(def)`に一本化＝**ドライサウナだけ**が±で弄れる。浴槽・水風呂・ミスト・塩は固定
  - **ぬる湯の浴槽（40℃）とあつ湯の浴槽（44℃）は残した**（作者の二案のうち「2つ残して調整機能だけ取っ払う」を採用）。ぬる湯とあつ湯は温度の設定値ではなく**風呂の種類そのもの**で、1つに統合すると序盤に選べる湯が減るため
  - **あつ湯を43→44℃**に。銭湯のあつ湯として実際の温度に寄せつつ、「超あつ湯」の帯（44℃〜／湯が赤くなる）と`furoHot`のセリフ、田所の「44℃のあつ湯を入れろ」を生かすため
  - 設備パネルは温度と帯を出したまま±ボタンだけ消し、「湯温はこの設備で決まっていて、変えられない。ぬる湯派とあつ湯派の両方を満たすには、湯船をもう1種類」と出す
  - 光熱費の温度上乗せ（¥100/℃/日）は**ドライサウナだけ**の話になった（`tempSurcharge`）
- **充実度と不満の指標を「温度の幅」→「風呂の種類数」に**（`furoKinds`。同じ設備を2台置いても1種類）。1種類=+0／2種類=+3／3種類=+6／4種類以上=+9
  - 風呂が1種類だと`hintFuroKind`「もっとお風呂の種類がほしいな」「お風呂は1種類しかないのか…」（作者の言葉をそのまま採用）
  - 種類はあっても全部あつ湯／全部ぬる湯なら、今までどおり`hintAtsuFuro`／`hintNuruFuro`で足りない側を欲しがる
  - 湯温が好みから外れた時の満足度の下げ幅を`-min(diff,9)`→`-min(diff,5)`に緩めた。**自分では直せない不満**になったので、水風呂と同じくらいの重みに（※数値は叩き台）
  - 【📊データ】の「湯の品揃え」は **風呂◯種類 ◎△** 表示に
- **ミスト60℃・塩55℃も固定に**（`gentle`のサウナ＝熱さの好みで評価されない“別ジャンル”なのに温度だけ弄れたのを揃えた）。塩は70→**55℃**（塩が溶ける湿った熱さ＝実際の塩サウナは50〜60℃台。ミストは作者指定の60℃のまま。実在の施設は40〜50℃が多いので、下げたければ数字だけ差し替え可）
- **昔のセーブの温度は設備の既定に戻す**（`loadGame`）。46℃にしてあった浴槽も、110℃にしてあったミストも既定値へ。ドライサウナの設定値だけ残る
- 田所の要求`atsuyu`は`{type:'temp',cat:'furo',op:'>=',v:44}`のまま維持し、達成条件の表示だけ「浴槽を44℃以上にする」→**「44℃以上の風呂を置く」**に（※セリフは叩き台）
- **設備パネルの修理の説明書きを削除**（作者指定）：「🔧 修理＝¥42,000で修理業者を呼ぶ（歩いて来て…）」の注記ブロックごと廃止。金額は下の【修理】ボタンに出ているので二度手間だった。`.repair-note`のCSSも削除
- 確認：新規ゲーム→風呂1種類で`hintFuroKind`が出る／ぬる湯2種類で`hintAtsuFuro`／あつ湯を足すと湯温系の不満が消える、田所の`atsuyu`達成、浴槽の上乗せ¥0・サウナ100℃で¥1,000/日、旧セーブ読み込みで浴槽39/40/44・ミスト60・ドライサウナ104、1日通し営業でコンソールエラー0

## 2026-07-23 整理（作者指定）＝カタログの「その他」を廃止。ドライヤーと化粧水は【洗面所】にまとめる
- **「その他」タブを廃止**：迷子になる設備を作らず、必ずどれかのタブに入れる方針にした。`CATS`から`etc`を落とし、行き先は`EQ`の`tab`で指定する（`cat`は傷みかた`wearPerDay`と絵の描き分けに使うので`etc`のまま）
  - **冷水機→【休憩】タブ**（設置できる部屋は今までどおり浴室・脱衣所の両方）。**扇風機・観葉植物→【脱衣所】タブ**
- **扇風機と観葉植物は脱衣所専用に**（`room:'datsui'`）。扇風機は名前も「浴室の扇風機」→**「扇風機」**（湯上がりに当たるもの＝浴室に置くものではない）
  - id は互換のため`fan_bath`のまま（昔のセーブと田所の要求`{key:'fan'}`が参照する）。田所のセリフからも「浴室の」を落とした
  - **壁掛けもやめた**（作者指定）：`wall:true`を外し、**脱衣所の床のみ**。壁に掛けられるのはポスターとテレビだけになった
  - ついでに**セリフの取り違えを修正**：扇風機を気に入った客が「水うめえ…！」（冷水機のセリフ）を喋っていた。`fanGood`を新設（※叩き台）
- **【洗面所】を新設し、ドライヤーと化粧水・乳液を吸収**（`sink`／2×1／¥90,000／`run:400`）。**それぞれに1マス使わせない**のが狙い
  - 単独の`dryer`・`lotion`は**廃止**。運営メニューのドライヤー代（無料/¥20）と化粧水代（無料/¥50/¥100）は、**洗面所を置くと2つとも解放**される（`hasWorking('sink')`）。`dryerFee`・`lotionFee`の仕組み自体は据え置き
  - `pas`は2つ分をまとめて`{sat:5, score:5}`（旧ドライヤー3＋化粧水3から1だけ削った）。値段は旧2台の合計¥100,000より少し安い¥90,000
  - 客の不満セリフは、洗面所が無いとき`hintDryer`（65%）と`hintLotion`（35%）から選ぶ＝1つの設備に不満が二重に立たないようにした
  - 絵は**鏡＋洗面ボウル2つ＋蛇口＋化粧水と乳液のボトル＋ドライヤー**を2マス幅で描き起こし（画像素材はゼロのまま）
- **昔のセーブの救済**：`ID_ALIAS = { lotion:'sink', dryer:'sink' }`で読み替える。1マス→2マスで**右隣が壁や他の設備で埋まっていると置けない**ので、その場合だけ撤去して**¥90,000を返す**（黙って消して損をさせない）。ドライヤーと化粧水を両方持っていた場合は洗面所が2つになるため、**洗面所どうしの重なりも見る**
- **実測**：脱衣所に洗面所を置いて1日営業＝アメニティ収入¥360（9回・ドライヤー¥20＋化粧水¥50）。旧セーブ移行テスト＝隣接した`dryer`+`lotion`+壁際の`lotion`→洗面所1つ＋¥180,000返金。console error 0

## 2026-07-23 調整（作者指定）＝パネルのボタンを1行に揃える
- **「🏮 営業開始」を18px→14px＋`white-space:nowrap`**。320px幅でも折り返さず1行（折り返すと高さが伸びて、隣の運営・データ・広告・融資と頭が揃わなかった）
- **営業中のボタンも横一列に**：`.biz-row`（客数・売上とボタンを同じ行に詰めて折り返していた）を廃止し、**上の行に客数/売上/場内人数（`#bizStats`）／下の行に運営・データ・一時停止（再開）・速度を等幅4分割**（`.biz-actions`）。準備中パネルと同じ並び方に揃えた。320px幅でも4つとも1行・はみ出しなし

## 2026-07-23 調整（作者指定）＝水風呂の水温は固定に。一桁℃は【シングルの水風呂】だけの特権
- **穴だった**：水風呂の水温は5〜22℃で自由に下げられ、しかも**下げる方向は光熱費の上乗せが0円**（`tempSurcharge`は既定より上げた分しか取らない）。¥400,000の水風呂を8℃にすれば、¥1,600,000の【シングルの水風呂】を買う理由が消えていた
- **対処**：水温は**設備ごとに固定**。`mizu1`＝15℃／`mizu2`（地下水かけ流し）＝**13℃**／`mizu_single`＝8℃の**三段**（作者指定）。`TEMP_RANGE`から`mizu`を外し、`canTemp`を浴槽・サウナだけに。設備パネルには**水温を表示するが±ボタンは出さない**（「一桁℃を出せるのは【シングルの水風呂】だけ」と添える）
- **客の好みも合わせて引き直した**：`idealCold(c) = 8 + (1 - coldLove) * 11`（8〜19℃）を新設し、`equipFit`と満足度判定の2か所で共用。旧式（11〜20℃）のままだと**誰の理想も8℃にならず**、シングルが全員にとって「冷たすぎる外れ」になってしまうため
  - 結果（15/13/8の3槽を置いた実測・各200回）：サウナー青年＝8℃（+4）／サラリーマン＝8℃（+1）／OLさん・金髪の兄ちゃん・親分＝13℃（+4）／じいさん・おばちゃん・おやじ＝13℃（+1）
- **水風呂だけ温度の一致を1.5倍で見る**（`equipFit`）：温度を弄れない以上、客は「合う方の槽を選ぶ」しか好みを満たす手段がない。等倍のままだと★の差（シングルは★5・ふつうの水風呂は★3）が勝ってしまい、**OLさんが半分の確率で8℃に入って満足度を落としていた**（200回の抽選で実測）
- **昔のセーブの救済**：`loadGame()`で水風呂の`temp`は設備の既定値に上書き（5℃に冷やしたまま保存されたデータが、変更不能なまま残らないように）
- **文言**：3種のdescを書き換え、客の不満セリフ`hintMizuVar`から「もう少しぬるい水風呂も欲しいな」（もう叶えられない要求）を落として「一桁の水風呂、この街には無いのかな」に差し替え
- **13℃にしたことの副作用（承知のうえ）**：
  - `tempBand()`の境界は15℃なので、**13℃は「キンキン水風呂」表示**（水面の色もセリフも8℃と同じ扱い）。三段を見た目でも描き分けたいなら、9℃未満に「シングル」の帯を足す手がある
  - **15℃と13℃を並べても品揃えは0**（`tempVariety`は3℃差から「別の選択肢」と数える）。しかも★5の地下水にほぼ全員が流れるので、ふつうの水風呂は**収容2人ぶんの控え**になる＝地下水は同カテゴリの格上げ、幅を稼ぐなら8℃と組ませる、という住み分け

## 2026-07-23 追加（作者指定）＝右上に【☰ メニュー】。保存・トップ画面へ・今後のウェブサイト用の入口
- **場所**：上のバーの右端。`#btnMenu`（枠つきの☰）。準備中・営業中どちらでも、配置の途中でも開ける
- **中身**（`renderMenu()`で組み立て）：**💾 いまの状態を保存**（`saveGame()`＋「○日目の状態を保存した」のトースト）／**🏠 トップ画面へもどる**／とじる
- **もどる時は確認を挟む**：「保存していないぶんは消える。どうする？」→ **保存してもどる／保存せずもどる／やめる**。うっかり進行を失わせない
- **営業中は断り書き**：「保存すると、この日は準備中からやり直しになる」。セーブ形式が日の頭までしか持たないことを、その場で伝える
- **`returnToTitle()`を掃除**：戻る前に`deselect()／endPlacing()`を呼び、閉じ忘れやすいモーダル（メニュー・広告・融資）も閉じる。配置の途中で戻って、次に入った時に確認バーが残る事故を防ぐ
- **ウェブサイトのリンクは1行で足せる**：`js/data.js`の`MENU_LINKS`に`{ label, url }`を足すとボタンが増える（`window.open(url,'_blank','noopener')`＝別のタブ）。いまは空＝何も出ない

## 2026-07-23 修正＝大きい設備（2×2）で「置けるはずのマスが赤」だった不具合
- **症状**：遠赤サウナのような2×2の設備を置こうとすると、浴室の右端の列と間仕切りの手前の行が赤く塗られる。実際はタップすれば置けるのに「置けない」と見える
- **原因**：色分け（`placeMask()`）は**そのマスを左上の原点にした場合**の可否で判定していたのに、タップ時（`pointerup`）は端をタップすると内側に寄せていた。判定と実際の動きがずれていた
- **対処**：`snapAnchor(id, rot, tx, ty, moving)`を新設し、**タップ時と色分けの両方が同じ関数を通る**ようにした。色＝「このマスをタップしたら置けるか」になり、見た目とタップ結果が必ず一致する
- **さらに（第2弾）**：寄せ方を「枠内に収める」から**「タップしたマスを必ず含む置き方を、タップ位置に近い順に総当たり」**に変更。`w×h`通り（2×2なら4通り）を試し、最初に成立したものを採用する
  - これで**古い浴槽の左隣**（そのマスを左上に固定すると浴槽と重なる）も緑になり、1マス左にずれて置かれる。右端・下端・間仕切りの手前も同じ仕組みで自然に拾えるので、部屋ごとの特別扱いのコードは削除した
  - 返り値に`ok`を持たせ、`placeMask()`は`snapAnchor().ok`だけを見る（判定の二度打ちを防ぐ）。1×1の設備は候補が1つなので従来と同じコスト。2×2でマスク全体を計算し直しても**中央値2.0ms／最悪3.5ms**（回転・設備数・客数が変わった時だけの再計算）

## 2026-07-23 追加アップデート その17（作者指定）＝左右の壁を「内側の壁」にしてポスターを掛けられるように／英単語の排除
- **左右の壁が置き場になった**：`isWallMount(id)`（データ側は`wall:true`）と`isInnerWall(x,y)`を追加し、`placementValid()`の枠内判定を`surfaceOK()`に差し替え。壁掛けの1×1設備だけは`x=0 / x=W-1`（y=1〜H-2）にも置ける。壁掛け以外は従来どおり床のみ
  - 壁掛けにした設備＝**昭和アイドルのポスター／テレビ／浴室の扇風機**。部屋の縛り（`room`）は壁でもそのまま効くので、ポスター・テレビは脱衣所側の壁、扇風機は浴室側の壁にしか掛からない
  - `placeMask()`/`drawPlaceZones()`の走査範囲を壁掛け時だけ`x=0..W-1`へ拡張。部屋の緑枠も壁を含む幅に。タップ座標のクランプ（`pointerup`）も同様
  - 確認バーに「左右の壁にも掛けられる」を追記
  - **見た目**：`drawFloorAndWalls()`で左右の壁を板張り（継ぎ目＋腰の見切り縁）に描き替え、「掛けられる面」だと分かるようにした
- **ロッカーを【脱衣所】タブへ**：カタログのタブを`shopTabOf(id)=EQ[id].tab||EQ[id].cat`で決めるようにし、ロッカーに`tab:'datsui'`を付与。**【ロッカー】タブは廃止**（8タブ→7タブ）。`cat:'locker'`は受入人数`lockerCapacity()`・開店条件・傷み速度の判定に使うのでそのまま。あわせて`room:'datsui'`＝ロッカーは脱衣所にしか置けなくなった（初期配置の古いロッカーは元から脱衣所なので影響なし）
- **説明文の差し替え**（作者指定の文言そのまま）：インフィニティチェア＝「無重力の幸福。ここで「ぶっ飛ぶ」客が続出。」／化粧水・乳液＝「無料／¥50／¥100を運営メニューで選べる。湯上がりの心地良さが変わる」
- **ゲーム内から英単語を排除**（カタカナ英語はOK）：`VS→対`（投票対決の中央表示）／`設備15%off→15%引き`／`ここでOK？→ここでいい？`／`ONにすると→オンにすると`。文字列リテラルを走査するスクリプトで全チェックし、残ったラテン文字はコード片（テンプレート式・HTML属性）のみ
  - **作者指定で据え置き**：`蒼天SPA`（固有名詞）／`OLさん`（客タイプ名）／`常連A・常連B`（話者名）はそのまま

## 2026-07-23 追加アップデート その16（作者指定・まとめて対応済み）＝銭湯らしい設備の大量追加／キャラが歩いて来る／修理業者・バット・ヤミ金／100日設計
作者から27項目のまとめ指定。**セリフ・数値はすべて叩き台**（作者リライト前提）。

### 1. 配置・操作まわり
- **番台を入口の真横（左）へ**：`INIT_EQUIP`の`bandai`を`(4,9)→(5,9)`。入口の内側マス`(6,9)`がそのまま支払い待ちの先頭になる
- **営業中でも移動・売却できる**：`selectEquip()`の営業中制限を撤去し、タップ時の表示内容を準備中と完全に同一化。使用中の客は`evictUsers(it)`で追い出す（満足度-8＋「え、いま使ってたのに…」）
- **配置エリアの色分け**：`drawPlaceZones()`＝置けるマス＝緑／置けないマス＝赤＋斜線。部屋限定の設備（浴室のみ／脱衣所のみ）は**その部屋を緑の枠で囲う**。`placeMask()`で回転・設備数・客数が変わった時だけ再計算（毎フレームBFSを回さない）
- **部屋制限を汎用化**：`bathOnly`→`room:'bath'|'datsui'`（`roomOK()/roomLabel()`）。ポスター・観葉植物など“置いてあるだけ”の物は`needsAccess()`で**到達性チェックから除外**＝隅に置ける

### 2. 設備を銭湯らしく（全国の銭湯をリサーチして反映）
電気風呂・ジェット・バイブラ・薬湯・炭酸泉・壺/シルク風呂・立ちシャワー・マッサージチェア・体重計などが実在の定番。カタログを**17→42種**へ。タブに**【脱衣所】**を新設。
- 風呂：ぬる湯／**季節の替わり湯（ゆず・菖蒲）**／電気風呂／ジェットバス／高濃度炭酸泉／寝湯／露天の岩風呂 を追加
- サウナ：ミストサウナ・塩サウナ（`gentle:true`＝温度の好みで評価されない別ジャンル）
- 水風呂：シングルの水風呂（9℃・チラー）
- 洗い場：立ちシャワー／休憩：インフィニティチェア
- **脱衣所（`room:'datsui'`）**：ドライヤー／化粧水・乳液／体重計／テレビ／昭和アイドルのポスター／将棋台／マッサージチェア／牛乳の自販機／ドリンクの自販機
- **その他**：冷水機（**浴室・脱衣所どちらにも置ける**）／浴室の扇風機（浴室のみ）
- **置くだけで効く設備**＝`pas:{sat,score,likes,like}`。`customerLeave()`で種類ごとに1つだけ加算（上限+20）。**客のタイプで刺さる物が違う**（爺さん＝ポスター/将棋/体重計/扇風機、若い層＝テレビ/化粧水）。充実度`facilityScore()`にも加算
- **ドライヤー＝無料／¥20、化粧水・乳液＝無料／¥50／¥100** を運営メニューで選択（`opts.dryerFee/lotionFee`。設置して初めて解禁）。マッサージチェアは百円玉がぼちぼち入る。売上は日報「ドライヤー・化粧水など」行
- 新規アートはすべてコード描画（画像素材ゼロ）：ドライヤー／化粧水ボトル／**針が振れるアナログ体重計**／**画面が明滅するブラウン管テレビ**／ポスター／将棋台／マッサージチェア／冷水機（水が落ちる）／**首を振って羽根が回る扇風機**／インフィニティチェア／ドリンク自販機

### 3. 「2つ置かないと全員は満たせない」温度の品揃え
- `equipFit(it,c)`を新設し、`findFreeEquip(cat,c)`が**客の好みに一番近い一台を選ぶ**ように。90℃派と110℃派、あつ湯派とぬる湯派、キンキン派とぬるめ派が**別々の設備に流れる**＝1種類だけでは必ず誰かが不満
- 客タイプの好みを整理し、**近所のおやじ**（ぬる湯・88℃）を追加。じいさん45℃/82℃、サラリーマン42℃/96℃、サウナー青年43℃/110℃
- `tempVariety(cat)`＝湯温が何段階に分かれているか。充実度に加算し、**データ画面に「あつ湯/ぬる湯 ◎△」**として表示。1種類しかない時は客が「もっと熱いサウナが欲しい」等と不満を言う（`hintSaunaVar`等）

### 4. 修理と破壊（応急修理の廃止）
- **応急修理を撤去**。`maintain()`から修理タスクを削除し、主人公とバイトは**代金の受け取りと掃除だけ**
- **修理業者キャラを新設**：【🔧 修理】で呼ぶと**入口から歩いて来て、トンカチでガンガン叩いて直す**（`callRepairman()`／ヘルメット＋作業着のドット絵／打点に✳）。状態0の設備は業者を呼ぶまで使えない
- **劣化を加速**：`applyDailyWear()`で**1営業日ごとに必ず減る**（`CONF.wearPerDay`＝ロッカー10＝**10日に一度は修理が要る**／カラン6／湯船・サウナ4…）＋利用ごとの消耗も約2倍。修理費は購入額の12%
- **ヤクザの破壊をバット描写に**：`startRaid()`＝**バットを提げた若い衆が歩いて上がってきて、設備をバンバン叩く**（1発ごとに`💥バンッ！`＋火花、居合わせた客が「ひっ…！」と怯えて満足度↓）。壊しきると次の標的へ→引き上げ。`mode:'take'`なら**借金のカタに持ち去る**

### 5. 金の締め付け（借金でヌルくなっていた問題）
- **信用金庫は300万が限度**（`loanMax`。旧800万）。資金ショート時の自動借入もこの枠まで
- **ヤミ金「灰田金融」**を新設（`G.yami`）。信金が限度いっぱいの時だけ手が出せる。**利息5%/日＋元金5%/日＝毎日、元金の10%を取りに来る**。開店直後に灰田が歩いて来て集金モーダル。**払えなければ若い衆が来て設備を持って行く**（`failYami()`→`startRaid(...,'take')`）。完済すれば二度と来ない
- どこも貸してくれない日は`cash=0`＋評判-2（店の信用が落ちる）
- **設備の売却額＝購入額の10%**（`CONF.sellRate`。旧40%）＝置き場所と品揃えは考えて決めろ、という圧
- **入浴料の上限を¥1,000に**（`FEE_RANGE=[300,1000]`・`FEE_CEIL`）。小さな銭湯なので`worthFee()`も¥1,000で頭打ち。サウナ料は`[200,300,400]`／自由枠`[0,600]`

### 6. 人物イベントの順番と“歩いて来る”演出
作者の問い「田所・ヤクザ・黒田が同時並行で進んでしまう。どの順番がいい？」への回答＝**焦点は1日ひとり、arcは順番に**。
- **重要人物は客と同じように入口から歩いて来る**（`G.npcs`／`makeNpc()`／`NPC_LOOK`）。**顔で誰か分かる**ドット絵＝田所（白髪＋白い太眉＋顎髭）／黒田（七三＋スーツ＋赤ネクタイ）／玲奈（黒のロングヘア＋口紅＋スーツ）／鬼頭（スキンヘッド＋サングラス＋髭）／灰田（眼鏡＋革鞄）／修理業者（黄ヘルメット）／若い衆（バット）。**頭上に名札**を出して初見でも分かるように
- 到着したら本題のモーダル→閉じると**帰っていき、営業が再開**（`startVisit()/dismissVisitor()/resumeAfterVisit()`）。移動はポーズ中も実時間で動く
- **1日にひとりだけ**＝`pickTodaysVisitor()`が **田所 → 鬼頭 → 黒田 → 玲奈** の優先順で今日の来訪者を1人決める（`startDay`で時刻も抽選）
- **arcのゲート**：黒田の初登場は**田所が決着してから**（`dueKuroda`）、玲奈の初登場は**黒田が決着してから**（`maybeReinaCinematic`）。鬼頭だけはプレイヤーが「刺青・強面お断り」を押した時点で走り出す（並行だが、来訪日は必ず1人）
- 全画面で見せたい節目（蒼天SPAへの招待・投票の中間発表・投票日・エンディング）だけは夜（準備画面に入る前）に流す

### 7. 田所・黒田の「無茶な要求」＝ミッション
- `TADOKORO_DEMANDS`／`KURODA_DEMANDS`（`data.js`）。`need`の型＝`equip`（置け）／`remove`（どかせ）／`opt`（値下げしろ）／`temp`（湯を熱くしろ）
- **田所＝古臭い・安い・爺さん受け**：昭和アイドルのポスター／浴室の扇風機／季節の替わり湯（ゆず湯）／将棋台／体重計／浴槽を44℃以上に／入浴料を¥430以下に／**「あのけばけばしい寝椅子はどかせ」**（リクライニングチェアを持っている時だけ出る）
- **黒田＝高価だが確かに良い物**：フィンランド式サウナ／ジェットバス／高濃度炭酸泉／シングルの水風呂／インフィニティチェア／マッサージチェア
- 流れ＝来訪して**要求**（引き受ける／断る）→達成しておくと**次の来訪で認められる**（田所は常連絆+12・評判+3／黒田は評判+4・数字↑）→未達なら催促。**田所は3件、黒田は2件**こなすと決着の資格（`TADOKORO_DEMAND_CLEAR/KURODA_DEMAND_CLEAR`）
- 抱えている注文は**準備画面の1行ヒント**と**データ画面**に出る（※全体像は見せない方針は維持＝出すのは「いま受けている注文」だけ）

### 8. 「📊 データ」画面を新設
準備中・営業中どちらからも開ける計器盤（`renderData()`）。**営業データ**（日数・資金・借入・ヤミ金残債・直近5日の平均収支と黒字日数・入浴料と納得される額）／**客の評価**（評判・充実度・ととのい率・受入人数・**湯の品揃え◎△**・客が欲しがっている物）／**設備の状態**（傷んでいる順6件・汚れ・バイト）／**人との関係**（常連絆・親父・**会った相手だけ**）／抱えている注文／投票対決の残り日数と見込み票。
※その15の方針は維持＝**まだ会っていない相手は出さない／残りいくつかは見せない**

### 9. その他の作者指定
- **蒼天SPAの水風呂を8℃・15℃・20℃に**（`STORY_SOUTEN_VISIT`）
- **投票対決に「中間発表」**を追加（`STORY_DUEL_MID`／`openDuelMid()`）。準備期間の折り返しでローカルニュースが途中経過を報じる。アートは`duel`を流用し`window.DUEL.mid=true`で**青いバナー「📺 中間発表 ―― 大接戦！」**に切り替え。数字は僅差に見える中間集計（55%消化）
- **初めて「刺青・ヤクザNG」をONにした時**＝`flags.banFirst`。次の営業日に鬼頭が必ず来て「**俺たちを入れねえってんなら、みかじめ料をもらうぞ**」と脅す（初対面テキストを差し替え）
- **100日クリアの傾き**：評判は上へ行くほど伸びにくく（`repD *= 1 - rep/125`）、ボスの再訪間隔を3〜4日に。田所の決着条件を絆55・評判38へ緩和（要求3件の達成が主軸になったため）。**時間ではなく金と要求で詰まる設計**＝劣化・修理費・売却10%・借入300万が実質のペース配分

### 検証
`node --check` 3ファイルOK／ブラウザ実機で：番台(5,9)／部屋制限（ドライヤー＝脱衣所のみ・扇風機＝浴室のみ・冷水機＝両方）／配置の緑赤塗り分け（スクショ確認）／田所の来訪→歩いて来る→注文→達成表示／黒田の投資提案（フィンランドサウナ¥2,400,000）／修理業者が歩いて来てcond0→100／みかじめ拒否→**バットで新しいカランを叩き壊す**（スクショ確認）／ヤミ金の借入→翌日集金→不払い→**遠赤サウナを持ち去られる**／中間発表シネマ（夕凪199-蒼天169・スクショ確認）／40日ぶんの自動周回で例外0・**console error 0**。テスト用セーブは消去済み。

## 2026-07-23 追加アップデート その15（作者指定・対応済み）＝「📖 記録」メニューを撤去＝全体像はあえて見せない
作者指定＝「記録メニューはなくていい。**あえて全体像をユーザーには見せない**」。進捗一覧＝残りいくつ／誰が未登場／何をすれば解決、が一目で分かる「攻略チェックリスト」になっており、**先が見えない再建の手探り感**を殺していたため撤去。
- 撤去物：準備画面の【📖 記録】ボタン（`index.html`）／`kirokuModal`（同）／`renderKiroku()`（`game.js`・約48行）／`hideAllModals`相当の一括クローズ配列から`kirokuModal`／CSS `.kiroku-*`・`.ki-*`（`style.css` 14行）
- 進捗の伝え方は**その場のフィードバックだけ**に一本化＝ボス登場・イベント・決着はモーダル／全画面シネマで、日々の指針は**準備フェーズのヒスト1行**（投票対決の残り日数＆見込み票もここに出る）。**総数も未登場ボスの存在も明かさない**
- `PROBLEMS`（`data.js`）はエンド判定（`checkGrandEnding`の全solvedチェック）に必要なので存置。`name/icon/todo/done`は表示先を失うが、対立の設計メモとして残す
- ブラウザ検証：`btnKiroku`/`kirokuModal`/`renderKiroku` すべて存在せず（`undefined`）／準備画面のボタンは【営業開始・運営・広告・融資】の4つで横並びレイアウト崩れなし／新規開始→営業→締めの1日サイクル通し・console error 0

## 2026-07-23 追加アップデート その14（作者指定・対応済み）＝玲奈戦のクライマックスを「サウナ天下分け目の投票対決」に格上げ
作者提案＝「蒼天SPAと夕凪湯が街を巻き込んだ壮大な勝負。テレビ・新聞で報道。**両施設に通った客が一人一票**、票数が多い方が勝ち」。従来の“玲奈が静かに認める決戦（モーダル）”を廃し、**公開投票対決の全画面シネマ**に置き換え。売却（負け）エンドだけがシネマだった非対称を解消し、**勝ちの見せ場**を最上位に。台詞・数値は叩き台。
- **開催トリガー＝玲奈の挑戦状（作者選択）**：`maybeReina()`で `reina.met` 後、**評判60**（`REINA_DUEL_APPEAR_REP`）に達すると `openReina('duel')`＝挑戦状モーダル（`REINA_TEXT.duel`＝報道陣を連れて「公開投票対決で決めましょう」）。プレイヤーの資格グラインドは不要＝玲奈が仕掛ける
- **準備期間（作者選択）**：`acceptReinaDuel()`で `reina.duel='announced'`・`duelDay=day+7`（`REINA_DUEL_PREP_DAYS`）。この間だけ競合圧が最高潮＝`startBiz`で `n *= (duel==='announced' ? REINA_PRESSURE_DUEL(0.68) : REINA_PRESSURE(0.78))`。準備画面ヒントに**投票日までの残り日数＋見込み票**を表示（追い込みの目標が見える）
- **投票日＝全画面シネマ `duel`**：`openReinaDuel()`が票を算出し `window.DUEL={yu,so,t0}` をセット→`Story.play(win?STORY_DUEL_WIN:STORY_DUEL_LOSE)`。アート（`story.js`の`duel(ctx)`・`STORY_ANIM_ARTS`に追加）＝報道バナー／左に**夕凪湯**（小・あたたかい・灯る窓＋「ゆ」暖簾＋湯気）／右に**蒼天SPA**（高い冷たいガラスタワー・窓が明滅）／中央VS＋スポットライト／群衆の列／下部に**綱引き票バー＋カウントアップ数字**（琥珀＝夕凪／青＝蒼天、`window.DUEL`を読み最終比率へイージング）。すべて`Date.now()`ベースでアニメ
- **票の計算（叩き台・定数化）**：蒼天票＝`SOUTEN_DUEL_VOTES(300)±15`（規模の壁）。夕凪票＝`評判×3.2＋常連絆×2.4＋一級設備数×24＋孤高stage(≤2)×16＋親父和解45＋田所仲間20`。**規模では届かないが、絆・個性・物語を積めば逆転**。挑戦は評判60で来るが勝つには“心”の積み上げが要る（弱ビルド240<300で負け／中ビルド346・強519で勝ち＝検証済）。**同数は挑戦者＝夕凪湯の勝ち**（`yu>=so`）
- **勝敗（作者選択＝負けても再戦）**：`finishReinaDuel(win,yu,so)`。勝ち→`resolved+ally+solved.reina`＝競合圧解除・玲奈仲間（設備15%off据置）。負け→ゲームオーバーにせず `duel='none'・lost++・nextDay=day+8`（`REINA_DUEL_COOLDOWN`）＝**クールダウン後に再び挑戦状**（設備と絆を上げて雪辱戦）。検証＝勝ち/負け/再戦ループ・アニメ稼働&離脱で停止（リークなし）・console error 0
- 旧 `kessen` 分岐（モーダルの静かな承認）と `REINA_KESSEN_REP` は撤去。買収の誘い＝孤高stageを積む揺さぶり＆売却エンド分岐として存続（stageは投票の共感票に効く）

## 2026-07-23 追加アップデート その13（作者指定・対応済み）＝ボス3体目「桐生玲奈」（蒼天SPA・設備/業界）を実装＝第1章のボス完

作者方針＝ボスを1体ずつ。田所（①地域/伝統）→黒田（②数字/継続）に続く**最後のライバル「桐生玲奈」**（巨大スーパー銭湯 蒼天SPAの開発責任者・テーマ＝設備/業界）を「味方に引き込むarc」＋**買収エンド分岐**として実装。台詞は叩き台。これで4対立（田所・鬼頭・黒田・玲奈）＋親父が揃い、第1章はひと通りクリア可能に。

- **玲奈トラッキング**：`G.reina{met,stage,resolved,ally,nextDay,poachDone}`＋`newReina()`＋save/load/reset。`reina`識別子＝`solved.reina`と一致。旧セーブ救済は`reina.resolved && reina.ally`のときのみ`solved.reina`（売却エンドは`ally=false`のままなので誤解決しない）
- **登場＝蒼天SPA訪問シーン（作者要望の“圧倒される”掴み）**：`maybeReina()`をenterPrepPhaseチェーン末尾に追加（…→黒田→**玲奈**、`if(maybeKuroda())return; maybeReina();`）。**評判50**で`openReinaVisit()`→フルスクリーンStory `STORY_SOUTEN_VISIT`（art=`souten`）。玲奈に招かれ、巨大サウナ施設に乗り込む。設定＝**実在サウナ施設をモチーフ**（作者指定URL＋フロア図2枚）＝**東京の激戦区でも勝てる“圧倒的王者”仕様**。※固有ブランド名は流用せず本質を借用（inspired-by）。一枚絵`souten`は**作者指示で数回改稿→最終＝“RPGマップ風の重厚な大浴場（俯瞰）”**（改稿履歴：街見下ろし外景→浴室内横アングル→フラットなフロアマップ→**作者提示のRPG風スパ地図をベースに全面リビルド**）。`story.js`の`chamber/basin/plunge/onsen/zen/waterfall/frame/lantern/plant/lounger/firepit`ヘルパーでコード描画。内容＝**石枠のテーマ別サウナ5室**（金＝板張り／石＝丸ストーブ＋蒸気／緑＝光る祭壇＋植栽／紫＝瞑想の蝋燭／塩＝光る塩ブロック・各室ランタン）＋**滝**、左に**多彩な湯**（青タイル・桃・紫の電気風呂・翡翠のジェット）、中央に**枯山水の島＋焚火の広場＋白湯・薬草・寝湯**、下段に**水風呂3種**（浅い氷青／濃紺の深水／氷塊の極寒）、右に**岩の露天＋ととのい寝椅子群**、要所に**ランタン・緑**。**番号・ラベルなし**、温かみの石畳。**主人公は広場にちいさく立たせて**（影＋体＋頭）規模を対比。**“動く一枚絵”**＝Storyの再描画ループ（`STORY_ANIM_ARTS={'souten'}`・`setArtAnim`・80ms間隔、シーン離脱で停止）で、`Date.now()`ベースに**湯気の立ちのぼり／水面のさざ波／桃湯・炭酸の泡の上昇／ジェット・滝の流れ／電気・祭壇・蝋燭・ランタン・焚火の明滅**が動く（他の一枚絵は静止）
- **競合圧**：訪問後 `soutenPressureOn()`（`reina.met && !resolved`）が続き、`startBiz`の客数 `n *= REINA_PRESSURE(0.78)`＝蒼天SPAに客を吸われる。仲間化（resolved）で自動的に解ける
- **反復イベント**（`reinaModal`＝`reinaTitle/reinaInfo/reinaChoices`）：
  - **引き抜き**（`poach`・バイトが居て未実施のとき1回）：💴待遇を上げて引き留める（¥150,000）／🤝本人に任せる→常連絆`najimi≥40`なら忠義で残り評判+2、薄いと`staffCount--`（蒼天に引き抜かれる）
  - **買収の誘い**（`buyout`・以降毎回）：🛁断る→孤高`stage`+1・評判+2（決戦に近づく）／💰2,000万で売る→**確認ゲート**（`sell`＝「❄最後の確認」で誤操作防止）→やっぱり売れない＝断る扱い／それでも売る＝`doReinaSell()`
- **決戦条件＝孤高 stage≥2 かつ 評判≥60 かつ 個性設備**：`reinaHasCharacter()`＝本格サウナ＋水風呂に一級の設え（bath2/sauna3/sauna2/mizu2/chair2のいずれか）。満たすと`kessen`「あなたの湯には帰る場所がある。それだけは金で作れない」→`solved.reina`＋`ally`＋競合圧解除
- **玲奈の仲間効果**（`reinaAllyOn()`）：①設備15%off＝`eqPrice(id)=price×0.85`（renderShop表示＋購入判定＋設置差引に反映・ショップに「玲奈割15%off」表示）②集客↑＝`startBiz`で`n+5`③常連化↑（集客増が常連母数を押し上げ）
- **売却エンド（作者要望＝悲しそうな親父）**：`doReinaSell()`＝`cash+2,000万`・`flags.ended='sold'`（復活エンド再判定を停止）・`reina.resolved=true`。Story `STORY_REINA_SELL`（art=`oyajiSad`＝薄暮／暖簾を下ろし「ゆ」の跡だけ残る店／「蒼天SPA 分館 建設予定」看板／背を向け肩を落とす親父＋足元の桶）→`returnToTitle()`。**セーブは消さない**（安全策＝確認ゲートで意図を確認済みだが破壊はしない）
- **記録UIゲージ**：玲奈登場後（`flags.reinaMet`）は［孤高 X/2・評判 Y/60・個性設備:○×］。未登場は？？？
- ブラウザ検証：訪問=評判50で発火→13行Story完走→reinaMet/pressure=on／競合圧で客数38→29.6(×0.78)／買収断るでstage+1／stage2＋評判60＋個性設備(sauna1+mizu2追加)で決戦発火→ally・solved.reina・圧解除・設備1,500,000→1,275,000／引き抜き=retain(−150k・staff維持)・trust低絆(staff−1)・trust高絆(残留+評判2)／売却=確認ゲート「❄最後の確認」→+2,000万・ended='sold'→oyajiSad描画→タイトル復帰。console error 0
- これで**第1章のボスは完**（田所・鬼頭・黒田・玲奈＋親父）。玲奈の仲間効果・数値バランス、売却エンドの台詞は作者リライト待ち

## 2026-07-23 追加アップデート その12（作者指定・対応済み）＝ボス2体目「黒田修司」（同級生・数字/継続）を実装

作者方針＝ボスを1体ずつ。田所（①地域/伝統）に続き、2体目の**黒田修司**（エリート同級生サラリーマン40歳・テーマ＝数字/継続）を「味方に引き込むarc」として実装。倒す敵でなく、数字と継続を証明して認めさせる相手。台詞は叩き台。

- **黒田トラッキング**：`G.kuroda{met,stage,resolved,ally,nextDay}`＋`newKuroda()`＋save/load/reset。`kuroda`識別子はその11で鬼頭改名により解放済みの枠を使用（`solved.kuroda`と一致）
- **発火＝条件ベース**：`maybeKuroda()`をenterPrepPhaseの優先チェーン末尾に追加（親父エンド→鬼頭決着→**田所→黒田**の順、`if(maybeTadokoro())return; maybeKuroda();`）。**評判35**で登場（田所より後・店が軌道に乗ってから）。以後数日おき（`nextDay`＝+5）に`event`が反復
- **二択**（`kurodaModal`＝`kurodaTitle/kurodaInfo/kurodaChoices`）：**📊数字で示す**→経営姿勢`stage`+1（決戦に近づく）／**🤝現場を見せる**→常連の絆`najimi`+6（黒田は納得しないが田所側に効く＝トレードオフ）
- **決戦条件＝経営 stage≥2 かつ 数字2/3**：`kurodaBiz()`が3条件を判定＝①直近5日で3日以上黒字（`G.recentProfits`＝`closeDay`で日次収支を5日分リング記録）②手元資金50万＋③健全経営（直近5日資金ショート無し＆借金≤300万＝**ヤミ金不使用の読み替え**。ヤミ金は未実装のため）。stage2でも数字が足りなければ`event`が続く（詰み防止）。決戦で認める→反転台詞「逃げてたのは俺の方だったのかもな」→`solved.kuroda`＋`ally`
- **黒田の仲間効果**（`kurodaAllyOn()`・`closeDay`）：①人件費4%off（`staffCost×0.96`）②金利25%off（`interest×0.75`＝0.5%→0.375%相当）③経費6%off（`amenityCost`・`towelCost×0.94`）④会社員客↑（`spawnCustomer`で`weights.salaryman+12`）
- **記録UIゲージ**：黒田登場後（`flags.kurodaMet`）は［経営姿勢 X/2／黒字N日:○×・手元50万:○×・健全:○×（Y/3）］を表示。未登場なら？？？のまま
- ブラウザ検証：intro=評判35未満で不発／評判36で登場（flags.kurodaMet）／現場choiceで絆+6・stage据置／event反復（nextDay前は不発）／数字choice×2でstage2／数字条件bad(count0)なら決戦不発でevent継続、good(count2+)で決戦発火「黒田が、認めた」／決戦解決でally・solved.kuroda・金利5000→3750・人件費27000→25920／save-loadでkuroda/recentProfits永続／記録ゲージ「経営姿勢1/2／黒字3日:○・手元50万:○・健全:○（3/3）」表示／エンド=黒田単独solvedでは不発・4対立＋親父で発火。console error 0

### 親父の"ながら確執"（通奏低音・軽い小言）を追加
作者決定＝親父は独立編にせず、ゲーム全体で確執（新設備・借入に文句）→復活エンドで回収。その手触りを軽く実装。
- `OYAJI_NAG`（data.js・叩き台）＝{equip,loan}×{karai,yawa}。`oyajiNag(kind)`＝全画面イベントにせず**log＋toast**で軽く。**関係`oyajiRel≥45`で口調が辛口→軟化**、**1日1回まで**（`flags.lastOyajiNagDay`）、**初日は控える**、**和解後(`solved.oyaji`)は言わない**
- フック①**立派な設備**（`price≥200000`＝風呂/サウナ/水風呂のみ、小物のカラン/ロッカー/ベンチ/イス/自販機/植物は対象外）を設置＋確率60%で`oyajiNag('equip')`。②**借入**は初回=STORY_LOAN（全画面）／2回目以降=`oyajiNag('loan')`
- ブラウザ検証：初日suppress／関係0で辛口「また金のかかるもんを入れて…」／同日2回目はcap／関係60で軟化「無理はするな。体も、金もな」／和解後は沈黙／閾値=風呂サウナ水風呂だけ発火・小物は静か。console error 0
- 次：**玲奈**（蒼天SPA開発責任者・③設備/業界。引き抜き・2000万買収エンド分岐・競合圧＝重い新システム込み）で第1章のボス完

## 2026-07-23 追加アップデート その11（作者指定・対応済み）＝ボス整理（黒田＝同級生に譲る／ヤクザ改名）＋田所実装＋4対立構成

作者が完成版100日シナリオの人物設定を提供。元々「黒田」はエリート同級生サラリーマン**黒田修司**の予定だった→うちのヤクザが名前を借りていたので、**ヤクザを「鬼頭（きとう）」に改名**して両キャラを活かす。ボスは三人（田所／黒田修司／玲奈）＋別枠の反社（鬼頭）＋通奏低音の親父。

- **ヤクザ改名 黒田→鬼頭**：コード識別子も全面リネーム（`kuroda/Kuroda/KURODA`→`kito/Kito/KITO`、`G.kito`・`KITO_OUT`・`KITO_PAYOFF`・`kitoModal/kitoTitle/kitoInfo/kitoChoices`・`resolveKito`・`maybeKitoShowdown`等）、表示文字列「黒田」→「鬼頭」「黒田組」→「鬼頭組」。`kuroda`識別子は今後の黒田修司用に解放。※「黒塗りのベンツ」は改名対象外（色名）
- **エンド構成をOption Aに（作者選択）**：**4つの対立＋親父＝最後の扉**。`PROBLEMS`＝【田所・鬼頭・黒田修司(kuroda)・玲奈(reina)】の4枠。`checkGrandEnding`＝4対立全solved **かつ** 親父和解`solved.oyaji`（`oyajiRel≥70`で`STORY_OYAJI_CLEAR`）で発火。**「常連客問題」は田所に統合**（旧joren枠・STORY_JOREN_CLEAR撤去、najimiは田所の決戦＆鬼頭の街ぐるみが共用）。親父は独立枠でなく記録の下段に「🚪最後の扉」として別表示。`newSolved`=`{tadokoro,yakuza,kuroda,reina,oyaji}`
- **田所源造＝実装（ライバル①・地域/伝統・味方に引き込むarc）**：`G.tadokoro{met,stage,resolved,ally,nextDay}`＋`newTadokoro`＋save/load/reset。発火は**条件ベース**＝`maybeTadokoro()`（enterPrepPhaseで親父エンド→鬼頭決着→田所の優先順）。サウナ設置＆評判15で登場（反対表明）→中間イベント（古参vsサウナー衝突／不器用な手伝い）で毎回**共存/改革の二択**（`tadokoroModal`）。共存→常連の絆`najimi`+18、改革→評判+3。**決戦**＝`najimi≥65 かつ 評判≥40`で田所が認める→`solved.tadokoro`＋`ally`。台詞は`TADOKORO_TEXT`＝叩き台
- **田所の仲間効果**：`tadokoroAllyOn()`で①来店客+4（地元客）②客の初期満足度+3③設備消耗×0.6（維持費↓）④修理費20%off（`repairCost`）
- **記録UI刷新**：4対立＋親父の扉（`.kiroku-final`＝破線で仕切り）、田所は［絆/65・評判/40］ゲージ、黒田修司・玲奈は未登場＝？？？（`flags.<key>Met`で解禁）。カウントは「おさめた対立 X/4」
- ブラウザ検証：記録=0/4＋親父扉表示／田所intro（サウナ＋評判15）→共存で絆+18・stage進行→eventA→決戦（絆70/評判45）で田所認める・solved・ally／修理費¥160,000→¥128,000（20%off）／改革=評判+3・絆不変／エンドゲート=4solvedでも親父未和解なら不発、和解で発火(revival)／鬼頭改名クリーン（決着モーダルに「黒田」残存なし）／save-loadでtadokoro永続。console error 0
- 次：**黒田修司**（同級生・数字/継続・27〜50日相当の条件ベース＝経営黒字/手元資金/ヤミ金不使用で味方に）→**玲奈**（蒼天SPA競合・引き抜き・2000万買収エンド分岐）

## 2026-07-23 追加アップデート その10（作者指定・対応済み）＝第1章エンディングを物語ドリブンに（純資産1億ゴール廃止）

作者決定：**純資産1億ゴールは不要**。第1章エンドは**5つの問題を全部クリア→親父復活**で発火（数値ゴールなし）。「俺はもう大丈夫だから、ここはまかせろ」→第1章 完→第2章「独立編」。

- **5問題システム**：`data.js`の`PROBLEMS`（joren/oyaji/yakuza/tadokoro/reina）＋`G.solved`フラグ（`newSolved()`・save/load/reset・旧セーブは黒田resolvedからyakuza救済）。田所・玲奈は`mystery:true`で未登場（名前伏せ）
- **常連客問題・親父問題を"解決イベント"化**（新設定不要で実装）：絆`najimi`≥70で`STORY_JOREN_CLEAR`、関係`oyajiRel`≥70で`STORY_OYAJI_CLEAR`が`afterReport`で発火し`solved`を立てる（しきい値`JOREN_CLEAR_AT`/`OYAJI_CLEAR_AT`）。関係を70まで届かせるため**親父との関係にも受動成長**を追加（親父登場後・満足度68+の日に+1／`closeDay`）。日報の絆/関係行を「/70」ゲージ表示に変更（未解決問題のみ）
- **ヤクザ問題**＝`resolveKuroda`で決着時に`solved.yakuza`（どの結末でも）
- **グランドエンド**：`checkGrandEnding()`＝5問題全trueで`STORY_ENDING`（親父復活・叩き台）を再生→`flags.ended`（二度は出ない）。`enterPrepPhase`にフック（`if(!checkGrandEnding()) maybeKurodaShowdown()`）
- **エンディング一枚絵**：`StoryArt.revival`（朝の空＋朝日と光条＋塗り直した銭湯＋ネイビー暖簾＋親父〈白髪・桶〉と主人公が並ぶ）
- **再建の記録モーダル**：準備画面に【📖 記録】ボタン＋`kirokuModal`＋`renderKiroku()`。5問題を✅/未＋説明＋常連/親父はゲージ表示、未登場ボスは「？？？」。X/5カウント。CSS`.kiroku-*`追加
- ブラウザ検証：記録モーダル（0/5→2/5、田所/玲奈は？？？、絆44/70・関係52/70ゲージ）／yakuza=payoffでsolved／joren=絆72でclearイベント＆solved／oyaji=関係71でclearイベント＆solved／全5解決→`checkGrandEnding`発火・`STORY_ENDING`(art:revival)再生・再発火なし／save-loadで`solved`全項目＋`ended`永続／revival一枚絵は朝空(RGB164,203,240)＋親父白髪・桶＋主人公の並びを実画面で確認。console error 0。※テスト環境のブラウザHTTPキャッシュがstory.jsの`<script>`読込だけ古いまま張り付く事象あり（fetch?v=は最新取得＝ディスクは正／SWはnetwork-first＝実機は影響なし）

## 2026-07-23 追加アップデート その9（作者指定・対応済み）＝黒田の決着に「関係性」を導入

その8で作った黒田の決着を、作者指定で“ちゃんと関係性ゲー”に。決着の結果が固定ではなく、**常連・親父との積み重ね**で変わるようにした。

- **街ぐるみで追い返す＝成功率制に**。成功率は**常連との絆（`G.najimi` 0–100）**しだい（`machiChance()`＝絆<20は不可、以降(絆-15)/70で最大95%）。失敗すると常連が動かず不発（`machiFail`結末・未決着）→数日おいて出直し。評判40固定ゲートは廃止
- **常連客イベント**を新設（`STORY_NAJIMI`×3・叩き台）。評判18+で数日おき（`flags.lastNajimiDay`／`najimiIdx`で順送り）に一幕が起き、絆が+12〜14。加えて満足度の高い日は絆が受動的に+1〜2（`closeDay`）。黒田接触後は日報に「常連との絆」行を表示
- **大金で手を切る＝¥5,000,000一括**に変更（`KURODA_PAYOFF` 50万→500万）
- **顔を立てて付き合う＝永続の“付き合い”集金**に。決着後もずっと週1ペースで黒田が集金に来る（`startDay`のally枝＝`lastAllyDay`から5日以上＋確率、お断り設定に非依存）。額は通常の半分（`startMikajime`のally分岐）、専用の上機嫌セリフ（`showMikajimeModal`）、断っても**壊されない**（顔を潰すだけ・`refuseMikajime`のally早期return）
- **親父に相談＝成功率制に**。成功率は**親父との関係（`G.oyajiRel` 0–100）**しだい（`oyajiChance()`）。親父初登場で関係20スタート、以後`STORY_OYAJI`×3（叩き台・数日おき）で+12〜14。失敗すると`oyajiFail`結末（未決着）→出直し
- 保留・失敗のあとは数日決着を出さない（`kuroda.nextShowdownDay`＝`maybeKurodaShowdown`のクールダウン。毎日モーダルが出るのを防止）
- save/load/resetに`najimi`/`oyajiRel`、`newKuroda`に`nextShowdownDay`/`lastAllyDay`を追加
- ブラウザ検証：絆10/関係0/親父無→machi・oyaji・allyロック＆payoff¥5,000,000表示／絆90・関係70→成功率95%・79%表示／machi強制失敗=machiFail未決着+クールダウン、強制成功=評判+8・newRivalSeed・resolved／payoff=現金−¥5,000,000／ally=resolved後に5日周期で集金・額半分(¥45,000)／oyaji強制失敗=oyajiFail未決着／常連イベントで絆+12・親父登場で関係20＋日報に絆/関係行／save-load往復で全項目永続。console error 0

## 2026-07-23 追加アップデート その8（作者指定・対応済み）＝100日シナリオ ボス1体目「黒田」

作者方針＝100日シナリオは「ボスを1体ずつ肉付け」。1体目は黒田（みかじめのヤクザ＝その7の種）。決着の勝ち方は作者が“全部いい”→**選択肢を分岐化**して4結末とも実装。（※その9で決着に関係性を追加。金額は¥500,000→¥5,000,000に更新済み）

- **黒田トラッキング**（`G.kuroda`＝met/encounters/paid/paidTotal/refused/destroyed/resolved/outcome/ally。`newKuroda()`・save/load・resetStateに追加）。初回みかじめで黒田が名乗る（`showMikajimeModal`が初回だけ登場セリフ）。pay/refuseで各カウント加算
- **決着イベント**（`maybeKurodaShowdown()`＝met && !resolved && encounters>=3で、翌日のprep入りに`kurodaModal`を表示。`afterReport`のenterPrepPhaseにフック）。選択肢は`buildKurodaShowdown()`が動的生成＝アンロック条件付き：
  - `machi` 街ぐるみ（評判40+）→評判+8＋`G.flags.newRivalSeed`（次ボス布石）
  - `payoff` 大金¥500,000→現金減・平穏
  - `ally` 用心棒（paid>=2）→`kuroda.ally=true`（味方favorは今後肉付け）
  - `oyaji` 親父（`flags.father`）→評判+4＋`G.flags.oyajiKuroda`（親父ボス伏線）
  - `hold` 保留（また出る）
- `resolveKuroda(id)`で効果適用＋結末テキスト（`KURODA_OUT`／叩き台）に差し替え→とじる。resolved後は`startDay`のみかじめ予約ゲートで二度と来ない
- ブラウザ検証：3回遭遇→決着表示（払った¥120,000表示）／全4択のアンロック・ロック（評判20・親父無・paid1でmachi/ally/oyaji=disabled、payoff/hold=有効）／machiで評判+8・newRivalSeed・再表示なし・みかじめ予約0／payoffで−¥500,000／save/loadでkuroda永続。エラーなし

## 2026-07-23 追加アップデート その7（作者指定・対応済み）＝100日シナリオの第一歩（ヤクザ）

1. **のれんをネイビーに**（男湯なので紺。`drawFloorAndWalls` の暖簾色 `#c0392b`→`#1f3a6b`）
2. **温度と光熱費を連動**（`tempSurcharge()`＋`CONF.tempCostPerDeg=100`）。風呂・サウナを各設備の既定温度より1℃上げるごとに¥100/日。下げても割引なし（既定＝下限）。設備パネルに現在の上乗せ額、日報の光熱費に「うち温度上乗せ」を表示
3. **強面客とみかじめ料システム**：
   - 一般客が浴室で強面客と居合わせると怖がって満足度−7＋不満吹き出し（`yakuzaPresent()`／`c.sawYakuza`／`LINES.yakuzaGripe`）
   - 運営メニュー【入店ルール】に「刺青・強面お断り」トグル（`opts.banYakuza`）。ONで強面客はスポーンしない
   - お断り中は不定期（`startDay`で50%の日に`G.mikajimeAt`予約）に**みかじめ料イベント**（`startMikajime`）。額は評判・日数でスケール（¥25,000〜¥120,000）
   - 払う＝現金減（日報に「⚠ みかじめ料」行・収支に反映）／断る＝腹いせに1〜3設備を`cond=0`（故障中）に。番台は対象外（`payMikajime`／`refuseMikajime`）
4. **黒塗りベンツのカットシーン**（横向き・側面図。`startBenz`/`updateBenz`/`drawBenz`/`drawThug`／`G.benz`オブジェクト）：右下から走ってきて入口の真下(`BENZ_STOP_X`)に停車→強面2人が降りて立つ→（みかじめは停車保持`hold:true`、通常来店は`hold:false`で自動発進）→決着で`releaseBenz`→左へ去る→去りきったら`onDone`で営業再開。イベント中はゲームを一時停止し、ベンツ演出だけ実時間で動かす（`frame`のpauseゲート外で`updateBenz`）
   - ブラウザ検証：入場→停車(x=208)→モーダル→払う(−¥37,000)→退場→再開 の一連、通常来店の自動発進、温度上乗せ表示、お断りトグル、ネイビーのれん、いずれも実画面＋DOMで確認。エラーなし
5. **光熱費と水道代を別建てに**（作者指定・追加）：`waterCost()`（wash/furo/mizuの占有面積×`CONF.waterPerTile=350`）と`heatCost()`（wash以外のrun）に分離。カラン=水道代のみ（光熱費0・状態/修理で不変＝一定）、風呂/水風呂=光熱費(run)＋水道代(大きさ分)、サウナ=光熱費のみ。日報に「水道代」行、設備パネルに光熱費/水道代を出し分け。実測：カラン¥350・水風呂¥700・浴槽¥1,400・サウナ水道代0
6. **ベンツを作り直し**（作者指定・追加）：①速度をゲーム速度連動に（`[70,130,230][speedIdx]`＝速度1はゆっくり迫る）②「タクシー感／金の線」をやめ、角ばった一昔前の黒塗りセダンへ＝直線的なボディ＋直立ルーフ＋濃いスモークガラス＋クローム縦格子グリル＋角目＋クロームバンパー。金色モール(`#c9a24a`)は削除。6倍拡大レンダーで細部確認済み

## 2026-07-23 追加アップデート その6（作者指定・対応済み）

1. **営業中も設備タップで設備メニュー（selPanel）を表示**（`pointerup`のbizブランチをtoast→`selectEquip(it)`に。空タップで`deselect`）。営業中は**移動・売却を非表示**（使用中設備を動かして不具合を出さないため／改装は準備中）、**情報・湯温/室温調整・修理**は可能。運営/一時停止/速度（bizPanel）はパネルを開いても残る。ブラウザDOM検証＋実画面で確認済み

## 2026-07-23 追加アップデート その5（作者指定・対応済み）

1. **女性客は全員ロングヘア**（`drawChar`。`TYPES[].sex==='f'` に背面＋顔両脇の毛束を追加。湯では湯面に髪が広がる）。男性は短髪のまま
2. シャンプー・ボディソープの運営メニュー説明文を作者案に差し替え（「無料=集客↑満足度↑ 無料設置時は¥1,000/日。手ぶらセット・販売時は¥50/1本を自動で仕入れる。」）＝無料設置は定額のみ・従量仕入れは手ぶら／販売時、という実装どおりの表現に
3. **夕凪湯は「男湯」に確定**（`CONF.menOnly=true`）。男性客だけが来店し、女性タイプ(おばちゃん/OL)はデータとして温存＝女湯・新店解放時にfalseで復活。男女別マップ改修は見送り
4. **男キャラを2種追加**：`kinpatsu`（金髪の兄ちゃん＝金髪＋派手ジャケット・短気）／`yakuza`（強面の親分＝スキンヘッド＋サングラス＋への字の怒り眉＋胸の刺青・肝が据わって行列に強い）。見た目は `TYPES[].bald/shades/tattoo` フラグで`drawChar`が分岐
   - **サングラス**は二枚レンズ＋細いブリッジ＋各レンズの反射で「サングラスらしさ」を出す
   - **刺青**は胸割りの和彫り（両胸の藍＋中央に地肌のスジ＋緋色の牡丹＋金の芯＋白抜きの波）を素肌（タオル姿）のとき表示
   - **「ととのい」のキラリはサングラス越しにも出す**（四芒星がレンズ上に乗る＋頭上のきらめき）

## 2026-07-23 追加アップデート その4（作者指定・対応済み）

**演出**
1. カラン利用中はシャンプー（頭の泡＋両手＋飛沫）を描画
2. 垢すりタオル・サウナマットは置き場まで取りに行き、使い終わると**返しに行く**（状態 `atRack`／`c.carry`）。持っている間は手に持った絵
3. サウナではマットを敷いた上に座る（`drawChar`でマットを体より先に描画）
4. マット／垢すり置き場は「台の上に大きな長方形1枚」に簡素化
5. **ととのった客は休憩中に目がキラリ**（四芒星＋頭上のきらめき）
6. 着替え後の描き分け：**男は腰にタオル（素肌）／女は体に巻く**（`TYPES[].sex`）

**システム**
7. **手ぶらセット**（タオル＋シャンプー＋ボディソープ／¥300・¥500・¥700）。手ぶらで来た客(`c.tebura`)が買う。導入中は**シャンプーの無料設置は選べない**。タオル無料貸出だと誰も買わない
8. シャンプー・ボディソープの仕入れは**1本¥50・使われた分だけ自動仕入れ**（`CONF.soapUnitCost`）。無料設置は別途ポンプ代¥1,000/日
9. 速度表記を **1・2・3** に（実倍率は1/2/4のまま。`CONF.speedLabels`）
10. **ととのい率**を設備充実度と清潔さで変動（`totonoiChance()`）。順を踏んでも失敗することがある。**ととのった客の比率が高いほど評判UP**
11. **受付が詰まると2人目以降は店の外**（入口から右の壁沿い）に並ぶ。待ちくたびれると帰る＝機会損失を日報に計上
12. **ロッカー満杯**で荷物を置けない客は脱衣所で立ち往生（不満増）。他の客が出るまでロッカーは開かず、新規客も入れない

## 2026-07-23 追加アップデート その3（作者指定6項目・対応済み）

1. **連続設置**: 「やめる」を押すまで同じ設備を置き続けられる（全設備。`startPlacing`の`placedN`／`once`）
2. **シャンプー・ボディソープ**: ON/OFF → なし / 無料設置 / 販売 の3択。販売はシャンプー・ボディソープを別々に¥50/100/150/200で値付け（`G.opts.soapMode`／`shampooPrice`／`bodysoapPrice`）。高いと買われず「手ぶら客」は不満
3. **営業中も運営メニュー**: 営業パネルに⚙運営を追加、並びは 運営・一時停止・速度
4. **アメニティの置き場**: サウナマット・垢すりタオルはONにすると浴室内の場所を選んで設置（`matrack`／`akarack`＝`bathOnly`）。効果は現物の有無で判定（`hasMat()`／`hasAkasuri()`）。OFFで撤去
5. **入浴料**: ¥680を廃止し「自由」を追加。¥380〜¥3,000のスライダー（`FEE_RANGE`／`o.feeCustom`）
6. **サウナ料**: 「無料」を廃止し右端に「自由」を追加。同じく¥380〜¥3,000のスライダー

## 2026-07-23 追加アップデート その2（作者指定5項目・対応済み）

1. 日報の「差引き」行を削除（最上部の本日の収支と重複）
2. 掃除中はモップを動かす描画（`drawChar` の `task==='clean'`。柄＋金具＋糸＋水しぶき）
3. **サウナ料を運営メニュー化**: 固定¥400（自動徴収）→ 無料/¥380/¥480/¥580 の選択制。サウナ未設置なら🔒。払うのはサウナ目当ての客だけ
4. **赤枠のヒント吹き出し**: 運営メニューで直せる不満を客が指摘（料金/サウナ料/タオル/シャンプー/垢すりタオル/サウナマット/人手/掃除）。**料金への不満は施設の充実度（`facilityScore`）に応じて減少**＝納得料金 `worthFee()` を運営メニューにも表示。日報の「お客の声」にも⚠付きで残る
5. タオルは設置するだけで経費¥1,000/日（無料貸出なら＋客数×¥60の洗濯代）

## 2026-07-23 追加アップデート（作者指定13項目・対応済み）

ガラス引き戸／移動費／全設備4方向回転／耐久度バー常時表示／応急修理のトンカチ演出／速度1の減速／収容人数表示／日報の収支を最上部／満員・故障で行列＋不満／修理と応急修理の明文化／新着マーク／耐久度の減り緩和／営業中もカタログ表示。詳細はREADME.mdの「主なシステム」を参照。

---

## 対応済み（2026-07-23）

5項目すべて反映・ブラウザ検証済み。

1. 日報に「まだ赤字だが評判は上がっている。慌てて借りるな」ヒントを追加（`js/game.js` `closeDay`。条件: 赤字 && 評判上昇中 && 5日目以内）
2. 設備説明文を状態%依存に（`js/data.js` `equipDesc()`。古い設備は修理でdescFixに切替、新品も30%/60%未満でヘタり注記）
3. 開店直後(9〜11時)の来客重みを底上げ（`js/game.js` `hw` 配列: `[2,2,3,...]`→`[4,4,4,4,4,4,5,...]`）
4. 光熱費表示に「古い設備は修理しても燃費は悪いまま」を明記（`js/game.js` `selectEquip`）
5. 準備画面に次の評判解放設備をヒント表示（`js/game.js` `enterPrep`）

---

# プレイテスト改善メモ（2026-07-23 実施）

Claude Codeでタイトル〜1日目〜6日目まで実際にプレイして見つけた点。
次にこのプロジェクトを触るときの参考に。

## プレイ内容

導入ストーリー→屋号決め→準備フェーズ（遠赤サウナ¥800,000＋水風呂¥400,000＋ととのいイス¥50,000を配置）→営業6日分を実プレイ。

## 収支の推移（実測）

光熱費は6日間ずっと固定¥21,300。

| 日 | 客数 | 収支 | 満足度 | 評判 |
|---|---|---|---|---|
| 1 | 14人 | ¥-11,380 | 73 | 10→13.2 |
| 2 | 13人 | ¥-11,860 | 83 | 13.2→17.7 |
| 3 | 16人 | ¥-10,820 | 72 | 17.7→20.8 |
| 4 | 20人 | ¥-6,100 | 80 | 20.8→24.9 |
| 5 | 25人 | ¥-2,900 | 76 | 24.9→28.5 |
| 6 | 27人 | ¥-1,940 | 68 | 28.5→31.1 |

5〜6日目でほぼ黒字転換。数値バランス自体は悪くない（経年設備のせいで最初苦しい→評判を上げると客が増えて黒字化、という筋は通っている）。

## 良い点（維持すべき）

- **配置UX**: 設備を置くとき既存設備と重なると赤枠、空いてれば緑枠になる確認フローが分かりやすい
- **中古設備の修理上限75%**: 番台やロッカーなど「親父の代からの古い設備」は修理しても75%までしか戻らない（新品は100%）という地味だが効く設計（`js/game.js` `btnRepair`ハンドラ、`it.cond = EQ[it.id].old ? 75 : 100`）。「いずれ買い替えが必要」という誘導が仕組みに組み込まれている
- **ストーリー導入**: 終電→親父倒れる→ボロボロの銭湯、の掴みがしっかりしていて次にやる気にさせる

## 気になった点

### 1. 開店1〜3日目が「ずっと赤字」で、それが意図的な演出だと伝わらない
光熱費¥21,300固定に対し初日の売上はたった¥9,920。評判が貯まって客数が増えるまで4〜5日は毎日1万円前後の赤字が続く。ゲームとしては筋が通っているが、画面上にはそれを保証する言葉が一切ない。カジュアル層（カイロソフト風を想定）だと不安になって不要な融資に手を出すプレイヤーが出そう。

→ **改善案**: 日報か開店演出に「最初は経費がかさむのが普通」的な一言ヒントを添える（低コスト・高効果）

### 2. 朝(9〜11時)は客がほぼ来ない
`js/game.js` の来客時間重み配列 `[2,2,3,3,4,4,5,6,8,9,9,8,6,4,2]`（9時→23時、`updateBiz`付近）だと、開店直後の2時間は全体の約5%しか客が来ない設計。実プレイでも9:00開店から最初の客が来たのは14時過ぎだった。速度4倍にしても間延びして感じた。

→ **改善案**: 重み配列の最初の2〜3枠を+1程度底上げして開店直後の間延び感を軽減

### 3. 修理しても光熱費が変わらない
古い浴槽(状態7%→修理で75%)を修理しても「光熱費: ¥2,500/日」の表示は変化なし。修理は満足度・故障率には効くが経費には効かない仕様自体は理解できるが、プレイヤーは「直したのに経費が減らない」と誤解しそう。

→ **改善案**: 修理パネルに「古い設備は直しても燃費が悪いまま」と明記するか、経費表示に理由を添える

### 4. 設備の説明文が状態(%)に追従しない
古い浴槽を7%→75%に修理しても、説明文は「ヒビだらけ。ぬるい。」のまま変わらない。壊れかけの見た目のままなのは違和感がある。

→ **改善案**: フレーバーテキストを状態%で出し分ける（例: 50%未満なら現状の文言、それ以上なら「大分マシになった」的な文言）

### 5. （参考）評判解放の設備一覧が準備画面から見えにくい
評判15/18/20/25/30で解放される設備一覧は運営メニューを開かないと見えない。準備画面のヒントに一度出すと目標が分かりやすくなりそう。優先度は低め。

## 優先度順まとめ

1. 初日〜数日目の赤字が正常運転であることを明示（低コスト・高効果）
2. 設備説明文を状態%依存にする
3. 朝の客足を少し底上げ
4. 修理と光熱費の関係を明示
5. （余裕があれば）評判解放設備の導線改善
