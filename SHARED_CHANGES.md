# 共有ファイルへの変更（第2章セッション → 第1章セッションへの引き継ぎ）

**2026-08-06。第2章「横浜編」の作業で、共有ファイル（`js/game.js` / `js/data.js` /
`css/style.css` / `index.html`）に入れた変更の一覧です。**

- **第1章の挙動は変えていません。**すべて「章がフックや設定を持っていなければ、
  これまでとまったく同じ値になる」書き方にしてあります
- 各変更のあと `await guardAll()` が `[{ok:true, chapter:1}, null]` を返すことを確認済み
- **`js/data.js` に触ったのは1か所だけ**（`isPortable`。作者了承のうえ）

## 両セッション共通の約束（2026-08-06・実際に一度踏んだので明文化）

**共有コードの文言や数字を疑うときは、「いま、どの章の `EQ` / `CONF` を見ているか」を先に確認する。**

`EQ` も `CONF` も `applyChapter(n)` で中身が丸ごと差し替わります。
第2章をロードしたブラウザで `EQ.matrack.price` を引くと `EQ_Y` の値（¥60,000）が返り、
**第1章のデータ（`price: 0`）を見ているつもりで別章の数字を読みます。**

実際にこれで「第1章の『（無料）』という表記が実物と違う」と誤報しました。
第1章の表記は正しく、値段が付いていたのは第2章のほうでした。

確かめ方：`guardBoot(1)` してから引く／または `js/data.js` と `js/ch2b/data_y.js` を直接読む。

---

## 1. `js/data.js` — `isPortable`（★申請待ちコードに触っています）

```js
function isPortable(def) {
  return !!def && def.portable !== false            // ← この1行を足した
      && (def.portable === true || def.cat === 'rest' || def.cat === 'amenity');
}
```

**なぜ**：第2章の「畳の小上がり」「よもぎ蒸しの個室」「キッズスペース」「ワークスペース」
「静かなテレビ」は `cat:'rest'` だが、床や配線を伴う**造作**なので移動を有料に戻したい（作者決定 8/6）。

**第1章への影響**：`portable: false` を持つ設備は第1章に**1つもありません**。
無料対象は11件のまま変わらないことを実測で確認済み。データ指紋も変わりません。

---

## 2. `js/game.js` — 設備IDの章依存を外した（`hasRole` / `roleIds` / `roleBuildable`）

```js
function roleIds(role, ch1Id) { return (CONF.roleIds && CONF.roleIds[role]) || [ch1Id]; }
function hasRole(role, ch1Id) { return roleIds(role, ch1Id).some(id => hasWorking(id)); }
function roleBuildable(role, ch1Id) { return roleIds(role, ch1Id).some(id => !!EQ[id]); }
```

**なぜ**：`hasWorking('cooler')` のように**第1章のIDを名指し**している箇所が、
第2章では永久に不成立だった。実害：

- 冷水機（第2章は `y_cooler`）を何台建てても「給水が無い」扱い＝サウナ客が全員 −2、
  「冷水機を置け」というヒントも永久に出続けていた
- ミスト（12%）・塩サウナ（10%）・熱波師（12%）を欲しがる客が、永久に直せない不満を抱えていた

**差し替えた呼び出し**：`hasWorking('cooler')`（3か所）／`hasWorking('sauna_mist')`／
`hasWorking('sauna_shio')`／`nappaOn()` の `sauna_sp`。
**第1章は `CONF.roleIds` を持たないので、第2引数（第1章のID）がそのまま使われます＝不変。**

あわせて `spawnCustomer` の `wantsMist/wantsShio/wantsNappa` に `roleBuildable()` を掛けました
（**カタログに1つも無いものは欲しがらせない**＝直しようのない不満を作らない）。
第1章はミスト・塩・決戦仕様のどれも `EQ` にあるので、これまでどおり全員が転がします。

---

## 3. `js/game.js` — マッサージ機（`isMassage` / `coinPrice`）

`item.id === 'massage'` の名指しが10か所あり、第2章の `y_massage` / `y_x_massage` が
**一度も予定に入らず、1円も売れていなかった**。

```js
function isMassage(id) { return roleIds('massage', 'massage').indexOf(id) >= 0; }
function coinPrice(id) { return (EQ[id] && EQ[id].coin) || CONF.massagePrice || 100; }
```

- 売上の `G.cash += 100` を `coinPrice(item.id)` に（設備ごとに単価を変えられる。
  第2章：1Fの機械は10分¥200、4Fラウンジの椅子は¥100）
- **第1章の `massage` は `coin` を持たないので ¥100 のまま＝不変**

---

## 4. `js/game.js` — 客の予定と、設備の探し方（`planExtra` / `catMatch`）

```js
// buildPlan の最後
chHook('planExtra', c, plan, st);

// 予定の名前 → 設備の探し方
function catMatch(cat) { ... CONF.catFind で差し替え可能 ... }
```

**なぜ**：第2章の垢すり台・ラウンジ・物販は、それぞれ
「客の予定に項目を足したい」「その予定の設備を cat 以外で探したい」に帰着する。
`buildPlan` にフックが無く、`massage` だけが特例として直書きされていた。

- `planExtra` … 章が予定を**足すだけ**（背骨の 風呂→ととのい→飯 は触らない）
- `catMatch` … これまでの `massage` 特例をここに吸収。`CONF.catFind` を持たない章は
  **これまでどおり cat 一致＋massage 特例だけ＝不変**
- 差し替えた箇所：`findFreeEquip` / `catExists` / `hasWorkingCat` の3つの match 式

---

## 5. `js/game.js` — 客をタップすると札が出る（第2章のみ）

- 営業中のタップで、いちばん近い**人**（客・バイト・妻・主人公）を拾って
  `chHook('custTap', 人)` へ渡す。**`hasHook('custTap')` の中だけ**なので第1章は素通り
- `drawEffects` の最後に `chHook('drawTop', ctx)`＝最前面の層
- 採用時に `age: p.age` を `emp` へ写す（`STAFF_POOL` に `age` が無い第1章では `undefined`）

第1章にも入れる話は**保留**（作者判断 8/6）。受け口だけ寝ています。

---

## 6. `js/game.js` — そのほか（すべてフック追加・章ゲート）

| 変更 | 第1章への影響 |
|---|---|
| `custWeight` に `chHook('custWeightMul', k)` | フック未登録＝掛け算が起きない |
| 常連の増減に `chHook('segFan', c, ±1)` | 同上 |
| 子連れの来店条件を2つに割り、`CONF.kidsWithoutBan` を持つ章だけ前半を飛ばす | 札を持たない＝不変 |
| `yakuzaPresent` → **`tattooPresent`**（IDでなく `type.tattoo` で見る） | 第1章で `tattoo` を持つのは強面の親分だけ＝不変 |
| 「怖い」不満の大きさに `chHook('yakuzaGripe', c)` | 未登録なら従来の −12 |
| 「お断り」の対象を `TYPES[k].tattoo` に | 同上 |
| 運営メニューの説明文に `chHook('banYakuzaNote')` | 未登録なら従来の文言 |
| `tog('banYakuza', …)` ほか既存UIはそのまま | — |

---

## 7. `css/style.css` / `index.html`

- `#custCard` と `.cc-*`（客・バイト・妻・主人公の札）を追加。**第2章でしか生成されません**
- `index.html` に `js/ch2b/custcard_y.js` と `js/ch2b/uriage_y.js` の読み込みを追加

---

## 8. `js/dev/ch-baselines.js` — 第1章の指紋を2つ貼り直しました

そちらの作業（`portable:true` の付与・`saunaGoodHot` の分岐）でデータ指紋が変わったため、
**作業が終わったのを確認してから**貼り直しました。理由もファイルに書き添えてあります。

| | 前 | 後 |
|---|---|---|
| `EQ` | `fb91b63d` | `d3fbc77c` |
| `LINES` | `3fb5d9d4` | `ff525748` |

---

## こちらから、そちらへのお願い

1. **日報の「垢すり ◯本」の単位**（`closeDay`）。
   `akasuriRev` / `akasuriN` は定義も日報の行も利益計算もあるのに、
   **加算するコードがリポジトリに1行も無い**枠でした。第2章の垢すり台（ひとり¥3,000）で
   初めて埋めましたが、単位が「本」（もともと垢すりタオルの枚数として作られた行）なので、
   **「人」に直したい**です。`closeDay` は申請待ちなので、こちらでは触っていません。

2. **深夜割増が日報の収入から漏れています**（第2章だけの問題ですが、直すのは `closeDay`）。
   `bathRev = t.paid * G.opts.fee` と再計算しているため、`takePayment` が
   `take` に足した深夜割増（`nightRev`）が**収入合計にも利益にも入りません**（`G.cash` には入る）。
   `nightRev` は加算だけされて、日報に表示もされていません。

3. `soapUnits` は集計されているだけで、どこからも読まれていません（第1章の話）。

4. **日報のチップ「返済・税」の名前**（`closeDay` の表示・`chip('返済・税', …)`）。
   第2章で `chHook('dailyExtraCost')` を初めて使い始めました（5F食堂の板前の日給と、
   その日に出した皿の原価）。フックそのものは以前から `closeDay` にありましたが、
   **どの章も登録していなかったので、これまで常に0**でした。第1章は今も0のままです。
   ただ、チップの名前が「返済・税」に決め打ちなので、第2章では中身と合いません。
   `chHook('extraFixLabel') || '返済・税'` のように章で差し替えられると助かります。
   `closeDay` は申請待ちなので、こちらでは触っていません
   （当面は `dayReportExtra` に「食堂の粗利　売上 − 板前 − 原価」の行を自前で出しています）。

---

## 9. `js/game.js` の汚れまわりを2か所（2026-08-06・**第1章は不変**）

第2章で「主人公が立っている階しか汚れない」というバグが出たので、
共有側に**フックの受け口を1つ**足しました。どちらもフックが無ければ従来と同じ値です。

| 変更 | フックが無いとき（第1章） |
|---|---|
| `cleanFactor()` → `cleanFactor(f)`。`chHook('dirtMul', f ?? G.actF)` | 引数なし＝これまでどおり `G.actF`。第1章は `dirtMul` を登録していないので `mul = 1` |
| 汚れを落とす所に `if (hasHook('dirtSpot')) { … } else { 従来のコード }` | `else` 側＝**元のコードそのまま**（`reachableSet()` / `approachTiles()` / `f: G.actF`） |

**何が起きていたか**：`G.dirts.push({..., f: G.actF })` の手前で
`reachableSet()` と `approachTiles(item)` が「いま表示している区画」の盤面を読むので、
主人公が1階にいるあいだ、2階の設備の座標が1階の到達可能マスと噛み合わず、
**候補が空になって汚れが一つも落ちない**（実測：228人が風呂を使った一日で床の汚れ0個）。
区画が1つしかない第1章では常に正しいので、**第2章で初めて出たバグ**です。

`await guardAll()` は `[{ok:true, chapter:1}, null]` を確認済みです。

---

## 10. `js/game.js` — 館内案内図のタップに、章の受け口を1つ（2026-08-06）

```js
// guide の pointerup、guideRects を探す直前
if (chHook('guideTap', px, py)) return;
```

**なぜ**：第2章の案内図（`drawGuide` フックで丸ごと差し替えている絵）に
「⚠ スタッフがいません」の帯を出したので、**それを押したら【👥 バイト】へ飛ばしたい**。
区画の矩形（`guideRects`）とは別の当たり判定が要ります。

**第1章への影響**：`guideTap` を登録していないので `chHook` は `undefined` を返し、
これまでどおり `guideRects` だけを見ます＝不変。

`await guardAll()` は `[{ok:true, chapter:1}, null]` を確認済みです。

---

## 11. `js/game.js` — 吹き出しに「言った区画」を刻みました（2026-08-06・**第1章は不変**）

```js
function bubble(e, text, dur) { e.bub = { …, key: bubKey(e), f: e.f | 0 }; }   // ← f を追加
function hintBubble(e, text)  { e.bub = { …, key: bubKey(e), f: e.f | 0 }; }
function stuckBubble(e, text) { e.bub = { …, key: bubKey(e), f: e.f | 0 }; }
```

**なぜ**：第2章の館内案内図は、ビルの各階に「その階の客が言っていること」を出します。
吹き出しは数秒残るので、**言ったあとに階を移ると、移った先の階の声として拾われて**いました。

- 受付の「よろしく〜」が、2階の男湯から聞こえる
- 風呂上がりの「ととのった〜！！」が、**寝ているはずのカプセル階**から聞こえる

実測：吹き出しの延べ1,178件のうち **684件（58%）が「別の階で言った台詞」**でした。

**第1章への影響**：フィールドが1つ増えるだけで、読むのは第2章の案内図だけです。
区画が1つしかない章では `e.f | 0` は常に 0。描画も判定も一切参照していません。
`await guardAll()` は `[{ok:true, chapter:1}, null]` を確認済みです。

---

## 12. `js/game.js` — 設備の説明を「置く画面」に移しました（2026-08-06・**第1章は不変**）

第2章のカタログは、行を **名前・収容・値段だけ**に詰めて、説明は
**設備を選んだ直後の設置画面**（新しい `#placeInfo`）に移しました。
まだ金は払っていない（払うのは【ここに置く】を押した瞬間）ので、
「買う前に読む」という順番は変わっていません。

変更は4か所、すべて `hasHook('placeInfo')` / `chHook('placeInfo')` のゲート付きです。

```js
// renderShop() ── 一行説明を置く画面に譲る章だけ、行から外す
const noteTxt = hasHook('placeInfo') ? '' : (EQ_NOTE[id] || '');
// renderShop() ── 行を詰める（アイコン24px・余白3px）
list.classList.toggle('compact', hasHook('placeInfo'));

// startPlacing() ── 詳細の箱を出す
const pinf = chHook('placeInfo', id);
$('placeInfo').innerHTML = pinf || '';
$('placeInfo').classList.toggle('hidden', !pinf);
// startPlacing() ── 名前がすぐ上に大きく出ている章では、確認バーで繰り返さない
$('confirmText').textContent = pinf ? (rl ? `${rl}のみ` : '場所をタップ')
                                    : EQ[id].name + (rl ? `（${rl}のみ）` : '');

// endPlacing() ── しまう
$('placeInfo').classList.add('hidden');
```

`index.html` に `<div id="placeInfo" class="hidden"></div>` を確認バーの直前へ、
`css/style.css` に `#placeInfo` 系と `#shopList.compact` を追加しました。

**第1章への影響**：フックが無いので `pinf` は常に `undefined`。
カタログは一行説明つきのまま、`compact` は付かず、`#placeInfo` は `hidden` のまま一度も出ません。
確認バーの文言も従来の `EQ[id].name + （浴室のみ）` のままです。
**`updateConfirmText()` には触れていません**（申請待ちのため）。
`await guardAll()` は `[{ok:true, chapter:1}, null]` を確認済みです。

---

## 13. `js/game.js` — 評判の減点に、章の受け口を1つ（2026-08-06・**第1章は不変**）

```js
function repPenalties() {
  const p = [];
  const add = (k, l, v, sub) => p.push({ k, l, v, sub });   // ← 第1引数に目印 k を足した
  if (!G.opts.banYakuza) add('yakuza', '入墨・ヤクザが入店できる', 30, …);
  …
  return chHook('repPenalties', p) || p;                    // ← 章が拾い直せる
}
```

**なぜ**：第2章は「入墨・ヤクザが入店できる −30」を外す（作者判断）。
この章の強面の客は正規の客層で、デメリットは
**子連れが来なくなる**（`SEG_WANTS_Y` の m_kozure / f_kozure が「怖い客がいない」を6点ぶん求める）と
**居合わせた客の満足度が落ちる**（`yYakuzaGripe`）の2つをすでに持っている。
そこへ−30が乗ると受け入れている限り評判の天井が70点になり、
実質「お断りを選ばないと詰む」＝選択にならなかった（30日通して発見）。

`k` は文言で照合しないための目印。言い回しを直した日に静かに外れるのを防ぐ。

**第1章への影響**：フックが無いので `p` がそのまま返る。
確認済み — 第1章 `['yakuza:30','old:10','akasuri:5']`／第2章 `['akasuri:5']`。
`await guardAll()` は `[{ok:true, chapter:1}, null]`。

### ~~ついでに見つけた~~ → **こちらの誤りでした（2026-08-06 訂正）**

「置き場を設置する（無料）は実物と違う」と伝えましたが、**第1章では正しい表記**です。
js/data.js の matrack / akarack は `price: 0` で、実際に無料。
¥60,000／¥50,000 は `EQ_Y`（第2章）の値で、第2章がロードされた状態で `EQ` を引いた
こちらの読み違いでした。**第1章の文言は変更不要**です。

第2章側で `yRepPenalties()` が該当項目の `sub` を
「浴室に置き場を設置する（**6万／5万**）」に差し替えるようにしました（第1章には影響なし）。

---

## 14. 第1章セッションから受け取ったもの（2026-08-06）

`closeDay` に3件入れてもらいました。第2章側の対応は以下。

- **日報の「垢すり ◯本」→「◯人」** … 対応不要
- **深夜割増を日報へ反映**（収入合計・`profit` の両方に `t.nightRev`、収入欄に「深夜割増」チップ）
  … これで第2章の**深夜割増が日報から消えていた問題は解決**。こちらの積み残しも閉じました
- **固定費チップ `chHook('extraFixLabel') || '返済・税'`**
  … 第2章は **`'板前・原価'`** を返します（`yExtraFixLabel` / shokudo_y.js）。
  中身は源さんの日給＋その日に出した皿の原価

### soapUnits について（保留分の回答）

**→ 作者判断：今回は削除しない**（2026-08-06）。次の申請は「不具合と表記の修正」だけで固めるため、
金額が動かなくても機能に関係ない削除は混ぜない。掃除は別の版でまとめて。第2章に影響なし。

以下は調査結果（削除するときの根拠として残す）。**第2章でも使いません。**
`grep` したところ書き込み2か所（js/game.js:1864, 1871）のみで読み手ゼロ。
石鹸の仕入れ原価は `soapCost = guests * CONF.soapCostPer`（js/game.js:5547）で
**別に計算されていて、経費にはちゃんと乗っています**＝`soapUnits` は二重に数えかけて
やめた名残に見えます。落としても金額は動きません。

### そちらの申請待ち（8件）は把握しました

日報のドリンク台別内訳／田所のお助け条件／「動線」→「導線」／
中温サウナのセリフ分割（`LINES.saunaGood` と `saunaGoodHot`）／`isPortable`／
上記3件（`closeDay`）。これらに触る必要が出たら先に知らせます。

---

## 15. 第1章セッションからの申し送り 3件、対応しました（2026-08-07）

**★1 `confirm()` は使わない** … 了解しました。`grep -rn "confirm(" js/ index.html` → **0件**。
第2章のモーダル（開業準備・物語・雇う／やめておく・源さんの三幕）はすべて自前のDOMで、
ブラウザの `confirm` / `alert` / `prompt` は最初から使っていません。今後も使いません。

**★2 `CONF_Y.furoPrefMax: null`** … 確認しました。第1章 43／第2章 null（頭打ちなし）。
第2章の強面の客（`TYPES_Y.irezumi`）の好み **46℃が下げられていない**ことを実測で確認。
44℃のあつ湯もそのまま彼の湯です。第1章の `TYPES.yakuza` が 43 になったのとは別物。

**★3 `ch-baselines.js` の第1章の指紋、貼り直しました。**
`await guardAll()` → `[{ok:true, chapter:1}, null]`。

| | 前 | 後 | 理由（第1章の意図した変更） |
|---|---|---|---|
| `CONF` | 5f2545aa | **66823bf0** | `furoPrefMax: 43` の新設 |
| `EQ` | d3fbc77c | **3db69614** | あつ湯（bath1）44→43℃ |
| `TYPES` | a4c7c27d | **1cfdcd71** | `yakuza.furoPref` 47→43 |

`LINES`（ff525748）は前回ぶんで一致していたので据え置き。
それぞれ、なぜ動いたかを ch-baselines.js のコメントに残してあります。

---

## 16. 湯温まわりを3つ直しました（2026-08-08・**第1章は不変**）＋★第1章にも入れてほしい

第2章の湯船を整えるなかで、**第1章にもそのまま当てはまる不具合**が3つ見つかりました。
共有コードには**印を持つ設備だけが通る分岐**として入れてあり、第1章の設備には
その印を付けていないので、いまの第1章は計算も台詞も1つも変わっていません
（`await guardAll()` → `[{ok:true, chapter:1}, null]`／第1章に切り替えて
`CONF.furoFitMul` が `undefined`・`noTemp` を持つ湯船が0件であることを実測）。

**作者から「この変更は第1章にも入れたい」と指示が出ています。**
入れるかどうかと、入れ方の最終判断はそちらでお願いします。

### ★いちばん重い話 — 高濃度炭酸泉（¥120万・評判65）を買うと、店が壊れます

第1章の全客層で測った実測値です（各客2,000回・`menOnly` なので男性客のみ）。

| 湯船の組み合わせ | いま | 直したら |
|---|---|---|
| あつ湯だけ | 0%文句 / 満足 2.3 | 0% / 2.3 |
| あつ湯＋ぬる湯 | 1%文句 / 満足 3.8 | 0% / 3.8 |
| あつ湯＋電気風呂 | 0%文句 / 満足 4.0 | 0% / **5.0** |
| **あつ湯＋電気風呂＋炭酸泉** | **51%文句 / 満足 −0.1** | **0% / 5.5** |
| 終盤フル（8槽） | 1%文句 / 満足 4.5 | 0% / **5.2** |

炭酸泉を置いた瞬間に、**サラリーマン93%・金髪の兄ちゃん93%・サウナー青年86%・
強面の親分86%・常連のじいさん53%が「ぬるい」と言い出し、湯船の満足度が平均でマイナスに落ちます。**

原因は `equipFit`（js/game.js:1789）で、炭酸泉は★5なので `(d.q||1)*2 = 10` 点を持っており、
これが `near(温度, 好み)` の一致点を上回ります。結果、**43℃を求めている客まで
38℃の炭酸泉に吸い寄せられ、入ってから「ぬるい」と怒る。**
評判65で解放される終盤の目玉設備が、買うと罰になっている状態です。

### 入れた変更（3つとも `js/game.js`。第1章は印を持たないので不変）

**① `noTemp` — 湯温では採点しない湯**（js/game.js:1912 付近）

```js
if (cat === 'furo' && !def.old) {
  if (def.noTemp) {
    c.sat += 3 + Math.round((def.q || 2) * 0.6);   // ★5なら+6／★3なら+5
  } else {
    …従来の湯温での採点（そのまま）…
  }
}
```

作者の言葉そのままです ——
**「炭酸泉はぬるいことが前提だから、ぬるさに文句を言うのはおかしい。
電気風呂もビリビリが目的だから、ぬるい熱いの文句が出るのはおかしい」**（8/8）。
サウナのミスト・薬草（`def.gentle`）とまったく同じ考え方です。

満足度を★の高さから出しているのは、**一律の数字にすると目玉を買うほど満足が下がる**ため。
★5の炭酸泉は +6＝湯温がぴったり合った湯（+5）より上＝¥120万を出す値打ちがある、という形にしました。

**② `furoFitMul` — 湯温の一致を★の高さより重く見る**（js/game.js:1793）

```js
if (d.cat === 'furo') s += d.old ? 0 : near(t, furoPrefOf(c)) * (CONF.furoFitMul || 1);
```

**水風呂には元から掛かっていた重み**（×1.5・js/game.js:1796 の「★の差に負けて
寒すぎる槽へ入る客が出る」というコメント）を、湯船にも掛けられるようにしただけです。
**湯船だけ手当てが漏れていました。**
第2章は `CONF_Y.furoFitMul = 1.5`。第1章は札を持たないので `×1`＝従来と同一の計算。

**③ `line` — 設備が自分の台詞を指定できる**（js/game.js:2008 付近）

```js
else if (def.line && LINES[def.line]) bubble(c, pick(LINES[def.line]));
```

下の湯温での言い分けに落ちると、第2章の電気風呂（43℃）が**「あ〜熱い！最高！」**と言ってしまい、
ビリビリを目当てに入った客の台詞として噛み合わなかったためです。
**第1章の電気風呂（41℃）も同じで、いまは `LINES.bathGood`＝「いい湯だ」に落ちています。**

### ★そちらで入れるとしたら（提案）

`js/data.js` の EQ に印を足すだけです。**`CONF` には触りません**（指紋が動くので）。

```js
bath_tansan: { …, noTemp: true, line: 'furoTansan',  … },   // ぬるいのが売り。LINES.furoTansan は既にある
bath_denki:  { …, noTemp: true, line: 'furoDenki',   … },   // ビリビリが目的
```

`CONF.furoFitMul: 1.5` も入れるかは、そちらの判断で。**入れると `CONF` の指紋が動きます**
（第1章の意図した変更なので、`ch-baselines.js` の貼り直しが要ります）。
入れなくても①だけで炭酸泉の51%は消えますが、`bath1`＋`bath_denki` の満足度 4.0→5.0 の伸びは
②のぶんなので、両方入れたほうが素直です。

`LINES.furoDenki` は第1章にありません。第2章は `LINES` 本体を触らずに
**写しを作って重ねて**います（`LINES_Y` / js/ch2b/data_y.js）。
そちらで `js/data.js` の `LINES` に直接足す場合は、**`LINES` の指紋（ff525748）が動きます。**
文案（第2章で使っているもの・そのまま使っていただいて構いません）：

```js
furoDenki: ['うおっ、ビリビリくる…！', '腰にはこれなんだよ', 'この痺れがたまらん', '効くわ〜、これ'],
```

### ★15の ★2 は、こちらの都合で覆しました（お知らせ）

8/7 に「`CONF_Y.furoPrefMax: null`（第2章は頭打ちなし）を確認しました」と返しましたが、
**作者指示「第1章の変更点は基本的に第2章にも反映させたい」（8/8）により、
第2章も頭打ちを入れました。** 数字はそちらとは違います。

| | 第1章 | 第2章 |
|---|---|---|
| `furoPrefMax` | 43（いちばん熱い湯＝`bath1` 43℃） | **45**（いちばん熱い湯＝新設の `y_furo_gekiatsu` 45℃） |

null のままだと、好み46℃の強面の客が**何を建てても永久に「ぬるいな…」と言う客**でした
（＝そちらが `TYPES.yakuza` を 47→43 にしたのとまったく同じ理屈）。
考え方は「**その章で出せるいちばん熱い湯に合わせる**」で揃えてあります。
第2章の湯温は 38／42／45 の三段に作り直しました（第1章の設備・数値には一切触れていません）。

### 第1章に入りました（2026-08-08・第1章セッションから返信）

`js/data.js` に4点。**共有ツリーなので第2章側にもそのまま効いています。**

| | 入ったもの |
|---|---|
| `bath_tansan` | `noTemp: true, line: 'furoTansan'` |
| `bath_denki` | `noTemp: true, line: 'furoDenki'`（第1章の電気風呂は41℃） |
| `LINES` | `furoDenki` を追加 |
| `CONF` | `furoFitMul: 1.5` |

第1章の実測（向こうの検算）＝あつ湯＋電気＋炭酸泉が **57%文句 / 満足 −0.5 → 0% / 5.4**。
終盤フル8槽 5.2。田所の「43℃以上」もあつ湯1台で達成できることを再確認済み。

**これを受けて第2章側の2行を片付けました（死んだ線は戻す）**
- `LINES_Y`（`furoDenki` を重ねていた写し）を**削除**。第1章の `LINES` が持つようになったので用済み。
  `registerChapter2` は `LINES` をそのまま渡す形に戻しました
- `CONF_Y.furoFitMul: 1.5` は**明示のまま残す**（`...CONF` でも 1.5 が来るが、
  この章の値はこの章で決める）。コメントの「第1章はこの札を持たない」は事実と違うので直しました

### ~~⚠ いま `guardAll()` は `ok:false` を返します~~ → **解消済み（2026-08-08）**

> 一時的に `bad` が出ていた期間の記録です。**このゆるい見方はもう使いません。**
> 当時の `bad` は `CONF` / `EQ` / `LINES` の3件（その後 `EQ_NOTE` と `TYPES` も動いて5件）で、
> すべて第1章セッションの意図した変更でした。

**2026-08-08、第1章セッションが5項目すべての指紋を貼り直しました。**

| | 前 | 後 | 何を変えたか |
|---|---|---|---|
| `CONF` | 66823bf0 | **9a3a4471** | `furoFitMul: 1.5` 新設／`furoPrefMax` 43→45 |
| `EQ` | 3db69614 | **34aa1b8a** | 電気風呂・炭酸泉に `noTemp`+`line`／`bath_atsu2` 新設 |
| `EQ_NOTE` | acb2fa91 | **ffb95b31** | 超あつ湯の一行説明 |
| `LINES` | ff525748 | **bf54d679** | `furoDenki` 追加 |
| `TYPES` | 1cfdcd71 | **d3bc602f** | 親分の好み 43→45 |

**いまの見方＝`await guardAll()` が `[{ok:true, chapter:1}, null]` を返すこと。
`bad` が1件でも出たら事故。** 基準が全部そろったので、例外扱いの項目はもうありません。
（第2章の枠が `null` を返すのは `forSaveKey` が合わないためで、正常です）

### 超あつ湯が第1章にも入り、両章が揃いました（2026-08-08）

第1章セッションが `bath_atsu2`（45℃・★3・2×1・2人・¥380,000・評判20）を新設。
あわせて **`js/game.js` の「熱すぎ」判定を相対に変えました**（第1章の作業・章ゲートなし＝第2章にも効く）。

```js
c.tempReact = (temp - pref >= 4) ? 'hot' : …   // 旧：temp >= 44 && pref < 43
```

決め打ちのままだと、**好み42〜43の客が45℃に入ったとき、満足しているのに（差3＝満足度+2）
文句を言う**。満足度がマイナスに転じる差4以上に揃えたもの。「ぬるい」側は従来どおり `pref - temp >= 3`。
第2章で測り直しても階段は変わりませんでした（3槽 4.1 ／ 4槽 5.0 ／ 6槽 5.7・文句0%）。

**第2章の超あつ湯も ★2 → ★3 にしました**（第1章の指摘が第2章でも再現したため）。
★2だと `(q||1)*2` の質点が `near()×1.5` を上回られて、★3の電気風呂(43℃)へ客が流れ、
**この湯が選ばれるのは全体の5%**でした。★3で **10%**＝商店街の旦那(44)＋強面(45)の
合計(15%)に見合う数字になります。

| | 第1章 | 第2章 |
|---|---|---|
| 超あつ湯 | `bath_atsu2` 45℃ ★3（評判20） | `y_furo_gekiatsu` 45℃ ★3（評判15） |
| `furoPrefMax` | 45 | 45 |
| `furoFitMul` | 1.5 | 1.5 |
| いちばん熱い湯を好む客 | 親分 `furoPref: 45`（43から戻した） | 強面 `furoPref: 46`（**頭打ち45で効く**） |

**指紋は第1章セッションが貼り直し済み。`await guardAll()` → `[{ok:true, chapter:1}, null]` に復帰。**

> ※このとき `ch-baselines.js` を書いている最中に読んで `node --check` が落ちたが、
> 書き終わったら通った。**両セッションが同時に同じファイルを触る**ので、
> 落ちてもすぐ壊れたと判断せず、一拍おいて読み直すこと。

### ★両章共通の申し送り — 「置いても選ばれない設備」の見かた（2026-08-08 合意）

**新しい設備が死に設備になっていたら、まず `(q||1)*2` と `near()×furoFitMul` の綱引きを見る。**

`equipFit`（js/game.js:1787）で客が設備を選ぶとき、点数は

```
(★の数)×2  ＋  ゆらぎ(±0.8)  ＋  好みとの近さ(0〜10) × furoFitMul
```

★が1つ違うと **2点**。湯温が1℃ずれると **1.5点**（第2章 / 第1章とも `furoFitMul: 1.5`）。
つまり **★1つ差 ≒ 湯温1.3℃差**。★の高い設備は、温度が1〜2℃ずれていても客を奪えます。

実際に両章で同じ罠を踏みました。

| | 死に設備になっていたもの | 奪っていたもの | 直し |
|---|---|---|---|
| 第1章 | `bath_atsu2` 超あつ湯45℃ ★2 | 檜風呂 43℃ **★4** | ★3へ |
| 第2章 | `y_furo_gekiatsu` 超あつ湯45℃ ★2 | 電気風呂 43℃ **★3** | ★3へ（選ばれる率 5% → 10%） |

**確かめ方**：その設備を狙っている客層の重み合計（第2章の超あつ湯なら
商店街の旦那18＋強面9＝27／全体183＝**15%**）と、実際に選ばれる割合を比べる。
大きく下回っていたら★が足りていない。

### 強面46 と 親分45 は、揃えないことで合意（2026-08-08）

挙動は同じ（第2章は `furoPrefMax: 45` の頭打ちで45になる）だが、**意味が違う**ので両方残す。

- 第2章 `TYPES_Y.irezumi` = **46**「建てられるどの湯より熱いのが好き」
- 第1章 `TYPES.yakuza` = **45**「いちばん熱い湯がちょうどいい」

どちらかの章に46℃以上の湯を足したとき、**46の客はそちらへ移り、45の客は動かない。**
揃えるとこの違いが消える。

### 同時編集の作法（2026-08-08 合意）

- **共有ファイルに書く前に一声かける**（両セッションとも）
- 相手の書きかけを読んで `node --check` が落ちても、**即「壊れた」と判断せず一拍おいて読み直す**

---

## 17. `js/game.js` — 設備の解放に、章の受け口を1つ（2026-08-08・**第1章は不変**）

**なぜ**：第2章の解放が機能していませんでした。実測で、**2階建て・🍜飯0点・🛋くつろぎ0点・
総合408/800（番付最下位・福来湯486にも負け）の店が、ゲートのある44品のうち41品を開けきっていた。**
評判の10項目は男湯だけで満点近くが取れる（💧水風呂94・🌤ととのい91・🧼清潔81）ので、
ビルの2/7しか建っていなくても評判48に届きます。プレミアムカプセル（評判60）まで並んでいました。

作者決定で、**第2章は8部門スコア（各100点）で部門ごとに開く**形にしました。

```js
/* 章が `unlockInfo` を持てば、そちらが解放条件のすべてを決める。
   返すのは { ok, label }、ゲートの無い品には null */
function unlockOf(id) {
  const d = EQ[id]; if (!d) return null;
  if (hasHook('unlockInfo')) return chHook('unlockInfo', id, d) || null;
  return d.rep ? { ok: G.rep >= d.rep, label: '評判' + d.rep } : null;   // ← 第1章はこの行だけ
}
function unlockOk(id) { const u = unlockOf(id); return !u || u.ok; }
function nextUnlockEq()    { hasHook('nextUnlock') ? … : 従来の「評判のいちばん近い1品」 }
function nextUnlockText(d) { return chHook('nextUnlockText', d) || `評判${d.rep}で【${d.name}】`; }
```

**差し替えた呼び出しは7か所**：
`demandBuyable`／`renderShop` のロック判定・🔒チップ・タップ時のトースト／`isNewItem`／
準備画面の「次の解放」ヒント2か所（自由営業と通常）／**`repAdvice`**。

> **7か所目は、第1章セッションの指摘で見つかりました（2026-08-08）。**
> `grep` を `def.rep > G.rep` の形だけで打っていたため、`repAdvice` の
> `(d.rep || 0) <= G.rep` という **`<=` の変種**を取りこぼしていました。
> 【評判】タブの「◯◯に買い替えると伸びる」という助言を出す所で、第2章では
> **まだ買えない設備に買い替えろと言い続ける**状態でした。
> **第1章では旧も新も同じ比較に落ちるので、見た目がまったく同じ＝絶対に気づけない割れ方です。**
> 教訓：ゲートを移すときは `grep -n "\.rep\b"` で**全件**洗い、`>=` `<` `<=` の変種と
> `EQ[id].rep` 経由を1つずつ潰すこと。

**直さず残した所**：`shopIds` の並び順（js/game.js:9796-9803）は `EQ[id].rep` を見ていますが、
これは**ゲートではなく並び順**です。第2章も `rep` を「段の番号」として残している
（15→🔥サウナ30、20→🔥サウナ38…と読み替える）ので、**並び順と解放順は一致したまま**＝挙動は割れません。

**第1章への影響＝ゼロ。実測しました。**

| 評判 | 解放 | 🔒の文言 | 次の解放のヒント |
|---|---|---|---|
| 10 | 0/22 | 🔒評判20 | 評判20で【超あつ湯の浴槽】 |
| 30 | 1/22 | 🔒評判45 | 評判35で【立ちシャワー】 |
| 45 | 11/22 | 🔒評判65 | 評判50で【大型サウナ】 |
| 65 | 22/22 | — | — |

`hasHook('unlockInfo')` は第1章で `false`＝どの経路も従来の1行に落ちます。
`await guardAll()` → `[{ok:true, chapter:1}, null]`。

### 第2章側（`js/ch2b/battle_y.js`・共有ファイルではありません）

設備の cat で見る部門が決まります。`sauna→🔥サウナ` `mizu→💧水風呂` `rest→🌤ととのい`
`chubo/shoku→🍜飯` `capsule→🛋くつろぎ` `locker/wash/datsui/amenity→🧼清潔` `etc→✨個性`。
**湯船とフロントは部門になっていないので `total`（8部門の合計・800点満点）**
＝とくに靴箱は入館できる人数そのものなので、1部門の出来で閉じてはいけない器として総合に置きました。

直後の実測（同じ31日目の店）：**41/44 → 33/47**。閉じたのは狙いどおりの品です。

```
プレミアムカプセル 🔒🛋くつろぎ95（いま0）      ← 作者の指摘そのもの
生ビールサーバー   🔒🍜飯38（いま0）           ← 食堂を建てていない
ケロ材セルフロウリュ室 🔒🔥サウナ85（いま64）
大きな靴箱         🔒総合スコア570（いま401）
次の解放 → ✨個性30で【充電ステーション】（いま25）
```

---

## 18. `js/game.js` — 上のバーの評判表示に、章の受け口を1つ（2026-08-08・**第1章は不変**）

```js
// 旧
$('uiRep').textContent = repCounting() ? '評判 集計中' : `評判 ${G.rep}`;
// 新
$('uiRep').textContent = chHook('topRep') || (repCounting() ? '評判 集計中' : `評判 ${G.rep}`);
```

**なぜ**：第2章は「評判 0-100」と「番付スコア 0-800」の2つの数字が並んでいて、
しかも**設備の解放を800点側に移した**ので、上のバーに0-100が出ていると
何を見て設備が開くのか分からない状態でした。第2章は800点に一本化します。

**実測**

| | 第1章 | 第2章 |
|---|---|---|
| 集計中のあいだ | `評判 集計中` | `スコア 集計中` |
| それ以降 | `評判 48` | `スコア 116/800` |

第1章の `hasHook('topRep')` は `false`＝`||` の右側がそのまま出ます。
`await guardAll()` → `[{ok:true, chapter:1}, null]`。

### 第2章側の部門の作り替え（`js/ch2b/` のみ・共有ファイルではありません）

- **💧水風呂 → 💧風呂・水風呂**。湯船も点に入れる（水風呂65点ぶん＋湯船35点ぶん）。
  湯船を独立部門にすると9部門になってライバルの持ち点と噛み合わないので、
  同じ「水まわり」に束ねた。あわせて湯船の解放も `total` から `mizu` へ
- **🌤ととのいと🛋くつろぎを、置いた階で分けた。** 以前は両方が `yEquips('rest')` で
  全階をまとめて数えていたので、**浴室のととのいイスでくつろぎが上がり、
  ラウンジのマンガ棚でととのいが上がって**いた。
  いまは 浴室＋屋上→🌤ととのい／ラウンジ＋カプセル→🛋くつろぎ
- 解放のほうは置く階が決まらないので、**品物ごとに `dept: 'kutsu'`** を付けた（ラウンジの10品）
- データ欄の「🕒 時間帯ごとの客」は非表示（作者指定）。
  計算そのもの（`yHourTable`）は集客の心臓なので残してある

### `topRep` は絶対に空文字を返さない（第1章セッションの指摘 2026-08-08）

`chHook('topRep') || 従来の文字列` という受け方なので、**`''` は falsy で `||` の右をすり抜け、
第2章の上のバーに0-100の評判が復活する**＝いちばん紛らわしい形になります。
`yTopRep()` を try/catch で包み、どの経路でも非空を返すようにしました（最後の砦は `'スコア —'`）。

実測（第2章・8日目以降・`G.ch2` も `G.equip` も壊した状態）→ `"スコア —"`。例外も出ません。

### ついでに見つけた自作のバグ ―― 採点のキャッシュが古い点を返していた

解放判定が1画面で何十回も走るので、8部門の採点に
`(日 × 設備の数 × 階数)` を鍵にしたキャッシュを入れていました。**これが間違いです。**
**設備の数が変わらない変化を全部取りこぼします**＝修理して `cond` が戻っても、
湯温を変えても、深夜営業を伸ばしても、メニューを開発しても、解放が古いまま。

見つかった経緯も残しておきます。上の「空文字」検証で `G.equip` を `undefined` にしたのに
**同じ点を返し続けた**ので気づきました。壊れているのに正常に見えるのがキャッシュの怖さです。
**キャッシュは外しました。** 採点は `G.equip` を数回なめるだけで、
カタログ1画面ぶんでも数万回の演算にしかなりません。

### 第2章のスコアは初日から出す（作者指定 2026-08-08）

8部門は10項目の評判と違って**日ごとの平均をとっていない**＝いま建っている物からその場で出しています。
だから「集計中（7日間）」は要りません。実測：

```
1日目               スコア 116/800
サウナを1台置いた直後 スコア 139/800     ← 置いた瞬間に動く
水風呂も置いた直後    スコア 161/800
```

第1章は `評判 集計中` → `評判 N` のまま（7日ならしなので `repCounting()` が要る。あれとは別物）。

### ラウンジの基礎点 35 → 45（作者指定 2026-08-08・手1）

🛋くつろぎの品の解放が30〜38点から始まるので、35のままだと
**ラウンジを建てたのに何も置けない一瞬**ができていました。実測：

```
建てる前  くつろぎ 0
建てた直後 くつろぎ 45 → 買えないのは上位4品だけ
          （仮眠リクライナー・室内ハンモック・ワークスペース・よもぎ蒸しの個室）
```

---

## 19. `js/game.js` — 主人公の持ち場を章が差し替えられるように（2026-08-08・**第1章は不変**）

```js
/* 章が `playerArea` を持てばそちら（第2章＝バイト管理画面で動かせる）。
   持たない章＝これまでどおり CONF.playerArea（第1章は区画が1つなので常に 0） */
function playerArea() {
  const h = chHook('playerArea');
  return h == null ? (CONF.playerArea ?? 0) : (h | 0);
}
```

**なぜ**：作者指定で、**主人公もバイトと同じくバイト管理画面で階に置ける**ようにしました。
第2章は7階建てなので「主人公は常に1F受付」だと、上の階が手薄なときに打つ手が1つ足りません。
清潔度の採点は「バイトの数 ÷ 客数」なので、主人公を動かしてもバイトを雇う動機は薄れない、というのが作者の判断です。

**差し替えたのは5か所**（`CONF.playerArea ?? 0` の直書き）。前回の教訓に従い、
先に `grep -rn "playerArea" js/` で全件洗ってから着手しました
（ヒットは game.js の5か所＋各章のデータ定義のみ。`?? 0` 以外の書き方や
`CONF.areas[CONF.playerArea]` のような経由は無し）。

**あわせて `areaExtraWorker` の返り値を「人数」に**しました（`? 1 : 0` → `| 0`）。
第2章はその階に**主人公と妻の2人**が立ちうるので、真偽値だと枠の数が合いません。
フックを持たない章は `undefined | 0 = 0` ＝これまでと同じです。

**実測（第2章）**

```
はじめ    🏮 受付＆ショップ: あなた・奈津 〔2／2人〕／♨ 男湯: 〔🚫 利用不可〕
【＋】→   一覧に「🧑 あなた（店主）いまは 🏮 受付＆ショップ」が並ぶ
動かすと  🏮 受付＆ショップ: 奈津 〔1／2人〕／♨ 男湯: あなた 〔1／2人〕
```

**第1章**：`hasHook('playerArea')` は `false`、`playerArea()` は `0`。
`await guardAll()` → `[{ok:true, chapter:1}, null]`。

### ⚠ 前回の報告は誤りでした ―― 主人公の配置は効いていなかった（2026-08-08）

第1章セッションの指摘（「`playerArea` という名前を使わない**区画0の直書き**が残っていないか」）で
洗い直したところ、**主人公の生成が2か所とも `bandai()` のある区画に固定**されていました。

```js
const b = bandai(), deskF = b ? (b.f | 0) : 0, back = G.actF;   // enterPrep（6122）
const b = bandai(), deskF = b ? (b.f | 0) : playerArea(), …      // startDay（4028・片方だけ直していた）
```

つまり `playerArea()` を作っても、**主人公は毎朝かならず番台の階に湧き直す**＝
バイト管理画面で男湯へ動かしても翌朝には受付に戻る＝**配置そのものが効いていませんでした。**

```
配置した直後   持ち場 1（男湯） ／ 体 1
営業開始のあと 持ち場 1        ／ 体 0   ← 番台の階に戻っていた
```

両方を `playerArea()` に直しました。**実測**

| | 営業開始のあと | 準備画面のあと |
|---|---|---|
| 男湯へ配置 | 1（男湯） | 1（男湯） |
| 既定のまま | 0（受付・従来どおり） | 0 |
| 第1章 | 0（`areaCount` 1） | — |

### ★ 教訓（2つ目）

**「フックを足した」で終わらせず、「フックが実際に効いている」まで測る。**

前回の報告で「入りました」と書いたのは、**UIの見た目とフックの返り値しか見ていなかった**からです。
`startDay()` を回して `G.player.f` を読むところまでやれば、その場で気づけました。

あわせて1つ目の教訓（ゲートを移すときは `grep` で全件洗う）に補足：
**`playerArea` のような「名前」で grep しても、`: 0` のような直書きは引っかかりません。**
区画・階の前提を動かすときは、`\.f === 0` / `area === 0` / `: 0` のような
**値の直書き**も併せて洗うこと。第1章は区画が1つなので、そこが残っていても永久に同値です。

---

## 20. 番台の立ち位置（`deskLayout` フック・2026-08-08）

**第2章の受付カウンターは3マス幅**（(6,1)〜(8,1)。盤面を広げたあとは (8,1)〜(10,1)）で、
寄れるマスは 左端／正面3つ／右端 の5つ。共有側の決め方は

```js
bandaiFront() … 右側優先 → 右端
playerSpot()  … 上側優先（front を除く） → 左端
```

なので、**客が右端・主人公が左端＝カウンターの端と端で金を渡していた**（4マス離れている）。
第1章の番台は2マスなので、この決め方で自然に向かい合う＝**第1章では起きない**。

`js/game.js` に**フックを1つ**足し、4か所がそれを見るようにしました。
フックが無い章は1行も挙動が変わりません。

```js
function deskLayout() { return chHook('deskLayout') || null; }
bandaiFront()    → L.pay
playerSpot()     → L.staff
deskHelperSpot() → L.staff2
queueSpots()     → L.queue
```

第2章（`yDeskLayout`）の割り当て：**主人公＝左端／客＝正面の左端（主人公の真ん前）／
2人目＝正面の2番目（客の隣）／行列＝残りの正面 → 右端 → ロビーの床**。

> ⚠ `yDeskLayout` の中で `outsideSpots()` を呼んではいけない
> （`outsideSpots` → `bandaiFront` → `deskLayout` → …で無限に回る）。行列の尾は自前で伸ばしています。

**実測**（2階建て・1日）＝会計13件すべてで、客は `queueSpots()[0]`＝主人公の真ん前に立っていました。

## 21. 帰り道（`walkToExit` フック）

共有側の `walkToExit` は「その区画の入口まで歩いて、**外の壁沿い (0, H-1) へ**消える」。
区画が階になっている第2章では、2階の「入口」＝エレベーターの扉なので、
**扉まで行ったあと、その階の左下の壁へ歩いて消えて**いました（作者指摘）。

```js
function walkToExit(c) {
  if (chHook('walkToExit', c)) return;   // 章が自前で帰した
  …従来どおり
}
```

第2章は上の階の客をエレベーターで1階へ降ろし、降りてから玄関へ歩かせます。
**実測**＝退店31人中31人が1階の玄関から出るようになりました（従来は2階の壁へ消えていた）。
閉店時刻に館内に残っている客は、これまでどおりその場で片づきます。

## 22. バイト管理画面の描き先（求人広告しか出ない不具合）

`renderStaffMgr()` は **【運営】タブの中と、独立したモーダルの両方から呼ばれる**のに、
いつも `paneIfShown('staffMgrPane')`（＝運営タブ側）を先に見ていました。
そのため**運営メニューをバイトのタブで開いたまま**案内図の警告を叩くと、
中身が運営タブ側へ描かれ、独立したモーダルは空のまま開く＝
**題名と「📰 求人広告を出す」しか出ない。** これが「バイトを押すと求人広告が出る」の正体です。

- 自分のモーダルが出ているときは、必ず自分の中に描く
- `openStaffMgr()` は**先に `hidden` を外してから**描く

第1章は `CONF.staffRooms` を持たないのでこの画面自体に来ません。

## 23. 面接のあとバイト管理画面へ

`btnJobClose` で、採った人がいて `CONF.staffRooms` を持つ章なら `openStaffMgr()` へ。
第1章は持ち場の画面が無いので、これまでどおり閉じて終わりです。

## 24. 空の色（`skyPhase` の時間帯）

作者指定で 15〜17時＝昼間／17〜19時＝夕方／19〜翌6時＝夜／翌6時〜＝昼間 に付け替えました。
`skyPhase()` は `CONF.dayNight` を持つ章しか通らない＝**第1章は1度も走りません。**

> ⚠ **`CONF.dayNight` は、いま読み込まれているどの章も立てていません**＝
> `drawSky`／`drawLamps` は第1章でも第2章でも一度も走っていません。
> 第2章に屋外区画（`park`）も無いので、実際に空が見えるのは**館内案内図だけ**。
> 時間帯の色はそちら（`ySkyNow`／art_y.js）に持たせています。
>
> ### ⚠ `dayNight` を「どこにも無い＝バグ」と判断して足さないこと
>
> **`js/ch2/data2.js:707` に `CONF2.dayNight = true` があります。**
> これは棚上げ中の旧第2章（旧候補1「独立開業編」）のデータで、
> `index.html:424-431` でコメントアウトされているため今は読み込まれていません。
> **死んだコードではなく、旧第2章のために置いてあるもの**です。
>
> `drawSky` / `drawLamps` は**共有側**にあるので、フラグさえ立てば章を問わず通ります。
> `CONF`（第1章）や `CONF_Y`（第2章）に `dayNight: true` を足すと、
> **第1章で屋外用の暗転が走り出します**。第1章は屋外が無いのが仕様なので、
> ここは**立てないこと**（js/game.js:7513 のコメントが正）。
> ── 第1章セッションからの申し送り（2026-08-08）

## 25. 盤面を 16×10 → 20×14 へ（第2章のみ）

`AREAS_Y` の `W/H` と `divideY`、エレベーターの位置・大きさを変えただけで、
共有コードには一切触れていません（`CONF.W/H` は `applyArea` が区画から読む作り）。

| | 前 | 後 |
|---|---|---|
| 盤面 | 16×10 | **20×14** |
| 内側の使えるマス | 14×8＝112 | **18×12＝216** |
| 脱衣所（divideY） | 7（2段） | **9（4段）** |
| エレベーター | 右下 2×2 | **右下 2×2 のまま**（一度1×1にしたが、盤面が広がったので戻した） |
| 画面の縦横比 | 1.60 | **1.43**（縦に伸びる） |

`yPlaceBlock` の当たり判定に `+2` と直書きしてあったので、`EQ['y_elev']` の w/h から取るよう直しました。

**通し確認**＝7階建て・5日・エラー0・行き止まりの設備0・追い返し0。`await guardAll()` → `[{ok:true, chapter:1}, null]`。

## 26. 階ごとの入場制限（第2章のみ）

`yEntryFull` に「その階の脱衣ロッカーが満杯なら止める」を追加（作者指定）。
男湯だけ満杯の日でも女性客は通ります。館内案内図には
**靴箱が満杯＝ビルの上に大きく／浴室階のロッカーが満杯＝その階の帯に**「⚠️ 入場制限中」を出します。

## 27. 館内案内図の「未建設」の階（2026-08-08）

線 `.30`／字 `.42` で**空に溶けていた**（作者指摘）。夜でも読みづらく、
§24 で昼の空（明るい水色）を入れてからは、ほぼ見えなくなっていた。

**濃くするだけでは足りない。** 空の色は時刻で動くので、線と字だけを強めると
昼と夜のどちらかで必ず埋もれる。**先に暗い下地（`rgba(16,14,12,.38)`）を敷いて
空から切り離し**、その上に点線（`.62`）と字（`.92`＋影）を置いた＝どの時刻でも同じ読みやすさになる。
下地の濃さは、建っている階の名札（`rgba(12,10,8,.8)`）に合わせてある。

あわせて **「6F 🛏 カプセル（未建設）」のように階の数字を出す**ようにした
（建っている階は左に「2F 男湯」の名札が出るのに、未建設側だけ階が分からなかった）。

## 28. 盤面を変えた日のセーブの引っ越し（`migrateEquip` フック・2026-08-08）

**§25 で盤面を 16×10 → 20×14、仕切りを 7 → 9 に変えたのに、セーブの座標を移し替えていませんでした。**
作者の遊んでいたセーブは開業直後で、中身はこうなっていました。

```
y_elev(13,7)   ← 16×10 の右下。20×14 では**部屋のど真ん中**
bandai(6,1)
y_shoe(5,8)    ← 旧・最下段（入口のそば）。いまは部屋の中程
```

作者の指摘「EVは一番右下に置くこと。部屋の中途半端な位置にあるなんて変だよ」の正体はこれです。
**新規で始めれば正しく、続きから読むと壊れている**ので、こちらの通し確認（毎回 `guardBoot` で
新規に作っていた）では一度も踏みませんでした。

`js/game.js` の `loadGame()` に、設備を読んだあと・`fixEquipOverlap()` の前でフックを1つ。

```js
chHook('migrateEquip');
```

第2章（`yMigrateEquip`）の決まり：

| 対象 | どうするか |
|---|---|
| `y_elev` / `bandai` | 建物の一部なので、新しい定位置へ据え直す |
| 浴室階・旧脱衣所（旧 y≧7） | 2段下げて新しい脱衣所（y 9〜12）へ。下げないと浴室の中に立つ |
| 仕切りの無い階・旧下段（旧 y≧6） | 4段下げて下の壁ぎわのまま |
| それ以外 | 動かさない |

`G.ch2.boardV` で版を持ち、一度移したら二度と走りません。新規開始は最初から今の版です。

**実測**（作者のセーブそのもので「つづきから」）

```
bandai(6,1)     → (8,1)
y_shoe(5,8)     → (5,12)    入口のそば
y_elev(13,7)    → (17,11)   右下の角
y_locker12(2,7) → (2,9)     新しい脱衣所
y_sink(9,8)     → (9,10)    新しい脱衣所
y_sauna1(2,2)   → 動かない
```

画面の色を直接読んで、EVが角に接していることも確認：

```
y=11 : 17,18=EV(#5b6672)  19=右壁
y=12 : 17,18=EV(#4b545d)  19=右壁
y=13 : 17,18=下壁
```

移行後のセーブで1日通して 会計28件／追い返し0／退店は全員1階／行き止まりの設備0。

### ★ 教訓（3つ目）

**間取り・盤面・IDなど「セーブに入る前提」を変えたら、新規だけでなく“続きから”も必ず通すこと。**
`guardBoot` は毎回まっさらな店を作るので、**セーブの移行漏れは原理的に検出できません。**
`loadGame()` を通した確認を1回入れていれば、その場で分かりました。

## 29. 垢すり＝「人を1時間売る商売」に作り直した（2026-08-08）

作者指定。**設備ではなく人を売る**形に変えた。

| | 前 | 後 |
|---|---|---|
| 垢すり台 | ¥900,000 | **¥100,000** |
| 単価 | ひとり¥3,000 | **ひとり60分¥6,000** |
| 施術できる人 | その階のバイト（誰でも） | **【🧖 垢すり職人】を持っている人だけ** |
| 職人になれるのは | — | **主人公・妻・バイト**の3種 |
| 職人の入手 | — | **【🎓 講習を受ける】でたまに授かる**（45%／まだ誰も持っていなければ必ず） |
| 施術中 | 何も起きない | **その60分、その人は他に何もできない** |

`js/game.js` の変更は**1行だけ**（`updatePlayer` の頭）。

```js
if (chHook('playerBusy', p, dt)) return;
```

バイトと妻は既存の `staffJob` フック（食堂が使うのをやめて空いていた）で止める。
主人公・妻・バイトの3種を1つの列に並べて「手が空いている職人」を選ぶのは `yAkaProsOn` / `yFreeAkaPro`。
押さえるのは `useDur`（客が台に乗った瞬間）で、そこで台のそばまで歩かせる。

**実測**（2階建て・男湯に垢すり台1つ・7時間営業）

| 職人 | 垢すり | 売上 | 施術に使った分 | 施術中に別の仕事 | その日の会計 |
|---|---|---|---|---|---|
| いない | 0人 | ¥0 | 0分 | — | 26人 |
| 主人公 | 2人 | ¥12,000 | 130分 | 0 | 31人 |
| 妻 | 5人 | ¥30,000 | 334分 | **0**（番台にも掃除にも出ていない） | 28人 |
| バイト | 5人 | ¥30,000 | 311分 | **0** | 31人 |

妻を職人にすると420分中334分を台に取られ、**番台が手薄になって会計が28人に落ちる**＝
「¥30,000は取れるが、その1時間ぶん店は止まる」が数字に出ている。狙いどおり。

### ⚠ ついでに見つけた既存バグ：講習が一度も動いていなかった

選択肢の id は `kousyu`（OFFDAY_Y）なのに、処理側は `a.id === 'koushu'` を見ていた＝
**★の底上げも【🔥 サウナ専門家】も、講習に通っても何も起きていなかった。**
id はセーブ（`yPlan().act`）にも入るので、直したのは**綴りではなく処理側**（両方受ける）。

## 30. ととのいイスを5種足した（2026-08-08・第2章のみ）

ととのいは3種しか無く、**¥50,000 の定番と ¥190,000 の無重力の間が丸ごと空いていた**＝
中盤に買うものが無い。役目が重ならないように5つ足した（共有コードには触っていない）。

| 名前 | 値段 | 大きさ | 質 | 席 | 解放 | 役目 |
|---|---|---|---|---|---|---|
| 木のベンチ | ¥40,000 | 2×1 | 1 | 2 | — | （既存）安い |
| ととのいイス | ¥50,000 | 1×1 | 2 | 1 | — | （既存）定番 |
| **大判ととのいマット** | ¥60,000 | 2×2 | 2 | **3** | ととのい30 | 安い×大人数 |
| **デッキチェア** | ¥70,000 | 2×1 | 3 | 1 | — | **屋上だけ** |
| **アディロンダックチェア** | ¥95,000 | 1×1 | 3 | 1 | ととのい38 | 中間の万能 |
| **リクライニングチェア** | ¥150,000 | **1×2** | 4 | 1 | ととのい46 | 快適だが場所を食う |
| インフィニティチェア | ¥190,000 | 1×1 | 4 | 1 | ととのい70 | （既存）高級 |
| **畳の寝ころび台** | ¥260,000 | 3×2 | 4 | 2 | ととのい55 | 畳＋仕切り板（写真から） |
| **フルフラットベッド** | ¥380,000 | 3×2 | 5 | 3 | ととのい78 | 木の台＋衝立＋枕（写真から） |

### ⚠ ハンモックと畳は足していない

候補には挙がっていたが、**どちらも4階ラウンジに既にある**（`y_x_hammock` ¥200,000 /
`y_x_tatami` ¥340,000）。同じ物を浴室にも並べると、**ラウンジを建てる理由がその場で消える。**

### デッキチェアの「屋上だけ」

`area: AY.ROOF` を持たせるだけで効く（`rules_y.js:545` がカタログを絞っている）。
**7階を建てて初めて買えるものが、これで1つできた。**

### 絵

第1章の rest の4枚（`bench1` / `chair1` / `chair2` / `chair_inf`）を借りる（`EQ_ART_ALIAS_Y`）。
ただし借り物は**1マスぶんしか描かない**ので、2×2 の2つ（大判マット・フルフラットベッド）は
`yEquipArt` に自前の絵を持たせた（マットは3人ぶんの区切り線、ベッドは枕付き）。

**実測**（男湯に5脚並べて1日）＝5つとも `placeCheck` を通り、5つとも客に使われた。
使われた分数はおおむね質の順（フラットベッド190分 → リクライニング105 → アディロンダック67
→ ととのいイス26 → マット18）。`await guardAll()` → `[{ok:true, chapter:1}, null]`。

### 追記（同日・作者から実物の写真をもらって描き直した）

**大判ととのいマット** … はじめは「区切り線を入れた大きな四角」1枚で描いていたが、
**寝室のベッドに見える**（作者指摘）。枠のある厚い一枚板は、どうしてもマットレスになる。
**床に敷いた3枚**として描き直した＝縁も影も付けず、間に床を見せ、頭側に丸めたタオルを置く。

**フルフラットベッド** … 写真は「木の台にベージュのマット、頭側に低い木の衝立、丸めた枕」。
2×2 で1人ぶんとしていたのを、写真どおり **3×2 で3人ぶん**に変え、専用の絵を描いた。

**畳の寝ころび台（新規）** … サウナ東京の「休憩」の写真から。
**石の台に畳2枚、間に濃い木の仕切り板**。裸のまま隣を気にせず寝ころべる。
⚠ 4階ラウンジの【畳の小上がり】とは別物（あちらは館内着で上がる座敷、こちらは浴室のととのい）。

**実測**＝5つとも `placeCheck` を通り、フルフラットを外すと畳（139分）・リクライニング（88分）
・アディロンダック（16分）へ客が流れた＝質の高い台から順に埋まる。
`await guardAll()` → `[{ok:true, chapter:1}, null]`。

## 31. 倒れた日から抜けられない（詰み）を直した（作者報告 2026-08-08）

> 営業開始が出ずに「今日は動けない」しか出なくなった

**一度倒れると、二度と店を開けられなくなっていた。**

```
yOffDay()      … 倒れた日は必ず画面を開いて true を返す
                 → btnOpen の処理がそこで止まる＝店は開かない
倒れた日の「寝る」… yShowResult() へ流していた
                 → 【🏮 出勤する】が出る
                 → 押しても店は開かず、**G.day が進まない**
                 → 翌朝も downDay === G.day のまま＝永久ループ
```

倒れた日は店を開けないのだから、**読み終えたらそのまま明日の朝へ送る**のが正しい。
`yShowDownResult()` を足し、締めのボタンを【🌅 明日の朝へ】＝`yEndOffDay()`（`G.day++`）にした。

> ⚠ **この詰みを踏めるようにしたのは、この日の自分の変更です。**
> 8/8 に `yCollapse()` を `closeDay` へ移すまで**倒れる処理は一度も発火していなかった**ので、
> 行き止まり自体は前からあったが、誰も到達できなかった。
> 「効いていなかったものを効くようにする」変更は、**その先の道が繋がっているかまで見る**こと。

**実測**（本物の経路＝体力を尽きさせて閉店）

```
day1 営業 stam=0 downDay=2
day2 倒れた日 → 【🌅 明日の朝へ】
day3 営業 stam=0 downDay=4
day4 倒れた日 → 【🌅 明日の朝へ】
day5 営業 stam=0 downDay=6
day6 倒れた日 → 【🌅 明日の朝へ】
day7 … closed=false ／ ボタン＝「🏮 営業開始」
```

## 32. 吹き出しが読めなくなった（`bubScale`・作者報告 2026-08-08）

**§25 で盤面を広げた副作用。** キャンバスは横幅いっぱいに縮めて画面に出るので、
盤面を広げると**画面の中の字だけが小さくなる。**

| | 盤面 | キャンバス幅 | 縮尺 | 9px の実寸 |
|---|---|---|---|---|
| 前 | 16×10 | 1024 | 0.496 | **4.46px** |
| 後（不具合） | 20×14 | 1280 | 0.397 | **3.57px** ← 読めない |
| 直した後 | 20×14 | 1280 | 0.397 | **4.46px**（×1.25） |

`drawBubble` と金額の飛び出しの寸法を `CONF.bubScale` 倍にした。
第2章だけ `bubScale: 1.25`（＝1280/1024）。**第1章はこのキーを持たないので 1 倍**＝
実際に `drawBubble` を呼んで確かめたところ、使われるフォントは `9px` / `bold 9px` のまま
（掛け算はすべて ×1 で元の数値に戻る）。`await guardAll()` → `[{ok:true, chapter:1}, null]`。

> ⚠ **盤面（`AREAS_Y` の W）をまた変えたら、`bubScale` も一緒に見直すこと。**
> 盤面の広さと字の大きさは連動している。

### 追記2（同日・アディロンダックとリクライニングも写真から描き直した）

第1章の `chair2`（青い四角い椅子）を借りていたが、**どちらも借り物では特徴が消える。**

- **アディロンダックチェア** … 樹脂の椅子。**扇形に広がった背もたれの縦スラット**と
  **幅広の肘掛け**、この2つが無いとただの椅子になる。色は赤
  （ととのいイス＝白／インフィニティ＝青と、ひと目で見分けるため）
- **リクライニングチェア** … 白いS字ラウンジャー。**肘掛けは無い。**
  頭側がやや狭く足側が広い一枚板に、全面に細かい横スラット

これで **ととのい7種がすべて自前か専用の絵**になり、並べたときに一目で見分けられる。
`await guardAll()` → `[{ok:true, chapter:1}, null]`。

### 追記3（同日・ととのいの品揃えを作者と詰めた結果）

写真をもらいながら1つずつ詰めて、最終的にこうなった。

| 名前 | 値段 | 大きさ | 質 | 席 | 解放 | 備考 |
|---|---|---|---|---|---|---|
| 木のベンチ | ¥40,000 | 2×1 | 1 | 2 | — | 既存 |
| ととのいイス | ¥50,000 | 1×1 | 2 | 1 | — | 既存 |
| デッキチェア | ¥70,000 | 2×1 | 3 | 1 | — | **屋上だけ** |
| アディロンダックチェア | ¥95,000 | 1×1 | 3 | 1 | 38 | 扇形の背＋幅広の肘掛け |
| リクライニングチェア | ¥150,000 | 1×2 | 4 | 1 | 46 | 白いS字ラウンジャー |
| インフィニティチェア | ¥190,000 | 1×1 | 4 | 1 | 55 | 黒枠＋ベージュのメッシュ |
| 畳の寝ころび台 | ¥260,000 | 2×2 | 4 | 2 | 63 | 石の台＋畳＋仕切り板 |
| 雲のごろ寝マット | ¥320,000 | 2×2 | 5 | 2 | 70 | 白いフォーム＋枕 |
| フルフラットベッド | ¥380,000 | 3×2 | 5 | 3 | 78 | 木の台＋衝立＋枕 |

**廃止**：大判ととのいマット（作者判断）。EQ・一行ヒント・専用の絵をすべて消した。
古いセーブに残っていても `loadGame` の `.filter(e => EQ[e.id])` が読み飛ばす。

**直したこと2つ（実装中に見つけた釣り合いの崩れ）**

1. 畳を 3×2 → **2×2**。3×2 だと1人あたり3マスで、フルフラット（2マス）より贅沢だった
2. **値段の順に解放されていなかった。** ¥19万のインフィニティが ¥26万の畳より
   *あとに* 開く並びになっていて、質も同じ4・席は畳のほうが多い＝
   **インフィニティを買う理由が消えていた。** 解放を値段の順に付け直した（上の表）。
   通しで確認 → 「値段順に解放されるか: はい ✓」

これで**ととのい9種すべてが自前か専用の絵**になり、並べても一目で見分けられる。
`await guardAll()` → `[{ok:true, chapter:1}, null]`。

### 追記4（同日・全部「1台＝ひとり用」に揃えた／作者決定）

畳・雲・フルフラットは、はじめ写真のとおり**2〜3台ぶんをひとかたまり**で持たせていたが、
それだと**並べ方をこちらが決めてしまう。** 1台ずつ買って、好きなだけ横に並べてもらう形にした。

| 名前 | 前 | 後 |
|---|---|---|
| 畳の寝ころび台 | 2×2・2席・¥260,000 | **1×2・1席・¥120,000** |
| 雲のごろ寝マット | 2×2・2席・¥320,000 | **1×2・1席・¥230,000** |
| フルフラットベッド | 3×2・3席・¥380,000 | **1×2・1席・¥280,000** |

絵も1台ぶんに描き直した。畳は**仕切り板を脇に寄せた**ので、横に並べると板が
一人ぶんずつの仕切りになる（写真と同じ絵になる）。実測＝畳3台・雲2台・フラット2台を
隣り合わせに置けることを `placeCheck` で確認。

**解放（作者指定「アディロンダック以上はスコア制限を入れる」）** — まっさらな店で確認：

```
木のベンチ            制限なし
ととのいイス          制限なし
デッキチェア          制限なし（¥7万＝アディロンダックより安い。屋上だけの品）
アディロンダックチェア 🔒 ととのい38
畳の寝ころび台        🔒 ととのい46
リクライニングチェア  🔒 ととのい55
インフィニティチェア  🔒 ととのい63
雲のごろ寝マット      🔒 ととのい70
フルフラットベッド    🔒 ととのい78
```

値段の順に開く（通し確認で「値段順に解放: はい ✓」）。
`await guardAll()` → `[{ok:true, chapter:1}, null]`。

## 33. フロア画面を縦に広げた（作者指定 2026-08-08）

> フロア画面の表示領域をもっと縦方向に広げられないかな？館内案内図ボタンを移動させて。

**まず前提の確認。** キャンバスは `width:100%` なので、**表示の高さは横幅と縦横比だけで決まる。**
下に空きを作っても勝手には伸びない＝縦を使うには**行を増やす**しかない（作者の言うとおり縦長になる）。

375px幅での配分（前）：

```
上のバー      45px
ゲージ        28px
ひとこと      34px
フロア画面   263px   ← 375 ÷ 20 × 14
案内図の帯    52px   ← ここを浮かせた
下のパネル   391px（スクロールする）
```

- **帯をキャンバスの上に重ねた**（`index.html` で `#areaBar` を `#canvasWrap` の中へ移し、
  CSS で `position:absolute` の小さな錠剤にした）。文言も「← 館内案内図」→「← 案内図」に詰めた
- 浮いた 52px を使って **14行 → 17行**（仕切りも 9 → 11）

**実測**

| | 前 | 後 |
|---|---|---|
| フロア画面の高さ | 263px | **319px（+21%）** |
| 帯 | 52px を占有 | **25px・重ねる**（床を隠すのは1.3行ぶん） |
| マス1つ | 18.8px | 18.8px（変わらない） |
| ページのはみ出し | — | 0 |

> ⚠ **マスの大きさは変わらない。** 横のマス数(20)と画面幅で決まるので、
> 行を増やしても「見える範囲が広がる」だけで、字が大きくなるわけではない。

第1章は `syncAreaBar()` が `areaCount() > 1` のときしか帯を出さない＝**隠れたまま**。
実際に `guardBoot(1)` で確認（区画数1／帯は hidden）。

### セーブの引っ越しを「版を1つずつ上げる」形に作り直した

盤面を変えるたびに移行を書き足すと破綻するので、間取りを表にして順に送る形にした。

```js
const BOARD_V_Y = 3;
const BOARD_GEO_Y = {
  1: { H: 10, divideY: 7 },   // 16×10（開発初期）
  2: { H: 14, divideY: 9 },   // 20×14
  3: { H: 17, divideY: 11 },  // 20×17（いま）
};
```

**実測**（版1のセーブを1回の読み込みで版3へ）

```
bandai(6,1)     → (8,1)
y_shoe(5,8)     → (5,15)   下の壁ぎわのまま
y_elev(13,7)    → (17,14)  右下の角
y_locker12(2,7) → (2,11)   旧・脱衣所 → 新・脱衣所
y_sauna1(2,2)   → 動かない（浴室）
```

版2のセーブも同様に通ることを確認。7階建て5日通しでエラー0・行き止まり0・追い返し0、
退店95人すべて1階から。`await guardAll()` → `[{ok:true, chapter:1}, null]`。

## 34. 盤面を 16×19 に／カタログを横スクロールに（作者指定 2026-08-08）

> もっと縦方向に大きく表示する。マスのサイズは元に戻す。
> アイテム選択を横スクロールにして省スペース化する。

**マスの大きさは「画面幅 ÷ 横のマス数」だけで決まる。** だから横を20→16に戻せば
マスは元の大きさに戻り、縦をいくら伸ばしてもマスは小さくならない。

### 縦の作り直し（375×812での実測）

| | 前（20×17） | 後（16×19） |
|---|---|---|
| マス1つ | 18.8px | **23.4px（元のサイズ）** |
| フロア画面の高さ | 319px | **445px** |
| 下のパネル | 387px | 259px |
| 置けるマス | 270 | 238 |
| 吹き出しの実寸 | 6.59px | 6.59px（同じ） |

縦を捻出したのは2つ：
1. **カタログを横スクロールの棚に**（`#shopList.compact` を `flex-direction:row` の
   カード帯にした）。縦積み200px超 → 118px。タブも折り返しをやめて一列の横スクロールに
2. §33 の**案内図の帯をキャンバスに重ねる**（継続）

### `bubScale` を 1 に戻した

キャンバスの幅が 1280 → **1024**（＝16×10 だった頃と同じ）に戻ったので、
字の掛け戻しは要らなくなった。**盤面の横を変えたら必ずここも見直すこと。**

### 第1章に影響しないこと（実測）

- カタログの横スクロールは `#shopList.compact` にだけ効く。`compact` が付くのは
  `hasHook('placeInfo')` を持つ章＝第2章だけ → `guardBoot(1)` で
  **compact:false ／ flex-direction:column**（縦積みのまま）を確認
- 案内図の帯は `areaCount() > 1` のときしか出ない → 第1章では hidden

### セーブの引っ越しに版4を足した（**横が縮む版は初めて**）

```js
const BOARD_V_Y = 4;
const BOARD_GEO_Y = {
  1: { W: 16, H: 10, divideY: 7 },
  2: { W: 20, H: 14, divideY: 9 },
  3: { W: 20, H: 17, divideY: 11 },
  4: { W: 16, H: 19, divideY: 13 },   // いま
};
```

**実測**（版3のセーブを読み込み）

```
bandai(8,1)      → (7,1)
y_shoe(5,15)     → (5,17)    下の壁ぎわのまま
y_vend(18,3)     → (14,3)    ★横が狭くなったので内側へ寄せた
y_elev(17,14)    → (13,16)   右下の角
y_locker24(2,11) → (2,13)    旧・脱衣所 → 新・脱衣所
y_sauna1(2,2)    → 動かない
```

全部が盤面の内側に収まることを確認。7階建て5日通しでエラー0・行き止まり0・追い返し0、
退店87人すべて1階から。`await guardAll()` → `[{ok:true, chapter:1}, null]`。

### 追記（同日・脱衣所を縦4マスに／作者指定）

`divideY` 13 → **14**。浴室階の内訳が変わる。

| | 前 | 後 |
|---|---|---|
| 浴室 | y1〜12（12行） | **y1〜13（13行）** |
| 脱衣所 | y13〜17（5行） | **y14〜17（4行・56マス）** |

エレベーター(13,16)とその扉(12,17)は脱衣所の側に残る＝
**上がってきて、脱いで、浴室へ**という順路は変わらない。

セーブの引っ越しに**版5**を足した（`{ W:16, H:19, divideY:14 }`）。
版4のセーブで実測：

```
y_locker24(2,13) → (2,14)   旧・脱衣所の1行目 → 新・脱衣所の1行目
y_sink(6,16)     → (6,17)
y_sauna1(2,2)    → 動かない（浴室）
```

3日通しでエラー0・行き止まり0・追い返し0。`await guardAll()` → `[{ok:true, chapter:1}, null]`。

## 35. 看板の設置を「絵をタップして選ぶ」に／案内図のメニューも横スクロール（作者指定 2026-08-08）

### ① 袖看板・ネオンは、ビルの**右側面のどの階にも**付けられる

これまでは【2Fに付ける】【3Fに付ける】…というボタンの列で、しかも常に右側の決め打ち。
フロア画面で設備を置くのと同じ手つきに変えた。

- 一覧のボタンは【🪧 場所を選んで付ける】ひとつ（付けたあとは【🪧 付け替える】）
- 押すと**ビルの右側面に置き場が点線で浮かぶ**（2階以上・1階は地面に刺さるので出さない）
- タップしたところに付く。いま付いている階には【いまここ】と出る
- 選んでいる最中は、よそを叩いても階の中へは入らない

> ⚠ **左側面には出さない。** 階の名札（「2F 男湯」）がビルの左に並ぶので、
> 左に看板を出すと必ず字が重なる（作者指摘）。
> このため場所は階だけで足り、**セーブは今までどおり数字1つのまま**（`{f, side}` にはしない）。

**実測**＝【場所を選んで付ける】→ 4Fの置き場をタップ → `gaikan.sode = 3`（4F）。
【付け替える】→ 2Fをタップ → `gaikan.sode = 1`。置き場は付けた瞬間に消える。

### ② 案内図の下のメニューも横スクロールに

品と操作ボタンを1枚のカード（`.gk-card`）にまとめ、`#gaikanList.hstrip` で横に並べた。

| タブ | 前 | 後 |
|---|---|---|
| 🪧 外観 | 縦に約360px | **横スクロール 126px** |
| 🏗 増築 | 縦に長い | **横スクロール 87px** |
| 📣 集客 | 縦 328px | 縦のまま（中身は共有の `renderAds`） |

### 第1章に影響しないこと（まっさらから第1章だけ起動して実測）

```
外観パネル      隠れている ✓（renderGaikan フックを持たない）
.gk-card        0枚 ✓
案内図の帯      隠れている ✓
設備カタログ    compact:false ／ flex-direction:column ✓（縦積みのまま）
```

`await guardAll()` → `[{ok:true, chapter:1}, null]`。

## 36. 集客も横スクロール／外観に看板の絵／**第1章にメニューが残るバグ**（2026-08-08）

### ① 【📣 集客】も横スクロールに

広告の一覧を作るのは共有の `renderAds` だが、**器は案内図の中の `#sendenPane`** で、
これは第2章の外観メニューしか作らない。そこを狙えば**第1章の広告画面
（`#sendenList`・モーダル側）には1pxも触れない。**

実測＝第1章の広告モーダルを開いて計測 → `display:block`／3件が y313・y378・y443 と
**縦に並んでいる**（変わっていない）。

### ② 【🪧 外観】に看板の絵を入れ、フロアのカタログと同じ形に

看板は設備（EQ）ではないので共有の `iconFor()` が使えない。
`ySignIcon(id)`（art_y.js）でその場で描いて data URL にする。色はビルの絵と揃えた
＝一覧と外観で同じ物に見える。

- 袖看板 … 赤い縦板に「SAUNA」／ネオン … ピンク＋光／屋上 … 横長の板に♨
- 並びも **絵 → 名前 → 値段** の縦積みにして、フロアのカタログのカードと同じ形に

### ③ ★第1章の画面に第2章のメニューが残るバグ（作者指摘）

**第2章で遊んだあと第1章へ戻ると、【外観／集客／増築】が第1章の画面に残る。**

```js
function syncAreaBar() {
  const show = areaCount() > 1;
  bar.classList.toggle('hidden', !show);
  if (!show) return;            // ← 区画1つの章（第1章）はここで抜ける
  ...
  const gp = $('gaikanPanel');  // ← ここまで来ないので、畳まれない
```

第1章は `areaCount() === 1` なので早期 return し、**その下の gaikanPanel を畳む処理に
到達しない。** 第2章が開いたまま残した表示が、そのまま第1章に居座る。
抜ける前に必ず畳むようにした。

**実測**（章を行き来）

| | 直す前 | 直した後 |
|---|---|---|
| ① 第1章だけ | 隠れている | 隠れている ✓ |
| ② 第2章の案内図 | 出ている | 出ている ✓ |
| ③ **第2章 → 第1章** | **★出ている** | **隠れている ✓** |
| ④ 第1章で画面操作 | ★出ている | 隠れている ✓ |
| ⑤ 第1章 → 第2章 | 出ている | 出ている ✓ |

> 第1章単独で起動したときは元から隠れているので、**第1章だけを遊ぶ限り踏まない。**
> 章を行き来したときだけ出る。今日の変更で作り込んだものではなく、以前からあった穴で、
> 外観メニューを横スクロールのカードにしたことで目立つようになった。第1章セッションへ報告済み。

`await guardAll()` → `[{ok:true, chapter:1}, null]`。

---

## §37 吹き出しの大きさ／ラベルの言い換え／【融資】を運営メニューへ（2026-08-08）

作者から3件。いずれも **`CONF` に新しいキーを足さず、`CONF_Y` 側の旗で区切ってある。**

### 37-1 吹き出しがまだ小さい（`js/game.js` `drawBubble`）

**盤面のマス数がそのまま字の大きさになっている。** キャンバスは横幅いっぱいに
縮めて表示するので、横のマスが増えるほど画面の中の1マスも字も小さくなる。

| | 第1章 | 第2章 |
|---|---|---|
| 横のマス数 | 13 | **16** |
| 1マス（画面幅375のとき） | 28.8px | **23.4px** |
| 吹き出しの字（同上） | 8.1px | 6.6px → **10.5px** |

`bubScale` を 1 → **1.6**（`js/ch2b/data_y.js`）。1 は「昔の第2章と同じ」であって
「第1章と同じ」ではなかった。第1章に並べるだけなら 16/13＝1.23。

あわせて `drawBubble` に1行。**吹き出しは頭の上へ 34×s ＝54px 伸びるので、
最上段（y=1・py=48）の客のぶんが画面の外へ出る。**

```js
let by = e.py - 34 * s;
if (CONF.bubScale) by = Math.max(2, by);   // 縮尺を戻した章だけ
```

`CONF.bubScale` を持たない第1章は条件に入らない＝`by` の値も型も従来どおり。

> **第1章は元から切れていない**（第1章セッションの指摘で実測 8/8）。
> `py = y * T + T/2`（js/game.js:493）。第1章で歩ける最小の y は **1**
> （`walkable()` を y=0 から総当たりして確認。最上段は壁）＝ `py = 48`、`by = 48 − 34 = **14**`。
> 1日回して主人公・客・バイトの `py` を延べ1,483件拾っても最小は 48 だった。
> **つまり `Math.max(2, by)` は、仮に第1章で条件に入っても何もしない。**二重に無害。
> 切れるのは `s = 1.6` の第2章だけ（`by = 48 − 54.4 = −6.4` → 2）。
> ⚠ 当初「第1章は上に切れたまま＝従来どおり」と書きかけたが、**それは測っていない前提だった。**
> 「従来どおり」を確かめるには、いまの見た目そのものを測ること。

### 37-2 ラベルの言い換え

- `areaLabel`（案内図の見出し）… `🗺 館内案内図` → **`🏢 外観`**
- `btnToGuide`（index.html）… `← 案内図` → **`← 外に出る`**

どちらも第2章でしか表に出ない（第1章は `areaCount() <= 1` で帯ごと隠れる）。

### 37-3 【🏦 融資】を【運営】メニューの中へ

準備画面の下ボタンが5つで折り返していたので、バイトと同じ棚へ移した。
`CONF_Y.loanInManage = true`。**第1章はこのキーを持たない＝タブは2つのまま、
融資ボタンも下に残る。**

`js/game.js` の変更は4か所。

1. タブに `['loan','🏦 融資']` を足す（`CONF.loanInManage` のときだけ）
2. `manageTab === 'loan'` かつ旗が無い章 → `'fee'` に戻す（章を戻したとき用）
3. `loanHost(pane)` を新設＝資金繰りモーダルの中身3つ
   （`loanCash` / `loanSecBank` / `loanSecYami`）を運営メニューへ**貸し出す**。
   `null` で元へ返す。並びは【とじる】の前に入れ直すので保たれる
4. `btnLoan` / `btnLoanBiz` を `CONF.loanInManage` のときだけ隠す

index.html は `<div class="loan-sec yami">` に `id="loanSecYami"` を足しただけ
（第1章の CSS も JS もこの id を見ていない）。

> ⚠ **踏んだ罠。** `loanHost(null)` を `box.innerHTML = ...` の**後**に置いたら、
> 借りた3つが innerHTML で先に消えて**消滅**した（以降どの章からも資金繰りが空になる）。
> **返すのは必ず書き換えの前。** `openLoan` の頭にも `loanHost(null)` を入れて二重に保険。

タブが4つになると「ルール」「バイト」が2行に折り返したので、
`.opt-tabs.many`（タブが3つ以上のときだけ付く）で横スクロールに。
**第1章はタブが2つ＝この class が付かない**（実測 146x39 ×2・従来どおり）。
開いているタブが画面の外にいることがあるので `scrollLeft` で寄せる。
⚠ 1コマ待つこと（`requestAnimationFrame`）。【運営】を押した直後は
renderManage →モーダルを開く の順で、描く時点ではまだ幅が 0。

### 実測（章を行き来）

| | 第1章 | 第2章 |
|---|---|---|
| 運営のタブ | 💴料金／🚪ルール（`opt-tabs`） | ＋👥バイト／🏦融資（`opt-tabs many`） |
| タブ列の高さ | 39px | 40px（折り返し無し） |
| 融資ボタン（準備・営業） | 出ている | 隠れている |
| 資金繰りの中身3つ | モーダルの中 | 融資タブを開いている間だけ運営メニュー |
| 融資タブを開いたまま第1章へ切替 | **中身はモーダルへ戻る／タブは fee** | — |

`await guardAll()` → `[{ok:true, chapter:1}, null]`。

### 見つけたが直していないもの（第1章の担当）

`renderKouko()` が `loanBankTitle` / `loanInfoBank` / `koukoAmts` を書き換えるが、
**`CONF.kouko` を持たない章（第1章）へ戻したとき、元の「信用金庫に門前払い」の文へ
戻す処理が無い。** 第2章で資金繰りを開いたあと第1章へ切り替えると、第1章の画面に
「🏦 横浜信用金庫（融資課）／枠5000万円」が残る（実測）。
§36 と同じ種類の、**章を行き来したときだけ踏む**穴。配信ビルドには出ない。

**→ 直した**（第1章セッションの依頼 8/8。あちらが `js/game.js` に触ると競合するため、こちらで実施）。
`if (CONF.kouko) renderKouko();` に `else` を足し、`index.html:280-282` の原文へ戻す。

```js
if (CONF.kouko) renderKouko();
else {
  $('loanBankTitle').textContent = '🏦 銀行から借りる';
  $('loanInfoBank').innerHTML = '「申し訳ありませんが、<b>審査は通りません</b>。……失礼ですが、赤字の銭湯さんですので。」<br>—— 信用金庫 融資担当';
  $('koukoAmts').innerHTML = '';
}
```

**実測**

| | `loanBankTitle` | `loanInfoBank` | `koukoAmts` |
|---|---|---|---|
| A 読み込み直後（index.html の原文） | 🏦 銀行から借りる | 審査は通りません… | 空 |
| B 第2章で資金繰りを開いた後 | 🏦 横浜信用金庫（融資課） | 年2.0%・枠5000万円… | 空 |
| C 第1章へ戻して開いた後 | 🏦 銀行から借りる | 審査は通りません… | 空 |

**A と C は文字列として完全一致。** `koukoAmts` はボタン2つを差し込んだ状態からも
0個に戻ることを別途確認した（B の欄が空なのは、この条件では枠が出ないだけ）。
第1章だけを起動している限り3つとも書き換わらないので、そこでは何も変わらない。

---

## §38 不具合6件＋仕様2件（2026-08-08）

`js/game.js` 5か所・`css/style.css` 1か所。**すべて第2章が旗やフックを持たなければ従来と同一。**

### 38-1 帰る客が左下へワープする（`walkToExit`）

```js
c.path = (findPath(...) || []).concat([{ x: 0, y: CONF.H - 1 }]);   // 左下の角
```
第1章は玄関が (0,10) ＝この角そのものなので、同じ升目を2回指しているだけ。
第2章は玄関が下辺の中央なので、**そこから左下まで突っ切っていた。**
`chHook('exitTile')` で章に決めさせる。第2章は `{x: CONF.entrance.x, y: CONF.H-1}`。

**ただし本命の原因はこちらだった**（js/ch2b/rules_y.js `yStepTransit`）。
エレベーターで降りた直後、**まだ「乗る前の階」が CONF に載っている最中**に
`walkToExit` / `goLocker` を呼んでいた（`forEachArea` は階ごとに CONF を差し替えて回す）。
＝**乗る前の階の玄関へ向かう道を、降りた階の盤面に引いていた。**
降りた階を `applyArea(to, true)` してから呼び、`finally` で戻す。

実測：帰る客90人が全員 `0階(6,18)`＝玄関へ。第1章は37人が全員 `(0,10)` のまま。

### 38-2 カタログのタブを配列でも持てるように（`shopTabsOf`）

ドリンク自販機は「1階の待合」と「浴室階の脱衣所」の両方に置ける約束だったが、
`tab` を1つしか持てず**脱衣所から消えていた**。`tabs` 配列を足す。
`tabs` を持つ設備は第2章の自販機1つだけ＝js/data.js には0個（実測）。

### 38-3 導線の台詞（`chHook('dosenLines')`）

「導線が終わってる。サウナ→水風呂が遠すぎだろ」は、
**距離ではなく1回の来店で歩いた総マス数**で出る。第2章は7階を上下するので、
隣に並べても言われる。台詞を章に選ばせ、第2章は**本当に8マス以上離れているときだけ**名指しする。

### 38-4 歩数の数え方（`chHook('noWalkCount')`）

第2章は**浴室の階で歩いたぶんだけ**数える＝導線が本当に配置の指標になる。
第1章はフックが無いので毎回加算（実測：歩数は従来どおり積まれる）。

### 38-5 配置バーを下に貼り付ける（`#confirmBar.stick`）

【向き・ここに置く・やめる】がスクロールしないと見えないことがあった。
`CONF.stickyPlaceBar` を持つ章にだけ class を付ける＝第1章にはこの規則が一度も当たらない。
実測：バー下端804 ≦ パネル下端812。

### 38-6 盤面を 16×19 → **13×19** に（第2章のみ）

**1マスを第1章とまったく同じ大きさに戻す**（作者指定）。脱衣所の縦4マスは維持。

| | 前 | 後 | 第1章 |
|---|---|---|---|
| 横のマス数 | 16 | **13** | 13 |
| 1マス（幅375） | 23.4px | **28.85px** | 28.8px |
| `bubScale` | 1.6 | **1.3** | なし（＝1） |
| 吹き出しの字 | 10.55px | **10.55px** | 8.11px |

> **`bubScale` を 1 に戻さなかった理由**（第1章セッションの指摘で作者に確認・8/8）。
> 1マスは 28.85px で第1章と完全一致した。字だけ 1.3倍のまま残す。
> 作者は**第1章と同じ 8.11px を一度も見ていない**（見たのは 6.59px＝「まだ小さい」と、
> 10.55px＝いま）。盤面を戻したついでに黙って 8.11px へ落とすと
> 「また小さくなった」としか映らないため、数値を並べて確認を出し、
> **「いまの見え方のまま（10.55px）」で決定**。
> `drawBubble` の `Math.max(2, by)` は `s=1.3` で `by = 3.8` ＝条件には入るが何もしない。

エレベーター(2×2)は右下へ `x 13→10`、扉 `12→9`。1階の玄関は `x 8→6`。
`BOARD_V_Y` を **6** に上げ、`BOARD_GEO_Y[6] = {W:13,H:19,divideY:14}` を追加。
横が縮むので、右端にあった設備は移行時に内側へ寄せ直す。

**実測**：7階すべてで全設備が置ける（置けない品 0）。
セーブ移行（版5→6）で壁の外に出た物 0・道が無くなった物 0。

### 38-7 ぬる湯で「あちー！」＝**子どものおしゃべり**だった

作者の一言（「子供が言ってた」）で割れた。`js/data.js:700`

```js
kidLine: ['あちー！', 'パパー、まだー？', 'およぐ！', 'つめたっ！', '牛乳のむ！', 'もう一回！'],
```

`js/game.js:3256` が **いま何に入っているかを一切見ずに**この表から引いていた。
だから 38℃のぬる湯で「あちー！」、45℃の超あつ湯で「つめたっ！」が出る。**症状は対称。**

`furoAtsu` / `saunaGoodHot` を温度で分けたとき、**ここだけ見落とされていた**（第1章も同じ）。

```js
bubble(c, pick(chHook('kidLines', c) || LINES.kidLine), 2.4);
```

第2章の `yKidLines(c)` は `c.use`（`item` と `cat`）を見て、
熱がる台詞は**43℃以上の湯とサウナ**、冷たがる台詞は**水風呂**でだけ許す。

**実測（設備ごとに、その表に「あちー！」「つめたっ！」が入るか）**

| 設備 | あちー！ | つめたっ！ |
|---|---|---|
| ぬる湯38℃ / あつ湯42℃ / ジェット40℃ | — | — |
| 電気風呂43℃ / 超あつ湯45℃ | ○ | — |
| サウナ90℃・110℃ | ○ | — |
| 蒸気サウナ60℃（gentle） | — | — |
| 水風呂16℃ / チラー8℃ | — | ○ |
| カラン・脱衣所・ロビー | — | — |

6日通しでも噛み合わない発話は0件。

> **第1章にも同じだけ残っている**（`LINES.kidLine` はそのまま）。
> 審査承認待ちのため直さず、第1章セッションへ報告のみ。フックは向こうがいつ直しても邪魔にならない形。

### かつて再現できなかった経緯（記録として）

**8日回しても出なかったのは、不満側（`温度 − 好み >= 4`）だけを見ていたから。** 作者のセーブそのままで8日回し、
ぬる湯・あつ湯に立った客の吹き出しを全部拾ったが、熱すぎ系は一度も出ていない。
`tempReact === 'hot'` は `湯温 − その客の好み >= 4` で、第2章の湯温の好みは最低38（女の子）、
ぬる湯も38なので**原理的に成立しない**。台詞の実物を作者に確認中。

---

## §39 章を行き来したときの DOM 残り検査（2026-08-08）

`js/dev/chapterDiff.js` を新設。**共有ファイルには一切触れていない**（index.html の
開発用ローダーに1行足しただけ。`chapterGuard.js` と同じく localhost かつポート指定ありのときだけ読む）。

```js
await chapDiff()                    // 第1章を正として、第2章を経由した往復
await chapDiff({base: 2, via: 1})   // 逆向き
await chapDiff({dry: true})         // 空振り＝揺れの下限
```

### 設計（第1章セッションの指摘を反映）

- **①でも正の章の画面を開く。**起動直後のDOMを正にすると `renderKouko` 型が丸ごと写らない
- **両端で `guardBoot(base)`。**手持ち資金・日数・評判が差分に出ない＝除外リストを育てなくていい
- 記録する欄は `class` / `hidden` / `display` / `style` / `innerHTML` / **親要素と位置**
  （最後の1つが無いと、融資タブの引っ越し型を捕まえられない）
- **canvas は `toDataURL` のハッシュ1個だけ。**中身は見ていない（結果にも明記して出す）
- **空振りを自動で1本走らせ**、そこに出た id には「不安定」の印を付ける
- 画面は**押せるボタンを自前で拾って全部押す**（表を信じない）。押したあと `.overlay` が
  出たかを毎回確かめ、出なかったものは結果の先頭に出す。**一度も開けていない画面も名前を出す**

### 道具そのものが作った差（実測で発見）

**ならし運転（⓪）を入れる前は、`shopPanel` が 18,290字 → 18,299字で本物のように見えていた。**
初回の描画だけ違うもの（🔴新着マークは一度描くと既読になる）が原因。
`guardBoot(base)` → 画面を開く、を1回捨てるだけで消える。**空振りでは捕まらない**
（空振りが本番のあとに走るので、そのときには既に温まっている）。

### 初回の結果（第1章 → 第2章 → 第1章）

差分33件。うち空振りでも出る3件（`app` / `jobModal` / `jobList` ＝応募者が毎回ちがう）を除き、
入れ物（子の巻き添えで出る親）を畳んで **21件**。

| 残ったもの | 欄 |
|---|---|
| `uiStam` / `uiStress` / `uiMood`（体力・ストレス・妻の機嫌） | class・display・style・innerHTML |
| `tip`「2階にサウナを置こう。まだ営業できない」 | innerHTML |
| `areaLabel`「🏢 外観」／`btnToGuide` の hidden | innerHTML・class |
| `shopTabsG`／`gaikanList`（外観・集客・増築のカード） | innerHTML・class・display |
| `staffMgrBody`（第2章のバイト管理の中身） | innerHTML |
| `guide` キャンバス（第2章のビルの絵） | 画像のハッシュ |

**21件とも、いまは第1章で1つも見えていない**（親が畳まれているため。実測で確認）。
ただし §36 は「親を畳み忘れた」1行で表に出た事故なので、**見えていない＝安全ではない。**

判定（どれを第2章側で直し、どれを第1章の文言として確定させるか）は第1章セッションへ送付済み。
**まだ1件も直していない。**

---

## §40 主役はサウナ（2026-08-08・作者決定）

**主人公の人生をゲームから外す。**

- 毎週**月曜が定休日**（固定）。ゲームは**火曜から**始まる
- 月曜は「今日は何をする？」を出さず、**強制イベント**が起きる（**中身は未定**）
- **体力・ストレス・妻の機嫌は使わない**。妻は番台に立つ人として残る

### ⚠ いつでも戻せる（作者指定）

**コードは1行も消していない。** `js/ch2b/data_y.js` の5つを戻せば元の形に戻る。

```js
gauges: false,      // → true   体力・ストレス・妻の機嫌が復活
staminaOn: false,   // → true   共有側の体力そのものが復活
weekOff: 0,         // → null   定休日なし
startDow: 1,        // → 0      月曜始まりに戻る
dayChoice: false,   // → true   毎朝の「今日は何をする？」が復活
```

**実測（旗を戻して確認）**：毎朝の一枚が2日目から出て、選択肢6つ
（そのまま出勤／寝る／筋トレ／ライバル店へ／妻と出かける／野毛で飲む）が全部生き返り、
ゲージの帯も体力も戻った。旗を戻し直すと、また新しい形になる。

`js/game.js` には**触れていない**（`dayOfWeek` は元からフックを持っていた）。

### 中身

| どこ | 何をしたか |
|---|---|
| `yDayOfWeek(d)` 新設 | `startDow` のぶん曜日をずらす。`dayOfWeek` フックとして登録 |
| `yClosedToday` | `weekOff` の曜日を定休として追加（`downDay` は残す） |
| `yClosedWhy` | 定休の理由に `'week'` を追加 |
| `yOffDay` | `dayChoice` が偽なら**選ばせない**。営業日はそのまま開け、定休日は下の一枚 |
| `yShowMondayEvent` 新設 | **強制イベントの置き場所。いまは「定休日」とだけ出す** |
| `yStamAdd` / `yMoodAdd` / `yStressAdd` | `gauges` が偽なら**増減しない**（数字は残る） |
| `moodPct` / `stressPct` | `gauges` が偽なら `null`＝帯そのものが出ない |
| 日報の体力の行 | `gauges` が偽なら出さない |

### 実測（8日通し）

```
1日目 火曜 営業 会計33人   5日目 土曜 営業 会計58人
2日目 水曜 営業 会計38人   6日目 日曜 営業 会計54人
3日目 木曜 営業 会計30人   7日目 月曜 ★定休（強制イベントの一枚）
4日目 金曜 営業 会計39人   8日目 火曜 営業 会計67人
```
「今日は何をする？」は一度も出ない。ゲージの帯は出ない。`staminaOn()` は false。

### まだ決まっていないこと

**月曜に何を起こすか。**お出かけ（`odekake_y.js`）・講習・野毛の源さん・ライバル視察は
**コードが全部残っている**ので、`yShowMondayEvent()` の中から呼び出しを繋げば動く。
デートを残すか外すかを含め、作者の決定待ち。

---

## §41 持ち場まわり3件（2026-08-08）

### 41-1 主人公を2階に配置すると掃除しない ← **妻が原因**

`yStaffOnFloor(f)` が **妻を数えていた。**

```js
G.staff.some(s => !(s.lateT > 0) && (s.f | 0) === (f | 0))
```

この判定の意味は「そこはその人に任せた＝主人公は踏み込まない」なので、
**掃除をする人だけ**を数えなければならない。妻は番台に立つだけで床は拭かない。

結果、**1階に汚れが6つあっても主人公は420分ずっと2階に立っていた**（実測）。
`!s.isWife` を足しただけで直る。

| | 直す前 | 直した後 |
|---|---|---|
| 1Fに配置：残った汚れ | 12 | **1** |
| 2Fに配置：残った汚れ | 12 | **5** |
| 2Fに配置：主人公が居た階 | 2Fに420分（動かない） | 1Fに333分／2Fに87分（汚れを追う） |

### 41-2 【バイト】を押すと第1章の画面になる（`renderStaffMgr`）

**「見えているか」で宛先を選んでいたのが原因。**

```js
const box = (own && !own.closest('.hidden')) ? own : (paneIfShown('staffMgrPane') || own);
```

【運営】は `renderManage()` →**そのあと**モーダルを開く順なので、描く時点では
タブの枠がまだ見えていない＝`paneIfShown` が null を返し、中身がまるごと
第1章側の `staffMgrBody` へ流れていた。第2章のタブには
「📰 求人広告を出す」しか残らない＝作者の言う「1章のバイト画面になる」。

**在るかどうかで選ぶ**ように変更。`staffMgrPane` は `renderManage` が
バイトのタブを描いたときにだけ在るので、第1章は必ず `staffMgrBody` に描かれる。

実測：第2章のタブに階ごとの枠・主人公・妻・🌙深夜バイトが全部出る。
第1章側の `staffMgrBody` は空のまま。

> ⚠ **同じ「1コマ後にしか見えない」問題を、今日3回踏んだ**
> （運営タブの横スクロール／バイト画面の宛先／配置バー）。
> `renderXxx()` の中で「見えているか」を条件にしない。**在るかどうかで判断する。**

### 41-3 持ち場の既定＝**妻は1階の番台、主人公は2階の男湯**

7階建てで誰がどこに立つかが最初の一手だが、**冒頭で画面を出して選ばせるのはやめた**（作者指定 8/8）。
代わりに既定をそこに置く。二人で番台に並ばせない＝会計は妻、掃除は主人公。

```js
wifeF: AY.FRONT,     // 1階（番台）
playerF: AY.OTOKO,   // 2階（男湯）
```

`yPlayerFloor()` の落ち先も `AY.FRONT` → `AY.OTOKO` に。
**まだ2階が建っていないセーブ**や、**営業中に女湯を指定したセーブ**は1階へ落ちる
（実測：区画が1つ→受付／営業中に女湯→受付）。`playerF` を持たない既存のセーブも、
読み込んだ時点で新しい既定（2階）になる。

**実測**（1日通し）：主人公は男湯80分／受付340分（汚れを追って降りる）、妻は受付に420分。
会計72人、残った汚れ5。冒頭でバイト画面は開かない。

> 一度「準備画面に初めて入ったとき、バイト画面を一度だけ開く」を作ったが、**作者の指示で取り消した。**
> そのとき `staffMgrBody` に案内文を `insertAdjacentHTML` で差し込んでいて、
> **第1章は `openStaffMgr` が何もしない章なので描き直されず、その一言が第1章に残った**（実測）。
> §39 の差分テストが warn していた型を、自分で作っていた。
> **出したい言葉は、章が持っている画面の中身（`staffMgrTop` フック）に入れること。**

`staffMgrBody` に第2章の中身が残ること自体は **§39 の9番**（既知）。
第1章は `staffRooms` を持たずバイト画面を開けないので、表には出ない。

---

## §42 月曜日の中身（2026-08-08・作者決定）

`js/ch2b/monday_y.js` を新設。**共有ファイルには触れていない。**

### 比率＝**店の進み具合で変わる**

| | まだ話を聞いていないライバル店がある | 全店を回ったあと |
|---|---|---|
| ♨️ ライバル店 | **50%** | 15% |
| 🛍 買い出し | 20% | **35%** |
| 🍶 野毛で飲む | 15% | 25% |
| 💗 妻とデート | 15% | 25% |

攻略のヒントは早く渡すほど効くので、序盤はライバル店に寄せる。
実測（各400回）：序盤 52/19/15/14、全店後 14/34/26/27。

### 中身

- **ライバル店** … 初回は既存の一枚絵＋立ち話（**弱点はその店の客が教える**）。
  **一度聞いた店へまた行くと、もう教わることは無い**＝ただ整って金だけ減る（専用の地の文4種）
- **買い出し** … 割引・掘り出し物・出来事は既存。足したのは**無駄遣いの日**（17%・6種）
- **妻とデート** … 行き先・出来事・中華街のスカウトは既存。足したのは**人材との出会い**（25%）
- **野毛で飲む** … 人脈・源さん・スカウトは既存。足したのは**バイトと鉢合わせる夜**（25%・5種）

### 冒頭で「今日は定休日」と言う（作者指定）

**プレイヤーは日付を見ていない。**いきなり中華街の絵が出ても、なぜ店を開けていないのか
分からない。一日の頭に必ず1枚挟む（4種）。

### 日曜日＝「明日は休みだ」

定休日の前の日は、主人公が独り言を言う（7種）。日付を見なくても休みが近いと分かる。

> ⚠ **喋れなかった回で間隔を食い潰さない。**先に間隔をリセットしていたら、
> 主人公が別のことを喋っている最中に順番が来るたび1回ぶん捨てられ、
> **丸一日ひとことも言わなかった**（実測1回／日）。言えたときだけ次の間隔を置く。

### 締めを1か所にまとめた（`yShowResult`）

`dayChoice` が偽の章では【🏮 出勤する】ではなく**【🌅 明日の朝へ】**を出す。
ここ1か所で、デートも買い出しも野毛もライバル店もスカウトも同じ締めになる。

> ⚠ **`yEndOffDay` で `Y_OFFDAY_OPEN` を必ず下ろす。**ライバル店の初回だけは
> `yShowResult` を通らずに直接ここへ落ちるので、下ろし忘れると旗が立ったままになり、
> **次の定休日に朝の画面が二度と出なくなる**（実測）。

### ゲージの数字を文から落とす（`yStripGauge`）

出来事の文に「体力+15」「機嫌−8」が残っていた。**台本は書き換えず**、
`gauges: false` の章でだけ表示から落とす＝旗を戻せば元の文がそのまま出る。

### 実測（3週間の通し）

```
1〜6日目 火〜日 営業（35〜52人）
7日目  月曜 ★定休（ライバル店の初回＝一枚絵で完結）
8〜13日目 火〜日 営業
14日目 月曜 ★定休「🌅 明日の朝へ」赤レンガ倉庫へ出かけた…
15〜17日目 火〜木 営業
```
日曜だけ「日曜が終われば、うちの正月だ」等が出る。水曜・土曜は無言。

---

## §43 子どものおしゃべりを共有の既定へ／章を移るときの巻き戻し（2026-08-08）

### 43-1 `kidLinePool()`（`js/game.js`）＝**両章共通の既定**

第2章だけのフックをやめ、共有側の既定そのものを直した（第1章セッションの指示）。

- 「あちー！」… **サウナ**か、**`CONF.kidHotTemp`（既定43℃）以上の湯**のときだけ
- 「つめたっ！」… **水風呂**のときだけ
- 残り4語（パパー、まだー？／およぐ！／牛乳のむ！／もう一回！）は**どこでも出る**

`LINES.kidLine` の6語は**触っていない**（章の指紋が動くため）。落とすのは選ぶ側。

**第1章の実測**（抽選300回×設備）

| 設備 | 出た台詞 |
|---|---|
| ぬる湯38℃ / 電気41℃ / ジェット41℃ / 替わり湯42℃ / カラン / 何にも入っていない | 4語のみ |
| あつ湯43℃ / 超あつ湯45℃ | ＋あちー！ |
| 遠赤サウナ90℃ / フィンランド100℃ / ミスト60℃ | ＋あちー！ |
| 水風呂15℃ / 地下水13℃ | ＋つめたっ！ |

12日回して噛み合わない発話は0件。
（第1章は子どもの客が自然には湧かなかったので、実客を子ども扱いにして同じ経路を通した）

### 43-2 章を移るときの巻き戻し（`js/chapter.js`）

**去り際に片付けない。入り口で戻す。**（第1章セッションの提案）

```
① ページ読み込み直後（どの章も描く前）＝index.html のままの姿を控える
② applyChapter() の頭で必ずそこへ戻す
③ そのあと、その章が自分の表示を描く
```

**踏んだ罠を3つ、順に。**

1. **親を丸ごと戻したら、ボタンが全部死んだ。**`prepMenu` ごと作り直され、起動時に付けた
   `onclick` が消えた（実測：btnManage も btnOpen も反応しない）。
   → **いちばん内側の違いだけ**戻す
2. **class を戻す前に innerHTML を数えていた。**親を先に見た時点では子の class がまだ
   古く、「親の中身が違う」と誤判定して親ごと作り直していた。
   → **①class/hidden/style を全部戻す → ②数え直す → ③戻す** の3手に分けた
3. **それでも `areaBar` が作り直され、【← 外に出る】が死んだ。**
   → 中に **index.html にもとから在る要素**を抱えているものは触らない。
     逆に**中身がまるごと後から作られたもの**（カタログ・外観のカード・バイト管理の中身）は
     作り直してよい（もとの姿は空で、ボタンも描くたびに付け直される）

**実測**

| | 差分 | ボタン |
|---|---|---|
| 第2章 → 第1章 → 第2章 | **0件**（不安定3件を除く） | — |
| 第1章 → 第2章 → 第1章 | **5件**（下記）＋ guide キャンバス | — |
| 4往復のあと | — | **死んだボタン0** |

残る5件は `game-ui` / `canvasWrap` / `panel` / `shopPanel` / `loanModal` ＝
**中に静的なボタンを抱えているため、あえて作り直していない親**。
第1章セッションの判定＝**そのままで結構。ハンドラの張り直しは作らない。**
（目的は「第2章の表示が第1章に残らないこと」であって、文字列を一致させることではない）

### 43-3 据え置きを結果に明記する（第1章セッションの条件）

無印のまま残すと、半年後に誰かが「5件も漏れている」と読んで
**親ごと作り直す＝ボタンを殺す**直し方をする。そこで結果に必ず出す。

```
game-ui    意図的に据え置き（中に index.html 由来のボタンを抱えるので親は作り直さない）
           ／中の要素 60件中 57件が一致　★不一致：canvasWrap,panel,shopPanel
canvasWrap 同上／中の要素 7件中 7件が一致
panel      同上／中の要素 40件中 39件が一致　★不一致：shopPanel
shopPanel  同上／中の要素 2件中 2件が一致
loanModal  同上／中の要素 10件中 10件が一致
```

★不一致に出るのは、**それ自身も据え置きの親**だけ。葉はすべて一致している
＝据え置きが免罪符になっていないことが読み手に分かる。

### 43-4 検査した画面を増やした

物語のモーダル（田所・玲奈・鬼頭・黒田）を、**描く関数を直接呼ぶ**形で対象に入れた。
`kurodaModal` は「課題」の中身が要るので、最小の課題をその場で組んで開く。

日報（reportModal）も入れた。開くには `closeDay()` が要り、その一回で日付も売上も動くので
いったん外していたが、**空振りにも同じ手順で入れて物差しに掛けた**（第1章セッションの指摘）。

> **物差しに掛けていない項目は、判定できないのであって、本物なのではない。**
> ならし運転の発見とまったく同じ形。外したままだと、3件が「本物」の顔で残り続けていた。

結果は**割れた**。

| | 空振りでも出るか | 判定 |
|---|---|---|
| `bizLog` | **出る** | **揺れ**（その日の記録＝客の引きで毎回変わる）→ 不安定の印が付いた |
| `shopTabs` / `shopList` | **出ない** | **本物**（下記 §43-7）|

### 43-7 【本物】カタログの開くタブが、章をまたいで持ち越される

`shopTab`（js/game.js の**変数**。DOMではない）が章をまたいで残る。

**再現**（プレイヤーが普通にやる操作）

```
① 第1章だけ         … カタログは【サウナ】で開く
② 第2章で【ととのい】を開く
③ 第1章へ戻る       … カタログが【風呂】で開く   ★①と違う
```

第2章のタブ（`gaiki`）は第1章の一覧に無いので、`renderShop` が
「無ければ先頭」（js/game.js:9964）に落ちる＝**先頭の【風呂】**になる。
落ちること自体は正しい。問題は**第1章がどのタブを開くべきか**が決まっていないこと。

**→ 直した**（第1章セッションの判定＝案1「章ごとに最後に開いたタブを覚える」）。
理由：第1章は起動直後に「おすすめは【サウナ】＋【水風呂】＋【ととのいイス】」と案内を出しており、
カタログが【サウナ】で開くのはその案内と対になっている。

`SHOP_TAB_MEM`（章 → タブ）。**セーブにも localStorage にも入れない**＝
アプリを開き直したら第1章は【サウナ】から。

**踏んだ罠を2つ。**

1. **毎回思い出したら、タブが1つも切り替わらなくなった。**
   タブを押す側は `shopTab = key; renderShop()` なので、`renderShop` の頭で
   記憶を書き戻すと押した値を即座に潰す。
   → **章が変わった最初の1回だけ**思い出す（`SHOP_TAB_LAST_CH`）
2. **「無ければ先頭」に落ちた値を覚えていた。**第2章はタブが階ごとに変わるので、
   【ととのい】を開いたまま1階へ降りただけで記憶が【受付】に化ける。
   → **落ちる前に覚える。**さらに落ちるときは、覚えているタブが使えるならそちらを優先する

**実測（第1章セッション指定の5点）**

| | 結果 |
|---|---|
| ⑤ 起動直後の `shopTab` | `sauna` |
| ① 第1章 | **サウナ**（タブ切替も動く：水風呂 → サウナ） |
| ② 第2章で【ととのい】を開く | ととのい |
| ③ 第1章へ戻る | **サウナ** |
| ④ 第2章へ戻る | **ととのい** |

記憶の中身 `{1:'sauna', 2:'gaiki'}`、セーブには入っていない。
差分テストも 1→2→1 の本物0件を維持。

### 43-8 乱数と時刻を固定した（第1章セッションの指摘）

**空振りを2回3回に増やすのは筋が悪い。**サンプルを増やしているだけで、原理が変わらない。
今日それが実際に出た（`bizLog` が、逆向きの回だけ空振りの引きに出ず「本物」の顔をした）。

`js/dev/chapterDiff.js` の中だけで、テストの間だけ `Math.random` を**種を決めた擬似乱数**に、
`Date.now` を**固定の時刻**に差し替える。`try / finally` で必ず戻す。

**効果**

- `bizLog` も応募者も天気も、**そもそも差分に出なくなった**（同じ種＝同じ結果）
- **「不安定」という分類そのものが要らなくなった**＝差分が出たら本物、で切れる
- **揺れに埋もれて見落とすものが無くなった**（`shopTabs` はそれで危うく埋もれるところだった）

**実測**

| | 差分 | 空振り | canvas |
|---|---|---|---|
| 第2章 → 第1章 → 第2章 | **0件** | 0件 | **0件** |
| 第1章 → 第2章 → 第1章 | **本物0件**（据え置き6件のみ） | 0件 | **0件** |
| 4往復のあと | — | — | 死んだボタン0 |

`Math.random` / `Date.now` が元に戻っていることも毎回確認する。

**キャンバスも0件になった。**寸法の控えが null（index.html に width/height を書いていない）のとき、
**属性を消す**ようにしたため。消しても大きさが違えば画像として一致しないので、
「中身を消す」だけでは足りなかった。第2章の案内図（1040×1150）は行き来したあとも正しく描ける。

### 43-5 キャンバスは「不安定」だった（→ 43-8 で解消）

`guide` と `storyArt` は空振りでも毎回ハッシュが変わる。
**案内図の空の色が実時間で変わる**ため（`ySkyNow`）。
寸法は控えて戻すようにしたが、それでも中身は揃わない＝
`canvasは不安定か: true` として結果に出す。**「見ていない」ではなく「揺れる」**と書き分ける。

### 最終の実測

| | 本物の差分 |
|---|---|
| **第2章 → 第1章 → 第2章** | **0件** |
| **第1章 → 第2章 → 第1章** | **0件**（据え置き5件・不安定3件は印つき） |
| 4往復のあと | **死んだボタン0** |

### 43-6 子どもが湧かないのはバグではない（第1章セッターの実測）

`G.opts.banYakuza`（刺青・ヤクザお断り）が off だと子連れは来ない。第1章の既定は off。
＝**札を出せば強面客とみかじめが消える代わりに子連れが来る**という交換。
関門として効いているのが正で、第1章は何も直さない。

> **今日いちばんの学び**：空振り実行は「実行ごとの揺れ」を捕まえるが、
> **「初回だけ違うもの」は捕まえない。**ならし運転が別に要る。

---

## §44 第2章のオープニングと、番付の見せ方（2026-08-08・作者決定）

**第2章にはオープニングが1つも無かった**（`introStory: () => []`）。5場面・28行で書いた。
共有ファイルには触れていない（`js/ch2b/` のみ）。

### 伝えること4つ（説明ではなく場面で）

| 場面 | 絵 | 伝えること |
|---|---|---|
| ① | `gate` | 夕凪湯を建て直して親父に返した。あれから六年 |
| ② | `office` | 会社員に戻り、設備屋の営業へ。転勤で**横浜**へ |
| ③ | `y_living` | **結婚**（妻・奈津）。休みは二人でよその風呂。ノートが増える |
| ④ | `y_date_minato1` | **一念発起して起業。**奈津は二年前から貯めていた |
| ⑤ | `y_tenku_sauna_in` | **横浜サウナバトル。**八部門八百点、王者は天空スパの650点 |

絵はすべて既存の使い回し。**1枚目を夕凪湯の門にして、第1章の続きであることを台詞ではなく絵で出す。**

> ⚠ **一枚絵の文字送りはタグを解釈しない**（textContent）＝`<b>` も `**` も画面にそのまま出る。
> 最初 `**横浜サウナバトル。**` と書いてアスタリスクが表示された。強調は言葉の置き方でやる。

### 番付は最初からデータ欄に置く

`battleKnown` を **既定 true**（旧セーブも移行時に true）。
「五軒すべてを回ると並びが出る」という一枚を消した。

**伏せるのは「よその店の数字」だけ**（`visited` で判定・「？」表示）＝**偵察に行く理由はそのまま残る。**
実測：1日目の番付タブに6軒が並び、自分以外は全部「？」、
「『？』はまだ行っていない店。休みの日に見に行くと数字が出る」の説明が出る。

### 【本物のバグ】家の絵が一度も出ていなかった

`STORY_IMG['y_living']` の登録が `js/ch2b/shokudo_y.js:457` にあったが、
このファイルは **index.html の422行＝`js/story.js`（434行）より先に読まれる。**
`typeof STORY_IMG !== 'undefined'` が常に false ＝**登録が一度も走っていなかった。**

影響は3か所。**源さんの場面**（野毛の板前）、**月曜の定休日の一枚**、そして今回のオープニング。
どれも家の絵が出ないまま文字だけ流れていた。
登録を `odekake_y.js`（story.js のあと）へ移した。

> ⚠ 家の絵だけ**ファイル名とキーが食い違う**（`living.webp` ↔ `y_living`）。
> `living` と書いても出ない。

### 44-1 視察の台本を**紹介体**に統一した（作者決定 8/8）

冒頭で「横浜に何年も住み、休みのたびに二人でよその風呂へ通った」と語った以上、
**五軒はとっくに知っている。**なのに台本は場所の説明から入り、初めて来た人の目線だった。
**冒頭を書いたことで、こちらが矛盾を作っていた。**

しかも**文体が揃っていなかった。**天空スパだけ「横浜のサウナの聖地と言えば、間違いなくここだ」＝
紹介体で、残り四軒は「〜していた」の体験体。

**五軒とも紹介体に統一。**店の名前を頭に置き、惚れている人間としてプレイヤーに紹介する。

> ⚠ **主人公にマイナスは言わせない**（作者指定）。「看板は小さい」「派手さはない」
> 「最新鋭ではない」「初めて来た人間なら少し身構えるかもしれない」は全部落とした。
> **穴を明かすのは、その店の客の役目**（`RIVAL_TALK_Y`）＝設計の芯を守る。
> 実測：改稿後にマイナスに読める行として残ったのは2行だけで、**どちらも客の台詞**
> （ハマサウナの寝椅子の男／福来湯の番台前の爺さん）。地の文には1行も無い。
>
> ⚠ ただし**「うちは〜」という自分の店への引け目は残す。**
> それは相手の欠点ではなく、この章を動かしている当人の話だから。

一度入れた「初回だけ二行足して枠を変える」（`RIVAL_FIRST_Y`）は、
**紹介体そのものが「知っている」を含むので不要になり、丸ごと削除した。**

| 店 | 改稿後の1行目 | 行数 |
|---|---|---|
| 天空スパ | 天空スパ。横浜のサウナの聖地と言えば、間違いなくここだ。 | 29 |
| 楽園スパ | 楽園スパ。ベイブリッジを望む好立地に建つ、上場企業の大箱だ。 | 30 |
| ハマサウナ | ハマサウナ。中華街のど真ん中にある、観光客にも人気の一軒だ。 | 26 |
| スパ・ルミナ | スパ・ルミナ。横浜駅西口から歩いて五分の、天然温泉だ。 | 28 |
| 福来湯 | 福来湯。うちの近所の、昔ながらの銭湯だ。 | 20 |

### 44-2 作者の手直し（8/8）

- 楽園スパの1行目を **ベイブリッジを望む好立地** に
- ハマサウナの1行目を **観光客にも人気の一軒** に
  → 客の立ち話が ▲「観光客と、墨」「客が地元じゃない」を言うので、
    **主人公が誉めた点を、そのまま客が穴として返す**形になった（仕込みと回収が噛み合った）
- 福来湯の湯を **電気、ジャグジー、ラドン** に整理し、締めを
  **「これだけ揃って、サウナ込み八百七十円。／——このコスパと四十年の歴史に、俺はどう立ち向かう。」** に

> ⚠ 作者の原文は「40年」だが、**地の文の数字は漢数字**という決まり（俺のサウナの地の文ルール）に
> 合わせて「四十年」にした。周りの「八百七十円」「一万冊」とも揃う。
>
> ⚠ **残った不整合**：楽園スパの本文に「みなとみらいが視界の端まで広がる」「奥の窓の向こうには、みなとみらい」
> が2行ある。1行目をベイブリッジに変えたので、見える方角が食い違う。**作者の判断待ちで直していない。**

### 44-3 オープニング、作者の改稿（8/8）

3枚目以降を作者が書き直した。5場面27行。

- 3枚目：「取引先の経理にいた人」→ **社内結婚**。「サウナデートばっかり」で二人の過去を1行に畳んだ
- 4枚目：起業の切り出しを **「俺たちのサウナを作らないか？」** に。**物件（伊勢佐木町）を先に押さえてある**という
  段取りの良さが出て、通帳の返しが効く。「三冊目のノート」で年数が具体になった
- 5枚目：**「私たちも出場するの？」「いつか横浜一のサウナを作りたい」**。
  一人称の決意から、**二人の挑戦**へ変わった

こちらで整えたのは2点だけ。
`なあ... ` の半角三点リーダを **`なあ……`** に（同じ場面の「……二年前？」と揃える）、
`作らないか？伊勢佐木町` に **全角アキ** を入れた。

点数の説明が落ちていたので、**5枚目に一行だけ戻した**（作者指定 8/8）。

```
横浜サウナバトル。六年連続の王者は天空スパ。
八つの部門を、それぞれ百点で採点する。合わせて八百点。王者の持ち点は、六百五十点。   ← 足した
奈津「……私たちも出場するの？」
```

これで**上の帯の `スコア N/800` に意味が渡る。**奈津の「私たちも出場するの？」が、
六百五十という数字を聞いた直後の反応になるので、置き場所としても噛み合う。
最終：5場面28行。

### 44-4 4枚目に描き下ろしの一枚を入れた（8/8）

作者が用意した**夜のみなとみらいと観覧車**（時計が 20:19）を、冒頭4枚目に。

`~/Downloads` の PNG（1672×941・2.4MB）を、既存の一枚絵に合わせて
**1280×720 の webp（256KB・quality 88）**へ変換し、`assets/story/y_intro_minato.webp` として置いた。
登録は `odekake_y.js`（`js/story.js` のあとに読まれる側）。

これまで4枚目は `y_date_minato1` を使い回していたが、あれは**昼のワールドポーターズ**で、
「ある夜、みなとみらいの観覧車の下で言った」と噛み合っていなかった。
時計が 20:19 を指しているので、**夜であることが絵の中に書いてある。**

> ⚠ 名前を `y_date_minato*` の並びに入れていない。あの並びは
> `odekake_y.js` の一覧から自動で登録され、**妻とのデートの行き先として使われる**ため。
> 冒頭専用なので `y_intro_minato` として別に登録した。

---

## §45 運営タブを2段に／データ画面が開けなかった（2026-08-08）

### 45-1 【本物のバグ】データ画面が丸ごと開かなかった

`yRepMain`（`js/ch2b/battle_y.js`）で **`known is not defined`** の例外。
番付を最初から出すようにした日（§44）に**変数 `known` の宣言だけ消して、
下の参照（`if (!known)`）を1か所残していた。**

例外が `renderData` の途中で飛ぶので、**データ画面が1行も描かれず、モーダルも開かない。**
案内文（「五軒すべて回ると並びが出る」）自体も不要になっていたので、行ごと落とした。

> ⚠ **消すときは宣言だけでなく参照も全部数える。**`grep -c known` を打っていれば
> その場で分かった。ここまでの一連で、同じ「消し残し」は2回目
> （`shopTabOf` は死んだ関数として残っていた／今回は死んだ参照）。

### 45-2 運営タブは**2段**（作者指定）

横スクロールにしていたが、**右端のタブが画面の外に隠れて気づかれない。**
`.opt-tabs.many` を `flex-wrap:wrap` ＋ 各タブ `flex:1 1 calc(50% - 3px)` にして、
**2×2 で全部いちどに見せる。**寄せるための `scrollLeft` の処理は要らなくなったので削除した。

**実測**

| | 段数 | タブの幅 | 帯の高さ |
|---|---|---|---|
| 第2章（4タブ） | **2段** | 146px ×4 | 84px |
| 第1章（2タブ） | 1段 | 146px ×2 | 39px |

第1章は `many` が付かないので**1段・146px のまま**（従来どおり）。
4タブとも押して切り替わることも確認した。

---

## §46 外観図を「絵を叩く」形に作り替えた（2026-08-08・作者決定）

**メニューを開いて名前で選ぶのをやめ、絵の中のその場所を押す。**

| 前 | 後 |
|---|---|
| 下のパネルの【🏗 増築】タブでカードを選ぶ | **未建設の階の【＋】**を押す → 「3階（女湯）を増築しますか？」 |
| 下のパネルの【🪧 外観】タブで看板を選ぶ | **ビルの横の【＋ 看板】**を押す → どれを出すか選ぶ |
| 下のパネルは3タブ（外観・集客・増築） | **集客だけ**（タブ自体を出さない。1枚のタブは押す意味がない） |

### 中身

- `Y_ZOU_RECTS`（art_y.js）… 未建設の階の【＋】。**出すのは次の1階だけ**
  （増築は下から順にしか積めないので、上の階に＋を出すと押せて当然のように見える）
- `Y_SIGN_RECTS` … **常に出す**ようにした。以前は「場所を選んでいる間だけ」だったが、
  外観メニューを畳んだのでここが唯一の入口になった。
  何も付いていない階に **【＋ 看板】**、看板が出ている階には出さない
- `yAskZou(f)` / `yAskSign(f)` … 確認の一枚。**`confirm()` は使わない**（この作品では禁止）。
  第2章が持っている `kaigyoModal` を借りて描く

> ⚠ 借りたモーダルが自前で持つ【とじる】は「やめる」と重なるので、
> **開いている間だけ畳んで、閉じるとき必ず戻す**（`yLendKaigyo`）。
> 戻し忘れると**開業準備の画面が閉じられなくなる**。

> ⚠ **戻せるようにしてある。**`yRenderZou` も `Y_PANEL_TAB` の分岐も消していないので、
> `yRenderGaikan` の `tabs.innerHTML` に3枚を並べ直せば元のメニューが生き返る。

**実測**：2階建ての状態で ＋が「3階」に1つ・看板の＋が「2階」に1つ。
＋を押すと「🏗 3階（女湯）を増築しますか？」（工事代・工期・所持金・条件つき）。
看板の＋を押すと袖看板／ネオン／屋上の3択が出て、袖看板を出すと ¥300,000 引かれ、
その階の【＋ 看板】が消える。下のパネルは広告だけになった。

## §47 タブは2段（データ欄も・作者指定 8/8）

`renderData` のタブも、運営メニューと同じ「3つ以上なら2段」に揃えた。

| | 段数 | 幅 |
|---|---|---|
| 第2章 データ（評判・経営・ライバル・スキル） | **2段** | 146px ×4 |
| 第1章 データ（評判・経営） | 1段 | 従来どおり |

---

## §48 導線の採点を、浴室ごとに測る（2026-08-09）

**第1章の点は1点も動きません。** フックを持たない章は、これまでとまったく同じ1回の計算がそのまま走ります。
同じ盤面で前後を突き合わせて確認済み（4区間とも距離・配点が一致、合計10.0で同値）。

### 直したバグ（複数フロアの章でだけ起きる）

第2章で実測して見つけたものです。

**① 点数が「いま見ている階」で変わっていた**

`dosenParts()` の扉の座標が `CONF.doorX` / `CONF.divideY` ＝**表示中の区画**の値でした。
第2章で1Fを表示していると `divideY = 0` になり、扉が y=−1 という存在しない場所に置かれます。

| 見ている階 | 入口→洗い場 | 導線の点 |
|---|---|---|
| 1F表示中 | 13マス | 6.2 |
| 2F表示中 | 1マス | 8.7 |

まったく同じ店・同じ配置です。1Fを見たまま夜を迎えると、その点が評判に記録されていました。

**② 階をまたいでいるのに「隣」と数えていた**

`catDist` → `liveOf(cat)` がカテゴリだけで絞り、`equipRect` は x,y しか見ていませんでした。
サウナ=2F・水風呂=3F の店で「サウナ → 水風呂 = 2マス」。男湯に水風呂が1台も無いのに高得点。

### 変えたところ

| 場所 | 変更 |
|---|---|
| `liveOf(cat, f)` | 第2引数 `f` を追加。**省けばこれまでどおり全部**。既存の呼び出しは1つも挙動が変わらない |
| `catDist(a, b, f)` | 同上。`f` を渡した時だけその階の設備で測る |
| `dosenLegsOn(doorX, divideY, f)` | **新設**。1つの浴室について4区間を測る（旧 `dosenParts` の中身をそのまま出しただけ） |
| `dosenParts()` | `chHook('dosenFloors')` を見る。**無ければ `dosenLegsOn(CONF.doorX, CONF.divideY)` を1回＝従来と同一。** あれば浴室ごとに測って**いちばん悪い浴室**を採る |

新しいフック名：**`dosenFloors`** … `[{ f, name, doorX, divideY }]` を返す。第2章は `yDosenFloors`（男湯・女湯）。
**客が使う設備が1つも無い浴室は返さない**＝3階を建てた直後に0点で数えて「増築が罰になる」のを防ぐため
（清潔度の `cleanFloors` と同じ考え方）。

### ついでに直した表示のバグ（第1章にも出ていました）

評判画面の「次にやること」が `dosenParts().list.filter(x => x.v < 3)` で絞っていました。
区間の配点は 3マス以内=**2.5**／4マス=1.2／5マス以上=0 なので、**満点の区間まで「遠い」に数えていました。**
4本とも3マス以内の完璧な店で「遠い：入口 → 洗い場 0マス」と出ます。**点数には影響せず、表示だけの取り違えです。**

`x.v < 2.5` に直し、遠い順に並べました。第1章で完璧な配置を作って確認：修正前は「遠い：入口 → 洗い場 0マス」、
修正後は何も出ません（点は修正前後とも10.0）。

浴室が複数ある章では、文言の頭に**どの浴室か**を付けます（例：`遠い：女湯：風呂 → サウナ どちらかが無い ほか2件`）。
第1章は `worstName` を持たないので、これまでどおり浴室名は付きません。

### 確認したこと

- `await guardAll()` → `[{ ok: true, chapter: 1 }, null]`
- 第1章・同一盤面での前後比較＝4区間の距離も配点も合計も完全一致
- 第2章①＝1F表示中／2F表示中とも 7.5 で同値
- 第2章②＝3Fを建てた直後（空っぽ）は採点対象が「男湯」だけ＝点が動かない
- 第2章③＝3Fに水風呂だけ置くと「女湯 2.5点（風呂→サウナ どちらかが無い）」を拾い、店全体も2.5点

---

## §49 歩数の数え方が、フレームの粗さで変わっていた（2026-08-09）

第1章セッションからの指摘。`stepMove`（js/game.js:1486あたり）が、**「その1フレームで次のマスに着いた」ぶんを数えていませんでした。**
加算行が `if (dist <= step) { … return }` の**後ろ**にあったためです。

同じ店・同じ日で `dt` を変えると、歩数の中央値が **0／4／24／21** と動きます。
実機では**動きの粗い端末ほど歩数が減り、導線の文句も減る**という形で出ます。

### 第1章は直しません（`CONF.walkExact` で分岐）

最初、分岐なしで「実際に動いたぶんを常に足す」形に直したところ、**`guardAll` が落ちました。**

```
［設備を足した店］の売上：29,700 → 30,300
［設備を足した店］の収支： 6,070 →  6,670
```

歩数が変われば満足度が動き、満足度が動けば売上が動きます。**第1章は基準95に一度も届かない**
（第1章セッターの実測：最長47マス／こちらの実測：最長51マス・281人中0人）ので、**直す実利もありません。**

```js
if (!chHook('noWalkCount', e)) {
  const moved = dist <= step ? (CONF.walkExact ? dist : 0) : step;
  if (moved) e.walkPx = (e.walkPx || 0) + moved;
}
```

`CONF.walkExact` を持たない章＝第1章は、**到着ぶんを足さない**＝これまでとまったく同じ数え方です。
`js/data.js` の `CONF` には足していません（第2章は `CONF_Y.walkExact = true`）。

### 直したあとの第2章（dt=0.05・3日ぶん・約220人／件）

| 店の作り | 客層 | 中央値 | 90% | 基準超え |
|---|---|---|---|---|
| 固めた店 | 一般（基準95） | 48 | 76 | **1%** |
| 固めた店 | 玄人（基準70） | 66 | 89 | **40%** |
| ばらけた店 | 一般（基準95） | 61 | 118 | 23% |
| ばらけた店 | 玄人（基準70） | 86 | 121 | 62% |

**一般客の基準95は、第2章でもよく効いています**（1% → 23%）。
**玄人の基準70だけが第2章では厳しすぎます。** 完璧に固めた配置でも4割が文句を言い、
ばらけた店（62%）との差もつきにくい。**`CONF_Y.dosenProTiles` の見直しが要ります（作者判断待ち）。**

### 確認したこと

- `await guardAll()` → `[{ ok: true, chapter: 1 }, null]`（分岐を入れる前は `ok: false`）
- 第1章＝281人・4日ぶんで基準超え0人（最長51マス）は分岐後も変わらず
